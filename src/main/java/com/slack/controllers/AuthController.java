/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.AuthRevokeErrorSchemaException;
import com.slack.exceptions.AuthTestErrorSchemaException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.AuthRevokeSchema;
import com.slack.models.AuthTestSuccessSchema;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class AuthController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public AuthController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Revokes a token.
     * @param  token  Required parameter: Authentication token. Requires scope: `none`
     * @param  test  Optional parameter: Setting this parameter to `1` triggers a _testing mode_
     *         where the specified token will not actually be revoked.
     * @return    Returns the AuthRevokeSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<AuthRevokeSchema> authRevoke(
            final String token,
            final Boolean test) throws ApiException, IOException {
        return prepareAuthRevokeRequest(token, test).execute();
    }

    /**
     * Revokes a token.
     * @param  token  Required parameter: Authentication token. Requires scope: `none`
     * @param  test  Optional parameter: Setting this parameter to `1` triggers a _testing mode_
     *         where the specified token will not actually be revoked.
     * @return    Returns the AuthRevokeSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<AuthRevokeSchema>> authRevokeAsync(
            final String token,
            final Boolean test) {
        try {
            return prepareAuthRevokeRequest(token, test).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for authRevoke.
     */
    private ApiCall<ApiResponse<AuthRevokeSchema>, ApiException> prepareAuthRevokeRequest(
            final String token,
            final Boolean test) {
        return new ApiCall.Builder<ApiResponse<AuthRevokeSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/auth.revoke")
                        .queryParam(param -> param.key("token")
                                .value(token))
                        .queryParam(param -> param.key("test")
                                .value(test).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, AuthRevokeSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new AuthRevokeErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Checks authentication &amp; identity.
     * @param  token  Required parameter: Authentication token. Requires scope: `none`
     * @return    Returns the AuthTestSuccessSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<AuthTestSuccessSchema> authTest(
            final String token) throws ApiException, IOException {
        return prepareAuthTestRequest(token).execute();
    }

    /**
     * Checks authentication &amp; identity.
     * @param  token  Required parameter: Authentication token. Requires scope: `none`
     * @return    Returns the AuthTestSuccessSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<AuthTestSuccessSchema>> authTestAsync(
            final String token) {
        try {
            return prepareAuthTestRequest(token).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for authTest.
     */
    private ApiCall<ApiResponse<AuthTestSuccessSchema>, ApiException> prepareAuthTestRequest(
            final String token) {
        return new ApiCall.Builder<ApiResponse<AuthTestSuccessSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/auth.test")
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, AuthTestSuccessSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Standard failure response when used with an invalid token",
                                (reason, context) -> new AuthTestErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}