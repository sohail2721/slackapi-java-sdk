/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.DefaultErrorTemplateException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.DefaultSuccessTemplate;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class SearchController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public SearchController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Searches for messages matching a query.
     * @param  token  Required parameter: Authentication token. Requires scope: `search:read`
     * @param  query  Required parameter: Search query.
     * @param  count  Optional parameter: Pass the number of results you want per "page". Maximum of
     *         `100`.
     * @param  highlight  Optional parameter: Pass a value of `true` to enable query highlight
     *         markers (see below).
     * @param  page  Optional parameter:
     * @param  sort  Optional parameter: Return matches sorted by either `score` or `timestamp`.
     * @param  sortDir  Optional parameter: Change sort direction to ascending (`asc`) or descending
     *         (`desc`).
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> searchMessages(
            final String token,
            final String query,
            final Integer count,
            final Boolean highlight,
            final Integer page,
            final String sort,
            final String sortDir) throws ApiException, IOException {
        return prepareSearchMessagesRequest(token, query, count, highlight, page, sort,
                sortDir).execute();
    }

    /**
     * Searches for messages matching a query.
     * @param  token  Required parameter: Authentication token. Requires scope: `search:read`
     * @param  query  Required parameter: Search query.
     * @param  count  Optional parameter: Pass the number of results you want per "page". Maximum of
     *         `100`.
     * @param  highlight  Optional parameter: Pass a value of `true` to enable query highlight
     *         markers (see below).
     * @param  page  Optional parameter:
     * @param  sort  Optional parameter: Return matches sorted by either `score` or `timestamp`.
     * @param  sortDir  Optional parameter: Change sort direction to ascending (`asc`) or descending
     *         (`desc`).
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> searchMessagesAsync(
            final String token,
            final String query,
            final Integer count,
            final Boolean highlight,
            final Integer page,
            final String sort,
            final String sortDir) {
        try {
            return prepareSearchMessagesRequest(token, query, count, highlight, page, sort,
            sortDir).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for searchMessages.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareSearchMessagesRequest(
            final String token,
            final String query,
            final Integer count,
            final Boolean highlight,
            final Integer page,
            final String sort,
            final String sortDir) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/search.messages")
                        .queryParam(param -> param.key("token")
                                .value(token))
                        .queryParam(param -> param.key("query")
                                .value(query))
                        .queryParam(param -> param.key("count")
                                .value(count).isRequired(false))
                        .queryParam(param -> param.key("highlight")
                                .value(highlight).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .queryParam(param -> param.key("sort")
                                .value(sort).isRequired(false))
                        .queryParam(param -> param.key("sort_dir")
                                .value(sortDir).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}