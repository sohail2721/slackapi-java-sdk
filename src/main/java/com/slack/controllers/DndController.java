/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.DefaultErrorTemplateException;
import com.slack.exceptions.DndEndDndErrorSchemaException;
import com.slack.exceptions.DndEndSnoozeErrorSchemaException;
import com.slack.exceptions.DndInfoErrorSchemaException;
import com.slack.exceptions.DndSetSnoozeErrorSchemaException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.DefaultSuccessTemplate;
import com.slack.models.DndEndDndSchema;
import com.slack.models.DndEndSnoozeSchema;
import com.slack.models.DndInfoSchema;
import com.slack.models.DndSetSnoozeSchema;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class DndController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public DndController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Ends the current user's Do Not Disturb session immediately.
     * @param  token  Required parameter: Authentication token. Requires scope: `dnd:write`
     * @return    Returns the DndEndDndSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DndEndDndSchema> dndEndDnd(
            final String token) throws ApiException, IOException {
        return prepareDndEndDndRequest(token).execute();
    }

    /**
     * Ends the current user's Do Not Disturb session immediately.
     * @param  token  Required parameter: Authentication token. Requires scope: `dnd:write`
     * @return    Returns the DndEndDndSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DndEndDndSchema>> dndEndDndAsync(
            final String token) {
        try {
            return prepareDndEndDndRequest(token).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for dndEndDnd.
     */
    private ApiCall<ApiResponse<DndEndDndSchema>, ApiException> prepareDndEndDndRequest(
            final String token) {
        return new ApiCall.Builder<ApiResponse<DndEndDndSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/dnd.endDnd")
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DndEndDndSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DndEndDndErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Ends the current user's snooze mode immediately.
     * @param  token  Required parameter: Authentication token. Requires scope: `dnd:write`
     * @return    Returns the DndEndSnoozeSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DndEndSnoozeSchema> dndEndSnooze(
            final String token) throws ApiException, IOException {
        return prepareDndEndSnoozeRequest(token).execute();
    }

    /**
     * Ends the current user's snooze mode immediately.
     * @param  token  Required parameter: Authentication token. Requires scope: `dnd:write`
     * @return    Returns the DndEndSnoozeSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DndEndSnoozeSchema>> dndEndSnoozeAsync(
            final String token) {
        try {
            return prepareDndEndSnoozeRequest(token).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for dndEndSnooze.
     */
    private ApiCall<ApiResponse<DndEndSnoozeSchema>, ApiException> prepareDndEndSnoozeRequest(
            final String token) {
        return new ApiCall.Builder<ApiResponse<DndEndSnoozeSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/dnd.endSnooze")
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DndEndSnoozeSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DndEndSnoozeErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Retrieves a user's current Do Not Disturb status.
     * @param  token  Optional parameter: Authentication token. Requires scope: `dnd:read`
     * @param  user  Optional parameter: User to fetch status for (defaults to current user)
     * @return    Returns the DndInfoSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DndInfoSchema> dndInfo(
            final String token,
            final String user) throws ApiException, IOException {
        return prepareDndInfoRequest(token, user).execute();
    }

    /**
     * Retrieves a user's current Do Not Disturb status.
     * @param  token  Optional parameter: Authentication token. Requires scope: `dnd:read`
     * @param  user  Optional parameter: User to fetch status for (defaults to current user)
     * @return    Returns the DndInfoSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DndInfoSchema>> dndInfoAsync(
            final String token,
            final String user) {
        try {
            return prepareDndInfoRequest(token, user).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for dndInfo.
     */
    private ApiCall<ApiResponse<DndInfoSchema>, ApiException> prepareDndInfoRequest(
            final String token,
            final String user) {
        return new ApiCall.Builder<ApiResponse<DndInfoSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/dnd.info")
                        .queryParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .queryParam(param -> param.key("user")
                                .value(user).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DndInfoSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DndInfoErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Turns on Do Not Disturb mode for the current user, or changes its duration.
     * @param  token  Required parameter: Authentication token. Requires scope: `dnd:write`
     * @param  numMinutes  Required parameter: Number of minutes, from now, to snooze until.
     * @return    Returns the DndSetSnoozeSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DndSetSnoozeSchema> dndSetSnooze(
            final String token,
            final String numMinutes) throws ApiException, IOException {
        return prepareDndSetSnoozeRequest(token, numMinutes).execute();
    }

    /**
     * Turns on Do Not Disturb mode for the current user, or changes its duration.
     * @param  token  Required parameter: Authentication token. Requires scope: `dnd:write`
     * @param  numMinutes  Required parameter: Number of minutes, from now, to snooze until.
     * @return    Returns the DndSetSnoozeSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DndSetSnoozeSchema>> dndSetSnoozeAsync(
            final String token,
            final String numMinutes) {
        try {
            return prepareDndSetSnoozeRequest(token, numMinutes).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for dndSetSnooze.
     */
    private ApiCall<ApiResponse<DndSetSnoozeSchema>, ApiException> prepareDndSetSnoozeRequest(
            final String token,
            final String numMinutes) {
        return new ApiCall.Builder<ApiResponse<DndSetSnoozeSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/dnd.setSnooze")
                        .formParam(param -> param.key("token")
                                .value(token))
                        .formParam(param -> param.key("num_minutes")
                                .value(numMinutes))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DndSetSnoozeSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DndSetSnoozeErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Retrieves the Do Not Disturb status for up to 50 users on a team.
     * @param  token  Optional parameter: Authentication token. Requires scope: `dnd:read`
     * @param  users  Optional parameter: Comma-separated list of users to fetch Do Not Disturb
     *         status for
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> dndTeamInfo(
            final String token,
            final String users) throws ApiException, IOException {
        return prepareDndTeamInfoRequest(token, users).execute();
    }

    /**
     * Retrieves the Do Not Disturb status for up to 50 users on a team.
     * @param  token  Optional parameter: Authentication token. Requires scope: `dnd:read`
     * @param  users  Optional parameter: Comma-separated list of users to fetch Do Not Disturb
     *         status for
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> dndTeamInfoAsync(
            final String token,
            final String users) {
        try {
            return prepareDndTeamInfoRequest(token, users).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for dndTeamInfo.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareDndTeamInfoRequest(
            final String token,
            final String users) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/dnd.teamInfo")
                        .queryParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .queryParam(param -> param.key("users")
                                .value(users).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}