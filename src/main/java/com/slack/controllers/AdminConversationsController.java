/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.AdminConversationsArchiveErrorSchemaException;
import com.slack.exceptions.AdminConversationsConvertToPrivateErrorSchemaException;
import com.slack.exceptions.AdminConversationsCreateErrorSchemaException;
import com.slack.exceptions.AdminConversationsDeleteErrorSchemaException;
import com.slack.exceptions.AdminConversationsDisconnectSharedErrorSchemaException;
import com.slack.exceptions.AdminConversationsGetTeamsErrorSchemaException;
import com.slack.exceptions.AdminConversationsInviteErrorSchemaException;
import com.slack.exceptions.AdminConversationsSearchErrorSchemaException;
import com.slack.exceptions.AdminConversationsSetConversationPrefsErrorSchemaException;
import com.slack.exceptions.AdminConversationsUnarchiveErrorSchema2Exception;
import com.slack.exceptions.AdminConversationsUnarchiveErrorSchema3Exception;
import com.slack.exceptions.AdminConversationsUnarchiveErrorSchemaException;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.DefaultErrorTemplateException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.AdminConversationsArchiveSchema;
import com.slack.models.AdminConversationsConvertToPrivateSchema;
import com.slack.models.AdminConversationsCreateSchema;
import com.slack.models.AdminConversationsDeleteSchema;
import com.slack.models.AdminConversationsGetConversationPrefsSchema;
import com.slack.models.AdminConversationsGetTeamsSchema;
import com.slack.models.AdminConversationsInviteSchema;
import com.slack.models.AdminConversationsRenameSchema;
import com.slack.models.AdminConversationsRenameSchema1;
import com.slack.models.AdminConversationsSearchSchema;
import com.slack.models.AdminConversationsSetConversationPrefsSchema;
import com.slack.models.AdminConversationsUnarchiveSchema;
import com.slack.models.DefaultSuccessTemplate;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class AdminConversationsController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public AdminConversationsController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Archive a public or private channel.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:write`
     * @param  channelId  Required parameter: The channel to archive.
     * @return    Returns the AdminConversationsArchiveSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<AdminConversationsArchiveSchema> adminConversationsArchive(
            final String token,
            final String channelId) throws ApiException, IOException {
        return prepareAdminConversationsArchiveRequest(token, channelId).execute();
    }

    /**
     * Archive a public or private channel.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:write`
     * @param  channelId  Required parameter: The channel to archive.
     * @return    Returns the AdminConversationsArchiveSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<AdminConversationsArchiveSchema>> adminConversationsArchiveAsync(
            final String token,
            final String channelId) {
        try {
            return prepareAdminConversationsArchiveRequest(token, channelId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminConversationsArchive.
     */
    private ApiCall<ApiResponse<AdminConversationsArchiveSchema>, ApiException> prepareAdminConversationsArchiveRequest(
            final String token,
            final String channelId) {
        return new ApiCall.Builder<ApiResponse<AdminConversationsArchiveSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.conversations.archive")
                        .formParam(param -> param.key("channel_id")
                                .value(channelId))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, AdminConversationsArchiveSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new AdminConversationsArchiveErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Convert a public channel to a private channel.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:write`
     * @param  channelId  Required parameter: The channel to convert to private.
     * @return    Returns the AdminConversationsConvertToPrivateSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<AdminConversationsConvertToPrivateSchema> adminConversationsConvertToPrivate(
            final String token,
            final String channelId) throws ApiException, IOException {
        return prepareAdminConversationsConvertToPrivateRequest(token, channelId).execute();
    }

    /**
     * Convert a public channel to a private channel.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:write`
     * @param  channelId  Required parameter: The channel to convert to private.
     * @return    Returns the AdminConversationsConvertToPrivateSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<AdminConversationsConvertToPrivateSchema>> adminConversationsConvertToPrivateAsync(
            final String token,
            final String channelId) {
        try {
            return prepareAdminConversationsConvertToPrivateRequest(token, channelId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminConversationsConvertToPrivate.
     */
    private ApiCall<ApiResponse<AdminConversationsConvertToPrivateSchema>, ApiException> prepareAdminConversationsConvertToPrivateRequest(
            final String token,
            final String channelId) {
        return new ApiCall.Builder<ApiResponse<AdminConversationsConvertToPrivateSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.conversations.convertToPrivate")
                        .formParam(param -> param.key("channel_id")
                                .value(channelId))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, AdminConversationsConvertToPrivateSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new AdminConversationsConvertToPrivateErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Create a public or private channel-based conversation.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:write`
     * @param  name  Required parameter: Name of the public or private channel to create.
     * @param  isPrivate  Required parameter: When `true`, creates a private channel instead of a
     *         public channel
     * @param  description  Optional parameter: Description of the public or private channel to
     *         create.
     * @param  orgWide  Optional parameter: When `true`, the channel will be available org-wide.
     *         Note: if the channel is not `org_wide=true`, you must specify a `team_id` for this
     *         channel
     * @param  teamId  Optional parameter: The workspace to create the channel in. Note: this
     *         argument is required unless you set `org_wide=true`.
     * @return    Returns the AdminConversationsCreateSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<AdminConversationsCreateSchema> adminConversationsCreate(
            final String token,
            final String name,
            final boolean isPrivate,
            final String description,
            final Boolean orgWide,
            final String teamId) throws ApiException, IOException {
        return prepareAdminConversationsCreateRequest(token, name, isPrivate, description, orgWide,
                teamId).execute();
    }

    /**
     * Create a public or private channel-based conversation.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:write`
     * @param  name  Required parameter: Name of the public or private channel to create.
     * @param  isPrivate  Required parameter: When `true`, creates a private channel instead of a
     *         public channel
     * @param  description  Optional parameter: Description of the public or private channel to
     *         create.
     * @param  orgWide  Optional parameter: When `true`, the channel will be available org-wide.
     *         Note: if the channel is not `org_wide=true`, you must specify a `team_id` for this
     *         channel
     * @param  teamId  Optional parameter: The workspace to create the channel in. Note: this
     *         argument is required unless you set `org_wide=true`.
     * @return    Returns the AdminConversationsCreateSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<AdminConversationsCreateSchema>> adminConversationsCreateAsync(
            final String token,
            final String name,
            final boolean isPrivate,
            final String description,
            final Boolean orgWide,
            final String teamId) {
        try {
            return prepareAdminConversationsCreateRequest(token, name, isPrivate, description, orgWide,
            teamId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminConversationsCreate.
     */
    private ApiCall<ApiResponse<AdminConversationsCreateSchema>, ApiException> prepareAdminConversationsCreateRequest(
            final String token,
            final String name,
            final boolean isPrivate,
            final String description,
            final Boolean orgWide,
            final String teamId) {
        return new ApiCall.Builder<ApiResponse<AdminConversationsCreateSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.conversations.create")
                        .formParam(param -> param.key("name")
                                .value(name))
                        .formParam(param -> param.key("is_private")
                                .value(isPrivate).isRequired(false))
                        .formParam(param -> param.key("description")
                                .value(description).isRequired(false))
                        .formParam(param -> param.key("org_wide")
                                .value(orgWide).isRequired(false))
                        .formParam(param -> param.key("team_id")
                                .value(teamId).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, AdminConversationsCreateSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new AdminConversationsCreateErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Delete a public or private channel.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:write`
     * @param  channelId  Required parameter: The channel to delete.
     * @return    Returns the AdminConversationsDeleteSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<AdminConversationsDeleteSchema> adminConversationsDelete(
            final String token,
            final String channelId) throws ApiException, IOException {
        return prepareAdminConversationsDeleteRequest(token, channelId).execute();
    }

    /**
     * Delete a public or private channel.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:write`
     * @param  channelId  Required parameter: The channel to delete.
     * @return    Returns the AdminConversationsDeleteSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<AdminConversationsDeleteSchema>> adminConversationsDeleteAsync(
            final String token,
            final String channelId) {
        try {
            return prepareAdminConversationsDeleteRequest(token, channelId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminConversationsDelete.
     */
    private ApiCall<ApiResponse<AdminConversationsDeleteSchema>, ApiException> prepareAdminConversationsDeleteRequest(
            final String token,
            final String channelId) {
        return new ApiCall.Builder<ApiResponse<AdminConversationsDeleteSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.conversations.delete")
                        .formParam(param -> param.key("channel_id")
                                .value(channelId))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, AdminConversationsDeleteSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new AdminConversationsDeleteErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Disconnect a connected channel from one or more workspaces.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:write`
     * @param  channelId  Required parameter: The channel to be disconnected from some workspaces.
     * @param  leavingTeamIds  Optional parameter: The team to be removed from the channel.
     *         Currently only a single team id can be specified.
     * @return    Returns the AdminConversationsRenameSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<AdminConversationsRenameSchema> adminConversationsDisconnectShared(
            final String token,
            final String channelId,
            final String leavingTeamIds) throws ApiException, IOException {
        return prepareAdminConversationsDisconnectSharedRequest(token, channelId,
                leavingTeamIds).execute();
    }

    /**
     * Disconnect a connected channel from one or more workspaces.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:write`
     * @param  channelId  Required parameter: The channel to be disconnected from some workspaces.
     * @param  leavingTeamIds  Optional parameter: The team to be removed from the channel.
     *         Currently only a single team id can be specified.
     * @return    Returns the AdminConversationsRenameSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<AdminConversationsRenameSchema>> adminConversationsDisconnectSharedAsync(
            final String token,
            final String channelId,
            final String leavingTeamIds) {
        try {
            return prepareAdminConversationsDisconnectSharedRequest(token, channelId,
            leavingTeamIds).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminConversationsDisconnectShared.
     */
    private ApiCall<ApiResponse<AdminConversationsRenameSchema>, ApiException> prepareAdminConversationsDisconnectSharedRequest(
            final String token,
            final String channelId,
            final String leavingTeamIds) {
        return new ApiCall.Builder<ApiResponse<AdminConversationsRenameSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.conversations.disconnectShared")
                        .formParam(param -> param.key("channel_id")
                                .value(channelId))
                        .formParam(param -> param.key("leaving_team_ids")
                                .value(leavingTeamIds).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, AdminConversationsRenameSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new AdminConversationsDisconnectSharedErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Get conversation preferences for a public or private channel.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:read`
     * @param  channelId  Required parameter: The channel to get preferences for.
     * @return    Returns the AdminConversationsGetConversationPrefsSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<AdminConversationsGetConversationPrefsSchema> adminConversationsGetConversationPrefs(
            final String token,
            final String channelId) throws ApiException, IOException {
        return prepareAdminConversationsGetConversationPrefsRequest(token, channelId).execute();
    }

    /**
     * Get conversation preferences for a public or private channel.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:read`
     * @param  channelId  Required parameter: The channel to get preferences for.
     * @return    Returns the AdminConversationsGetConversationPrefsSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<AdminConversationsGetConversationPrefsSchema>> adminConversationsGetConversationPrefsAsync(
            final String token,
            final String channelId) {
        try {
            return prepareAdminConversationsGetConversationPrefsRequest(token, channelId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminConversationsGetConversationPrefs.
     */
    private ApiCall<ApiResponse<AdminConversationsGetConversationPrefsSchema>, ApiException> prepareAdminConversationsGetConversationPrefsRequest(
            final String token,
            final String channelId) {
        return new ApiCall.Builder<ApiResponse<AdminConversationsGetConversationPrefsSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.conversations.getConversationPrefs")
                        .queryParam(param -> param.key("channel_id")
                                .value(channelId))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, AdminConversationsGetConversationPrefsSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new AdminConversationsUnarchiveErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Get all the workspaces a given public or private channel is connected to within this
     * Enterprise org.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:read`
     * @param  channelId  Required parameter: The channel to determine connected workspaces within
     *         the organization for.
     * @param  cursor  Optional parameter: Set `cursor` to `next_cursor` returned by the previous
     *         call to list items in the next page
     * @param  limit  Optional parameter: The maximum number of items to return. Must be between 1 -
     *         1000 both inclusive.
     * @return    Returns the AdminConversationsGetTeamsSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<AdminConversationsGetTeamsSchema> adminConversationsGetTeams(
            final String token,
            final String channelId,
            final String cursor,
            final Integer limit) throws ApiException, IOException {
        return prepareAdminConversationsGetTeamsRequest(token, channelId, cursor, limit).execute();
    }

    /**
     * Get all the workspaces a given public or private channel is connected to within this
     * Enterprise org.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:read`
     * @param  channelId  Required parameter: The channel to determine connected workspaces within
     *         the organization for.
     * @param  cursor  Optional parameter: Set `cursor` to `next_cursor` returned by the previous
     *         call to list items in the next page
     * @param  limit  Optional parameter: The maximum number of items to return. Must be between 1 -
     *         1000 both inclusive.
     * @return    Returns the AdminConversationsGetTeamsSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<AdminConversationsGetTeamsSchema>> adminConversationsGetTeamsAsync(
            final String token,
            final String channelId,
            final String cursor,
            final Integer limit) {
        try {
            return prepareAdminConversationsGetTeamsRequest(token, channelId, cursor, limit).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminConversationsGetTeams.
     */
    private ApiCall<ApiResponse<AdminConversationsGetTeamsSchema>, ApiException> prepareAdminConversationsGetTeamsRequest(
            final String token,
            final String channelId,
            final String cursor,
            final Integer limit) {
        return new ApiCall.Builder<ApiResponse<AdminConversationsGetTeamsSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.conversations.getTeams")
                        .queryParam(param -> param.key("channel_id")
                                .value(channelId))
                        .queryParam(param -> param.key("cursor")
                                .value(cursor).isRequired(false))
                        .queryParam(param -> param.key("limit")
                                .value(limit).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, AdminConversationsGetTeamsSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new AdminConversationsGetTeamsErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Invite a user to a public or private channel.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:write`
     * @param  userIds  Required parameter: The users to invite.
     * @param  channelId  Required parameter: The channel that the users will be invited to.
     * @return    Returns the AdminConversationsInviteSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<AdminConversationsInviteSchema> adminConversationsInvite(
            final String token,
            final String userIds,
            final String channelId) throws ApiException, IOException {
        return prepareAdminConversationsInviteRequest(token, userIds, channelId).execute();
    }

    /**
     * Invite a user to a public or private channel.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:write`
     * @param  userIds  Required parameter: The users to invite.
     * @param  channelId  Required parameter: The channel that the users will be invited to.
     * @return    Returns the AdminConversationsInviteSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<AdminConversationsInviteSchema>> adminConversationsInviteAsync(
            final String token,
            final String userIds,
            final String channelId) {
        try {
            return prepareAdminConversationsInviteRequest(token, userIds, channelId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminConversationsInvite.
     */
    private ApiCall<ApiResponse<AdminConversationsInviteSchema>, ApiException> prepareAdminConversationsInviteRequest(
            final String token,
            final String userIds,
            final String channelId) {
        return new ApiCall.Builder<ApiResponse<AdminConversationsInviteSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.conversations.invite")
                        .formParam(param -> param.key("user_ids")
                                .value(userIds))
                        .formParam(param -> param.key("channel_id")
                                .value(channelId))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, AdminConversationsInviteSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new AdminConversationsInviteErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Rename a public or private channel.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:write`
     * @param  channelId  Required parameter: The channel to rename.
     * @param  name  Required parameter:
     * @return    Returns the AdminConversationsRenameSchema1 wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<AdminConversationsRenameSchema1> adminConversationsRename(
            final String token,
            final String channelId,
            final String name) throws ApiException, IOException {
        return prepareAdminConversationsRenameRequest(token, channelId, name).execute();
    }

    /**
     * Rename a public or private channel.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:write`
     * @param  channelId  Required parameter: The channel to rename.
     * @param  name  Required parameter:
     * @return    Returns the AdminConversationsRenameSchema1 wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<AdminConversationsRenameSchema1>> adminConversationsRenameAsync(
            final String token,
            final String channelId,
            final String name) {
        try {
            return prepareAdminConversationsRenameRequest(token, channelId, name).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminConversationsRename.
     */
    private ApiCall<ApiResponse<AdminConversationsRenameSchema1>, ApiException> prepareAdminConversationsRenameRequest(
            final String token,
            final String channelId,
            final String name) {
        return new ApiCall.Builder<ApiResponse<AdminConversationsRenameSchema1>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.conversations.rename")
                        .formParam(param -> param.key("channel_id")
                                .value(channelId))
                        .formParam(param -> param.key("name")
                                .value(name))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, AdminConversationsRenameSchema1.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new AdminConversationsUnarchiveErrorSchema2Exception(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Search for public or private channels in an Enterprise organization.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:read`
     * @param  teamIds  Optional parameter: Comma separated string of team IDs, signifying the
     *         workspaces to search through.
     * @param  query  Optional parameter: Name of the the channel to query by.
     * @param  limit  Optional parameter: Maximum number of items to be returned. Must be between 1
     *         - 20 both inclusive. Default is 10.
     * @param  cursor  Optional parameter: Set `cursor` to `next_cursor` returned by the previous
     *         call to list items in the next page.
     * @param  searchChannelTypes  Optional parameter: The type of channel to include or exclude in
     *         the search. For example `private` will search private channels, while
     *         `private_exclude` will exclude them. For a full list of types, check the [Types
     *         section](#types).
     * @param  sort  Optional parameter: Possible values are `relevant` (search ranking based on
     *         what we think is closest), `name` (alphabetical), `member_count` (number of users in
     *         the channel), and `created` (date channel was created). You can optionally pair this
     *         with the `sort_dir` arg to change how it is sorted
     * @param  sortDir  Optional parameter: Sort direction. Possible values are `asc` for ascending
     *         order like (1, 2, 3) or (a, b, c), and `desc` for descending order like (3, 2, 1) or
     *         (c, b, a)
     * @return    Returns the AdminConversationsSearchSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<AdminConversationsSearchSchema> adminConversationsSearch(
            final String token,
            final String teamIds,
            final String query,
            final Integer limit,
            final String cursor,
            final String searchChannelTypes,
            final String sort,
            final String sortDir) throws ApiException, IOException {
        return prepareAdminConversationsSearchRequest(token, teamIds, query, limit, cursor,
                searchChannelTypes, sort, sortDir).execute();
    }

    /**
     * Search for public or private channels in an Enterprise organization.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:read`
     * @param  teamIds  Optional parameter: Comma separated string of team IDs, signifying the
     *         workspaces to search through.
     * @param  query  Optional parameter: Name of the the channel to query by.
     * @param  limit  Optional parameter: Maximum number of items to be returned. Must be between 1
     *         - 20 both inclusive. Default is 10.
     * @param  cursor  Optional parameter: Set `cursor` to `next_cursor` returned by the previous
     *         call to list items in the next page.
     * @param  searchChannelTypes  Optional parameter: The type of channel to include or exclude in
     *         the search. For example `private` will search private channels, while
     *         `private_exclude` will exclude them. For a full list of types, check the [Types
     *         section](#types).
     * @param  sort  Optional parameter: Possible values are `relevant` (search ranking based on
     *         what we think is closest), `name` (alphabetical), `member_count` (number of users in
     *         the channel), and `created` (date channel was created). You can optionally pair this
     *         with the `sort_dir` arg to change how it is sorted
     * @param  sortDir  Optional parameter: Sort direction. Possible values are `asc` for ascending
     *         order like (1, 2, 3) or (a, b, c), and `desc` for descending order like (3, 2, 1) or
     *         (c, b, a)
     * @return    Returns the AdminConversationsSearchSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<AdminConversationsSearchSchema>> adminConversationsSearchAsync(
            final String token,
            final String teamIds,
            final String query,
            final Integer limit,
            final String cursor,
            final String searchChannelTypes,
            final String sort,
            final String sortDir) {
        try {
            return prepareAdminConversationsSearchRequest(token, teamIds, query, limit, cursor,
            searchChannelTypes, sort, sortDir).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminConversationsSearch.
     */
    private ApiCall<ApiResponse<AdminConversationsSearchSchema>, ApiException> prepareAdminConversationsSearchRequest(
            final String token,
            final String teamIds,
            final String query,
            final Integer limit,
            final String cursor,
            final String searchChannelTypes,
            final String sort,
            final String sortDir) {
        return new ApiCall.Builder<ApiResponse<AdminConversationsSearchSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.conversations.search")
                        .queryParam(param -> param.key("team_ids")
                                .value(teamIds).isRequired(false))
                        .queryParam(param -> param.key("query")
                                .value(query).isRequired(false))
                        .queryParam(param -> param.key("limit")
                                .value(limit).isRequired(false))
                        .queryParam(param -> param.key("cursor")
                                .value(cursor).isRequired(false))
                        .queryParam(param -> param.key("search_channel_types")
                                .value(searchChannelTypes).isRequired(false))
                        .queryParam(param -> param.key("sort")
                                .value(sort).isRequired(false))
                        .queryParam(param -> param.key("sort_dir")
                                .value(sortDir).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, AdminConversationsSearchSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new AdminConversationsSearchErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Set the posting permissions for a public or private channel.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:write`
     * @param  channelId  Required parameter: The channel to set the prefs for
     * @param  prefs  Required parameter: The prefs for this channel in a stringified JSON format.
     * @return    Returns the AdminConversationsSetConversationPrefsSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<AdminConversationsSetConversationPrefsSchema> adminConversationsSetConversationPrefs(
            final String token,
            final String channelId,
            final String prefs) throws ApiException, IOException {
        return prepareAdminConversationsSetConversationPrefsRequest(token, channelId,
                prefs).execute();
    }

    /**
     * Set the posting permissions for a public or private channel.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:write`
     * @param  channelId  Required parameter: The channel to set the prefs for
     * @param  prefs  Required parameter: The prefs for this channel in a stringified JSON format.
     * @return    Returns the AdminConversationsSetConversationPrefsSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<AdminConversationsSetConversationPrefsSchema>> adminConversationsSetConversationPrefsAsync(
            final String token,
            final String channelId,
            final String prefs) {
        try {
            return prepareAdminConversationsSetConversationPrefsRequest(token, channelId,
            prefs).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminConversationsSetConversationPrefs.
     */
    private ApiCall<ApiResponse<AdminConversationsSetConversationPrefsSchema>, ApiException> prepareAdminConversationsSetConversationPrefsRequest(
            final String token,
            final String channelId,
            final String prefs) {
        return new ApiCall.Builder<ApiResponse<AdminConversationsSetConversationPrefsSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.conversations.setConversationPrefs")
                        .formParam(param -> param.key("channel_id")
                                .value(channelId))
                        .formParam(param -> param.key("prefs")
                                .value(prefs))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, AdminConversationsSetConversationPrefsSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new AdminConversationsSetConversationPrefsErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Set the workspaces in an Enterprise grid org that connect to a public or private channel.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:write`
     * @param  channelId  Required parameter: The encoded `channel_id` to add or remove to
     *         workspaces.
     * @param  teamId  Optional parameter: The workspace to which the channel belongs. Omit this
     *         argument if the channel is a cross-workspace shared channel.
     * @param  targetTeamIds  Optional parameter: A comma-separated list of workspaces to which the
     *         channel should be shared. Not required if the channel is being shared org-wide.
     * @param  orgChannel  Optional parameter: True if channel has to be converted to an org channel
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminConversationsSetTeams(
            final String token,
            final String channelId,
            final String teamId,
            final String targetTeamIds,
            final Boolean orgChannel) throws ApiException, IOException {
        return prepareAdminConversationsSetTeamsRequest(token, channelId, teamId, targetTeamIds,
                orgChannel).execute();
    }

    /**
     * Set the workspaces in an Enterprise grid org that connect to a public or private channel.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:write`
     * @param  channelId  Required parameter: The encoded `channel_id` to add or remove to
     *         workspaces.
     * @param  teamId  Optional parameter: The workspace to which the channel belongs. Omit this
     *         argument if the channel is a cross-workspace shared channel.
     * @param  targetTeamIds  Optional parameter: A comma-separated list of workspaces to which the
     *         channel should be shared. Not required if the channel is being shared org-wide.
     * @param  orgChannel  Optional parameter: True if channel has to be converted to an org channel
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminConversationsSetTeamsAsync(
            final String token,
            final String channelId,
            final String teamId,
            final String targetTeamIds,
            final Boolean orgChannel) {
        try {
            return prepareAdminConversationsSetTeamsRequest(token, channelId, teamId, targetTeamIds,
            orgChannel).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminConversationsSetTeams.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminConversationsSetTeamsRequest(
            final String token,
            final String channelId,
            final String teamId,
            final String targetTeamIds,
            final Boolean orgChannel) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.conversations.setTeams")
                        .formParam(param -> param.key("channel_id")
                                .value(channelId))
                        .formParam(param -> param.key("team_id")
                                .value(teamId).isRequired(false))
                        .formParam(param -> param.key("target_team_ids")
                                .value(targetTeamIds).isRequired(false))
                        .formParam(param -> param.key("org_channel")
                                .value(orgChannel).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Unarchive a public or private channel.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:write`
     * @param  channelId  Required parameter: The channel to unarchive.
     * @return    Returns the AdminConversationsUnarchiveSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<AdminConversationsUnarchiveSchema> adminConversationsUnarchive(
            final String token,
            final String channelId) throws ApiException, IOException {
        return prepareAdminConversationsUnarchiveRequest(token, channelId).execute();
    }

    /**
     * Unarchive a public or private channel.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:write`
     * @param  channelId  Required parameter: The channel to unarchive.
     * @return    Returns the AdminConversationsUnarchiveSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<AdminConversationsUnarchiveSchema>> adminConversationsUnarchiveAsync(
            final String token,
            final String channelId) {
        try {
            return prepareAdminConversationsUnarchiveRequest(token, channelId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminConversationsUnarchive.
     */
    private ApiCall<ApiResponse<AdminConversationsUnarchiveSchema>, ApiException> prepareAdminConversationsUnarchiveRequest(
            final String token,
            final String channelId) {
        return new ApiCall.Builder<ApiResponse<AdminConversationsUnarchiveSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.conversations.unarchive")
                        .formParam(param -> param.key("channel_id")
                                .value(channelId))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, AdminConversationsUnarchiveSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new AdminConversationsUnarchiveErrorSchema3Exception(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}