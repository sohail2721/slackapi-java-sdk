/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.RtmConnectErrorSchemaException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.RtmConnectSchema;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class RtmController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public RtmController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Starts a Real Time Messaging session.
     * @param  token  Required parameter: Authentication token. Requires scope: `rtm:stream`
     * @param  batchPresenceAware  Optional parameter: Batch presence deliveries via subscription.
     *         Enabling changes the shape of `presence_change` events. See [batch
     *         presence](/docs/presence-and-status#batching).
     * @param  presenceSub  Optional parameter: Only deliver presence events when requested by
     *         subscription. See [presence subscriptions](/docs/presence-and-status#subscriptions).
     * @return    Returns the RtmConnectSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<RtmConnectSchema> rtmConnect(
            final String token,
            final Boolean batchPresenceAware,
            final Boolean presenceSub) throws ApiException, IOException {
        return prepareRtmConnectRequest(token, batchPresenceAware, presenceSub).execute();
    }

    /**
     * Starts a Real Time Messaging session.
     * @param  token  Required parameter: Authentication token. Requires scope: `rtm:stream`
     * @param  batchPresenceAware  Optional parameter: Batch presence deliveries via subscription.
     *         Enabling changes the shape of `presence_change` events. See [batch
     *         presence](/docs/presence-and-status#batching).
     * @param  presenceSub  Optional parameter: Only deliver presence events when requested by
     *         subscription. See [presence subscriptions](/docs/presence-and-status#subscriptions).
     * @return    Returns the RtmConnectSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<RtmConnectSchema>> rtmConnectAsync(
            final String token,
            final Boolean batchPresenceAware,
            final Boolean presenceSub) {
        try {
            return prepareRtmConnectRequest(token, batchPresenceAware, presenceSub).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for rtmConnect.
     */
    private ApiCall<ApiResponse<RtmConnectSchema>, ApiException> prepareRtmConnectRequest(
            final String token,
            final Boolean batchPresenceAware,
            final Boolean presenceSub) {
        return new ApiCall.Builder<ApiResponse<RtmConnectSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/rtm.connect")
                        .queryParam(param -> param.key("token")
                                .value(token))
                        .queryParam(param -> param.key("batch_presence_aware")
                                .value(batchPresenceAware).isRequired(false))
                        .queryParam(param -> param.key("presence_sub")
                                .value(presenceSub).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, RtmConnectSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new RtmConnectErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}