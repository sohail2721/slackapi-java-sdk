/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.ChatScheduledMessagesListErrorSchemaException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.ChatScheduledMessagesListSchema;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class ChatScheduledMessagesController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public ChatScheduledMessagesController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Returns a list of scheduled messages.
     * @param  token  Optional parameter: Authentication token. Requires scope: `none`
     * @param  channel  Optional parameter: The channel of the scheduled messages
     * @param  latest  Optional parameter: A UNIX timestamp of the latest value in the time range
     * @param  oldest  Optional parameter: A UNIX timestamp of the oldest value in the time range
     * @param  limit  Optional parameter: Maximum number of original entries to return.
     * @param  cursor  Optional parameter: For pagination purposes, this is the `cursor` value
     *         returned from a previous call to `chat.scheduledmessages.list` indicating where you
     *         want to start this call from.
     * @return    Returns the ChatScheduledMessagesListSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<ChatScheduledMessagesListSchema> chatScheduledMessagesList(
            final String token,
            final String channel,
            final Double latest,
            final Double oldest,
            final Integer limit,
            final String cursor) throws ApiException, IOException {
        return prepareChatScheduledMessagesListRequest(token, channel, latest, oldest, limit,
                cursor).execute();
    }

    /**
     * Returns a list of scheduled messages.
     * @param  token  Optional parameter: Authentication token. Requires scope: `none`
     * @param  channel  Optional parameter: The channel of the scheduled messages
     * @param  latest  Optional parameter: A UNIX timestamp of the latest value in the time range
     * @param  oldest  Optional parameter: A UNIX timestamp of the oldest value in the time range
     * @param  limit  Optional parameter: Maximum number of original entries to return.
     * @param  cursor  Optional parameter: For pagination purposes, this is the `cursor` value
     *         returned from a previous call to `chat.scheduledmessages.list` indicating where you
     *         want to start this call from.
     * @return    Returns the ChatScheduledMessagesListSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<ChatScheduledMessagesListSchema>> chatScheduledMessagesListAsync(
            final String token,
            final String channel,
            final Double latest,
            final Double oldest,
            final Integer limit,
            final String cursor) {
        try {
            return prepareChatScheduledMessagesListRequest(token, channel, latest, oldest, limit,
            cursor).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for chatScheduledMessagesList.
     */
    private ApiCall<ApiResponse<ChatScheduledMessagesListSchema>, ApiException> prepareChatScheduledMessagesListRequest(
            final String token,
            final String channel,
            final Double latest,
            final Double oldest,
            final Integer limit,
            final String cursor) {
        return new ApiCall.Builder<ApiResponse<ChatScheduledMessagesListSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/chat.scheduledMessages.list")
                        .queryParam(param -> param.key("channel")
                                .value(channel).isRequired(false))
                        .queryParam(param -> param.key("latest")
                                .value(latest).isRequired(false))
                        .queryParam(param -> param.key("oldest")
                                .value(oldest).isRequired(false))
                        .queryParam(param -> param.key("limit")
                                .value(limit).isRequired(false))
                        .queryParam(param -> param.key("cursor")
                                .value(cursor).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, ChatScheduledMessagesListSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response if the channel passed is invalid",
                                (reason, context) -> new ChatScheduledMessagesListErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}