/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.TeamProfileGetErrorSchemaException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.TeamProfileGetSuccessSchema;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class TeamProfileController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public TeamProfileController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Retrieve a team's profile.
     * @param  token  Required parameter: Authentication token. Requires scope: `users.profile:read`
     * @param  visibility  Optional parameter: Filter by visibility.
     * @return    Returns the TeamProfileGetSuccessSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<TeamProfileGetSuccessSchema> teamProfileGet(
            final String token,
            final String visibility) throws ApiException, IOException {
        return prepareTeamProfileGetRequest(token, visibility).execute();
    }

    /**
     * Retrieve a team's profile.
     * @param  token  Required parameter: Authentication token. Requires scope: `users.profile:read`
     * @param  visibility  Optional parameter: Filter by visibility.
     * @return    Returns the TeamProfileGetSuccessSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<TeamProfileGetSuccessSchema>> teamProfileGetAsync(
            final String token,
            final String visibility) {
        try {
            return prepareTeamProfileGetRequest(token, visibility).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for teamProfileGet.
     */
    private ApiCall<ApiResponse<TeamProfileGetSuccessSchema>, ApiException> prepareTeamProfileGetRequest(
            final String token,
            final String visibility) {
        return new ApiCall.Builder<ApiResponse<TeamProfileGetSuccessSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/team.profile.get")
                        .queryParam(param -> param.key("token")
                                .value(token))
                        .queryParam(param -> param.key("visibility")
                                .value(visibility).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, TeamProfileGetSuccessSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new TeamProfileGetErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}