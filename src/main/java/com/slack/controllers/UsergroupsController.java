/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.UsergroupsCreateErrorSchemaException;
import com.slack.exceptions.UsergroupsDisableErrorSchemaException;
import com.slack.exceptions.UsergroupsEnableErrorSchemaException;
import com.slack.exceptions.UsergroupsListErrorSchemaException;
import com.slack.exceptions.UsergroupsUpdateErrorSchemaException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.UsergroupsCreateSchema;
import com.slack.models.UsergroupsDisableSchema;
import com.slack.models.UsergroupsEnableSchema;
import com.slack.models.UsergroupsListSchema;
import com.slack.models.UsergroupsUpdateSchema;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class UsergroupsController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public UsergroupsController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Create a User Group.
     * @param  token  Required parameter: Authentication token. Requires scope: `usergroups:write`
     * @param  name  Required parameter: A name for the User Group. Must be unique among User
     *         Groups.
     * @param  channels  Optional parameter: A comma separated string of encoded channel IDs for
     *         which the User Group uses as a default.
     * @param  description  Optional parameter: A short description of the User Group.
     * @param  handle  Optional parameter: A mention handle. Must be unique among channels, users
     *         and User Groups.
     * @param  includeCount  Optional parameter: Include the number of users in each User Group.
     * @return    Returns the UsergroupsCreateSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<UsergroupsCreateSchema> usergroupsCreate(
            final String token,
            final String name,
            final String channels,
            final String description,
            final String handle,
            final Boolean includeCount) throws ApiException, IOException {
        return prepareUsergroupsCreateRequest(token, name, channels, description, handle,
                includeCount).execute();
    }

    /**
     * Create a User Group.
     * @param  token  Required parameter: Authentication token. Requires scope: `usergroups:write`
     * @param  name  Required parameter: A name for the User Group. Must be unique among User
     *         Groups.
     * @param  channels  Optional parameter: A comma separated string of encoded channel IDs for
     *         which the User Group uses as a default.
     * @param  description  Optional parameter: A short description of the User Group.
     * @param  handle  Optional parameter: A mention handle. Must be unique among channels, users
     *         and User Groups.
     * @param  includeCount  Optional parameter: Include the number of users in each User Group.
     * @return    Returns the UsergroupsCreateSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<UsergroupsCreateSchema>> usergroupsCreateAsync(
            final String token,
            final String name,
            final String channels,
            final String description,
            final String handle,
            final Boolean includeCount) {
        try {
            return prepareUsergroupsCreateRequest(token, name, channels, description, handle,
            includeCount).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for usergroupsCreate.
     */
    private ApiCall<ApiResponse<UsergroupsCreateSchema>, ApiException> prepareUsergroupsCreateRequest(
            final String token,
            final String name,
            final String channels,
            final String description,
            final String handle,
            final Boolean includeCount) {
        return new ApiCall.Builder<ApiResponse<UsergroupsCreateSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/usergroups.create")
                        .formParam(param -> param.key("name")
                                .value(name))
                        .formParam(param -> param.key("channels")
                                .value(channels).isRequired(false))
                        .formParam(param -> param.key("description")
                                .value(description).isRequired(false))
                        .formParam(param -> param.key("handle")
                                .value(handle).isRequired(false))
                        .formParam(param -> param.key("include_count")
                                .value(includeCount).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, UsergroupsCreateSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new UsergroupsCreateErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Disable an existing User Group.
     * @param  token  Required parameter: Authentication token. Requires scope: `usergroups:write`
     * @param  usergroup  Required parameter: The encoded ID of the User Group to disable.
     * @param  includeCount  Optional parameter: Include the number of users in the User Group.
     * @return    Returns the UsergroupsDisableSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<UsergroupsDisableSchema> usergroupsDisable(
            final String token,
            final String usergroup,
            final Boolean includeCount) throws ApiException, IOException {
        return prepareUsergroupsDisableRequest(token, usergroup, includeCount).execute();
    }

    /**
     * Disable an existing User Group.
     * @param  token  Required parameter: Authentication token. Requires scope: `usergroups:write`
     * @param  usergroup  Required parameter: The encoded ID of the User Group to disable.
     * @param  includeCount  Optional parameter: Include the number of users in the User Group.
     * @return    Returns the UsergroupsDisableSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<UsergroupsDisableSchema>> usergroupsDisableAsync(
            final String token,
            final String usergroup,
            final Boolean includeCount) {
        try {
            return prepareUsergroupsDisableRequest(token, usergroup, includeCount).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for usergroupsDisable.
     */
    private ApiCall<ApiResponse<UsergroupsDisableSchema>, ApiException> prepareUsergroupsDisableRequest(
            final String token,
            final String usergroup,
            final Boolean includeCount) {
        return new ApiCall.Builder<ApiResponse<UsergroupsDisableSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/usergroups.disable")
                        .formParam(param -> param.key("usergroup")
                                .value(usergroup))
                        .formParam(param -> param.key("include_count")
                                .value(includeCount).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, UsergroupsDisableSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new UsergroupsDisableErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Enable a User Group.
     * @param  token  Required parameter: Authentication token. Requires scope: `usergroups:write`
     * @param  usergroup  Required parameter: The encoded ID of the User Group to enable.
     * @param  includeCount  Optional parameter: Include the number of users in the User Group.
     * @return    Returns the UsergroupsEnableSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<UsergroupsEnableSchema> usergroupsEnable(
            final String token,
            final String usergroup,
            final Boolean includeCount) throws ApiException, IOException {
        return prepareUsergroupsEnableRequest(token, usergroup, includeCount).execute();
    }

    /**
     * Enable a User Group.
     * @param  token  Required parameter: Authentication token. Requires scope: `usergroups:write`
     * @param  usergroup  Required parameter: The encoded ID of the User Group to enable.
     * @param  includeCount  Optional parameter: Include the number of users in the User Group.
     * @return    Returns the UsergroupsEnableSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<UsergroupsEnableSchema>> usergroupsEnableAsync(
            final String token,
            final String usergroup,
            final Boolean includeCount) {
        try {
            return prepareUsergroupsEnableRequest(token, usergroup, includeCount).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for usergroupsEnable.
     */
    private ApiCall<ApiResponse<UsergroupsEnableSchema>, ApiException> prepareUsergroupsEnableRequest(
            final String token,
            final String usergroup,
            final Boolean includeCount) {
        return new ApiCall.Builder<ApiResponse<UsergroupsEnableSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/usergroups.enable")
                        .formParam(param -> param.key("usergroup")
                                .value(usergroup))
                        .formParam(param -> param.key("include_count")
                                .value(includeCount).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, UsergroupsEnableSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new UsergroupsEnableErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * List all User Groups for a team.
     * @param  token  Required parameter: Authentication token. Requires scope: `usergroups:read`
     * @param  includeUsers  Optional parameter: Include the list of users for each User Group.
     * @param  includeCount  Optional parameter: Include the number of users in each User Group.
     * @param  includeDisabled  Optional parameter: Include disabled User Groups.
     * @return    Returns the UsergroupsListSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<UsergroupsListSchema> usergroupsList(
            final String token,
            final Boolean includeUsers,
            final Boolean includeCount,
            final Boolean includeDisabled) throws ApiException, IOException {
        return prepareUsergroupsListRequest(token, includeUsers, includeCount,
                includeDisabled).execute();
    }

    /**
     * List all User Groups for a team.
     * @param  token  Required parameter: Authentication token. Requires scope: `usergroups:read`
     * @param  includeUsers  Optional parameter: Include the list of users for each User Group.
     * @param  includeCount  Optional parameter: Include the number of users in each User Group.
     * @param  includeDisabled  Optional parameter: Include disabled User Groups.
     * @return    Returns the UsergroupsListSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<UsergroupsListSchema>> usergroupsListAsync(
            final String token,
            final Boolean includeUsers,
            final Boolean includeCount,
            final Boolean includeDisabled) {
        try {
            return prepareUsergroupsListRequest(token, includeUsers, includeCount,
            includeDisabled).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for usergroupsList.
     */
    private ApiCall<ApiResponse<UsergroupsListSchema>, ApiException> prepareUsergroupsListRequest(
            final String token,
            final Boolean includeUsers,
            final Boolean includeCount,
            final Boolean includeDisabled) {
        return new ApiCall.Builder<ApiResponse<UsergroupsListSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/usergroups.list")
                        .queryParam(param -> param.key("token")
                                .value(token))
                        .queryParam(param -> param.key("include_users")
                                .value(includeUsers).isRequired(false))
                        .queryParam(param -> param.key("include_count")
                                .value(includeCount).isRequired(false))
                        .queryParam(param -> param.key("include_disabled")
                                .value(includeDisabled).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, UsergroupsListSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new UsergroupsListErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Update an existing User Group.
     * @param  token  Required parameter: Authentication token. Requires scope: `usergroups:write`
     * @param  usergroup  Required parameter: The encoded ID of the User Group to update.
     * @param  handle  Optional parameter: A mention handle. Must be unique among channels, users
     *         and User Groups.
     * @param  description  Optional parameter: A short description of the User Group.
     * @param  channels  Optional parameter: A comma separated string of encoded channel IDs for
     *         which the User Group uses as a default.
     * @param  includeCount  Optional parameter: Include the number of users in the User Group.
     * @param  name  Optional parameter: A name for the User Group. Must be unique among User
     *         Groups.
     * @return    Returns the UsergroupsUpdateSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<UsergroupsUpdateSchema> usergroupsUpdate(
            final String token,
            final String usergroup,
            final String handle,
            final String description,
            final String channels,
            final Boolean includeCount,
            final String name) throws ApiException, IOException {
        return prepareUsergroupsUpdateRequest(token, usergroup, handle, description, channels,
                includeCount, name).execute();
    }

    /**
     * Update an existing User Group.
     * @param  token  Required parameter: Authentication token. Requires scope: `usergroups:write`
     * @param  usergroup  Required parameter: The encoded ID of the User Group to update.
     * @param  handle  Optional parameter: A mention handle. Must be unique among channels, users
     *         and User Groups.
     * @param  description  Optional parameter: A short description of the User Group.
     * @param  channels  Optional parameter: A comma separated string of encoded channel IDs for
     *         which the User Group uses as a default.
     * @param  includeCount  Optional parameter: Include the number of users in the User Group.
     * @param  name  Optional parameter: A name for the User Group. Must be unique among User
     *         Groups.
     * @return    Returns the UsergroupsUpdateSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<UsergroupsUpdateSchema>> usergroupsUpdateAsync(
            final String token,
            final String usergroup,
            final String handle,
            final String description,
            final String channels,
            final Boolean includeCount,
            final String name) {
        try {
            return prepareUsergroupsUpdateRequest(token, usergroup, handle, description, channels,
            includeCount, name).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for usergroupsUpdate.
     */
    private ApiCall<ApiResponse<UsergroupsUpdateSchema>, ApiException> prepareUsergroupsUpdateRequest(
            final String token,
            final String usergroup,
            final String handle,
            final String description,
            final String channels,
            final Boolean includeCount,
            final String name) {
        return new ApiCall.Builder<ApiResponse<UsergroupsUpdateSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/usergroups.update")
                        .formParam(param -> param.key("usergroup")
                                .value(usergroup))
                        .formParam(param -> param.key("handle")
                                .value(handle).isRequired(false))
                        .formParam(param -> param.key("description")
                                .value(description).isRequired(false))
                        .formParam(param -> param.key("channels")
                                .value(channels).isRequired(false))
                        .formParam(param -> param.key("include_count")
                                .value(includeCount).isRequired(false))
                        .formParam(param -> param.key("name")
                                .value(name).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, UsergroupsUpdateSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new UsergroupsUpdateErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}