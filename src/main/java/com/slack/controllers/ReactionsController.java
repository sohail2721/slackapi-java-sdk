/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.ReactionsAddErrorSchemaException;
import com.slack.exceptions.ReactionsGetErrorSchemaException;
import com.slack.exceptions.ReactionsListErrorSchemaException;
import com.slack.exceptions.ReactionsRemoveErrorSchemaException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.ReactionsAddSchema;
import com.slack.models.ReactionsListSchema;
import com.slack.models.ReactionsRemoveSchema;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class ReactionsController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public ReactionsController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Adds a reaction to an item.
     * @param  channel  Required parameter: Channel where the message to add reaction to was posted.
     * @param  name  Required parameter: Reaction (emoji) name.
     * @param  timestamp  Required parameter: Timestamp of the message to add reaction to.
     * @param  token  Required parameter: Authentication token. Requires scope: `reactions:write`
     * @return    Returns the ReactionsAddSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<ReactionsAddSchema> reactionsAdd(
            final String channel,
            final String name,
            final String timestamp,
            final String token) throws ApiException, IOException {
        return prepareReactionsAddRequest(channel, name, timestamp, token).execute();
    }

    /**
     * Adds a reaction to an item.
     * @param  channel  Required parameter: Channel where the message to add reaction to was posted.
     * @param  name  Required parameter: Reaction (emoji) name.
     * @param  timestamp  Required parameter: Timestamp of the message to add reaction to.
     * @param  token  Required parameter: Authentication token. Requires scope: `reactions:write`
     * @return    Returns the ReactionsAddSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<ReactionsAddSchema>> reactionsAddAsync(
            final String channel,
            final String name,
            final String timestamp,
            final String token) {
        try {
            return prepareReactionsAddRequest(channel, name, timestamp, token).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for reactionsAdd.
     */
    private ApiCall<ApiResponse<ReactionsAddSchema>, ApiException> prepareReactionsAddRequest(
            final String channel,
            final String name,
            final String timestamp,
            final String token) {
        return new ApiCall.Builder<ApiResponse<ReactionsAddSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/reactions.add")
                        .formParam(param -> param.key("channel")
                                .value(channel))
                        .formParam(param -> param.key("name")
                                .value(name))
                        .formParam(param -> param.key("timestamp")
                                .value(timestamp))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, ReactionsAddSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new ReactionsAddErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets reactions for an item.
     * @param  token  Required parameter: Authentication token. Requires scope: `reactions:read`
     * @param  channel  Optional parameter: Channel where the message to get reactions for was
     *         posted.
     * @param  file  Optional parameter: File to get reactions for.
     * @param  fileComment  Optional parameter: File comment to get reactions for.
     * @param  full  Optional parameter: If true always return the complete reaction list.
     * @param  timestamp  Optional parameter: Timestamp of the message to get reactions for.
     * @return    Returns the Object wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<Object> reactionsGet(
            final String token,
            final String channel,
            final String file,
            final String fileComment,
            final Boolean full,
            final String timestamp) throws ApiException, IOException {
        return prepareReactionsGetRequest(token, channel, file, fileComment, full,
                timestamp).execute();
    }

    /**
     * Gets reactions for an item.
     * @param  token  Required parameter: Authentication token. Requires scope: `reactions:read`
     * @param  channel  Optional parameter: Channel where the message to get reactions for was
     *         posted.
     * @param  file  Optional parameter: File to get reactions for.
     * @param  fileComment  Optional parameter: File comment to get reactions for.
     * @param  full  Optional parameter: If true always return the complete reaction list.
     * @param  timestamp  Optional parameter: Timestamp of the message to get reactions for.
     * @return    Returns the Object wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<Object>> reactionsGetAsync(
            final String token,
            final String channel,
            final String file,
            final String fileComment,
            final Boolean full,
            final String timestamp) {
        try {
            return prepareReactionsGetRequest(token, channel, file, fileComment, full,
            timestamp).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for reactionsGet.
     */
    private ApiCall<ApiResponse<Object>, ApiException> prepareReactionsGetRequest(
            final String token,
            final String channel,
            final String file,
            final String fileComment,
            final Boolean full,
            final String timestamp) {
        return new ApiCall.Builder<ApiResponse<Object>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/reactions.get")
                        .queryParam(param -> param.key("token")
                                .value(token))
                        .queryParam(param -> param.key("channel")
                                .value(channel).isRequired(false))
                        .queryParam(param -> param.key("file")
                                .value(file).isRequired(false))
                        .queryParam(param -> param.key("file_comment")
                                .value(fileComment).isRequired(false))
                        .queryParam(param -> param.key("full")
                                .value(full).isRequired(false))
                        .queryParam(param -> param.key("timestamp")
                                .value(timestamp).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> response)
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new ReactionsGetErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Lists reactions made by a user.
     * @param  token  Required parameter: Authentication token. Requires scope: `reactions:read`
     * @param  user  Optional parameter: Show reactions made by this user. Defaults to the authed
     *         user.
     * @param  full  Optional parameter: If true always return the complete reaction list.
     * @param  count  Optional parameter:
     * @param  page  Optional parameter:
     * @param  cursor  Optional parameter: Parameter for pagination. Set `cursor` equal to the
     *         `next_cursor` attribute returned by the previous request's `response_metadata`. This
     *         parameter is optional, but pagination is mandatory: the default value simply fetches
     *         the first "page" of the collection. See [pagination](/docs/pagination) for more
     *         details.
     * @param  limit  Optional parameter: The maximum number of items to return. Fewer than the
     *         requested number of items may be returned, even if the end of the list hasn't been
     *         reached.
     * @return    Returns the ReactionsListSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<ReactionsListSchema> reactionsList(
            final String token,
            final String user,
            final Boolean full,
            final Integer count,
            final Integer page,
            final String cursor,
            final Integer limit) throws ApiException, IOException {
        return prepareReactionsListRequest(token, user, full, count, page, cursor, limit).execute();
    }

    /**
     * Lists reactions made by a user.
     * @param  token  Required parameter: Authentication token. Requires scope: `reactions:read`
     * @param  user  Optional parameter: Show reactions made by this user. Defaults to the authed
     *         user.
     * @param  full  Optional parameter: If true always return the complete reaction list.
     * @param  count  Optional parameter:
     * @param  page  Optional parameter:
     * @param  cursor  Optional parameter: Parameter for pagination. Set `cursor` equal to the
     *         `next_cursor` attribute returned by the previous request's `response_metadata`. This
     *         parameter is optional, but pagination is mandatory: the default value simply fetches
     *         the first "page" of the collection. See [pagination](/docs/pagination) for more
     *         details.
     * @param  limit  Optional parameter: The maximum number of items to return. Fewer than the
     *         requested number of items may be returned, even if the end of the list hasn't been
     *         reached.
     * @return    Returns the ReactionsListSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<ReactionsListSchema>> reactionsListAsync(
            final String token,
            final String user,
            final Boolean full,
            final Integer count,
            final Integer page,
            final String cursor,
            final Integer limit) {
        try {
            return prepareReactionsListRequest(token, user, full, count, page, cursor,
            limit).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for reactionsList.
     */
    private ApiCall<ApiResponse<ReactionsListSchema>, ApiException> prepareReactionsListRequest(
            final String token,
            final String user,
            final Boolean full,
            final Integer count,
            final Integer page,
            final String cursor,
            final Integer limit) {
        return new ApiCall.Builder<ApiResponse<ReactionsListSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/reactions.list")
                        .queryParam(param -> param.key("token")
                                .value(token))
                        .queryParam(param -> param.key("user")
                                .value(user).isRequired(false))
                        .queryParam(param -> param.key("full")
                                .value(full).isRequired(false))
                        .queryParam(param -> param.key("count")
                                .value(count).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .queryParam(param -> param.key("cursor")
                                .value(cursor).isRequired(false))
                        .queryParam(param -> param.key("limit")
                                .value(limit).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, ReactionsListSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new ReactionsListErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Removes a reaction from an item.
     * @param  token  Required parameter: Authentication token. Requires scope: `reactions:write`
     * @param  name  Required parameter: Reaction (emoji) name.
     * @param  file  Optional parameter: File to remove reaction from.
     * @param  fileComment  Optional parameter: File comment to remove reaction from.
     * @param  channel  Optional parameter: Channel where the message to remove reaction from was
     *         posted.
     * @param  timestamp  Optional parameter: Timestamp of the message to remove reaction from.
     * @return    Returns the ReactionsRemoveSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<ReactionsRemoveSchema> reactionsRemove(
            final String token,
            final String name,
            final String file,
            final String fileComment,
            final String channel,
            final String timestamp) throws ApiException, IOException {
        return prepareReactionsRemoveRequest(token, name, file, fileComment, channel,
                timestamp).execute();
    }

    /**
     * Removes a reaction from an item.
     * @param  token  Required parameter: Authentication token. Requires scope: `reactions:write`
     * @param  name  Required parameter: Reaction (emoji) name.
     * @param  file  Optional parameter: File to remove reaction from.
     * @param  fileComment  Optional parameter: File comment to remove reaction from.
     * @param  channel  Optional parameter: Channel where the message to remove reaction from was
     *         posted.
     * @param  timestamp  Optional parameter: Timestamp of the message to remove reaction from.
     * @return    Returns the ReactionsRemoveSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<ReactionsRemoveSchema>> reactionsRemoveAsync(
            final String token,
            final String name,
            final String file,
            final String fileComment,
            final String channel,
            final String timestamp) {
        try {
            return prepareReactionsRemoveRequest(token, name, file, fileComment, channel,
            timestamp).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for reactionsRemove.
     */
    private ApiCall<ApiResponse<ReactionsRemoveSchema>, ApiException> prepareReactionsRemoveRequest(
            final String token,
            final String name,
            final String file,
            final String fileComment,
            final String channel,
            final String timestamp) {
        return new ApiCall.Builder<ApiResponse<ReactionsRemoveSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/reactions.remove")
                        .formParam(param -> param.key("name")
                                .value(name))
                        .formParam(param -> param.key("file")
                                .value(file).isRequired(false))
                        .formParam(param -> param.key("file_comment")
                                .value(fileComment).isRequired(false))
                        .formParam(param -> param.key("channel")
                                .value(channel).isRequired(false))
                        .formParam(param -> param.key("timestamp")
                                .value(timestamp).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, ReactionsRemoveSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new ReactionsRemoveErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}