/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.MigrationExchangeErrorSchemaException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.MigrationExchangeSuccessSchema;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class MigrationController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public MigrationController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * For Enterprise Grid workspaces, map local user IDs to global user IDs.
     * @param  token  Required parameter: Authentication token. Requires scope: `tokens.basic`
     * @param  users  Required parameter: A comma-separated list of user ids, up to 400 per request
     * @param  teamId  Optional parameter: Specify team_id starts with `T` in case of Org Token
     * @param  toOld  Optional parameter: Specify `true` to convert `W` global user IDs to
     *         workspace-specific `U` IDs. Defaults to `false`.
     * @return    Returns the MigrationExchangeSuccessSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<MigrationExchangeSuccessSchema> migrationExchange(
            final String token,
            final String users,
            final String teamId,
            final Boolean toOld) throws ApiException, IOException {
        return prepareMigrationExchangeRequest(token, users, teamId, toOld).execute();
    }

    /**
     * For Enterprise Grid workspaces, map local user IDs to global user IDs.
     * @param  token  Required parameter: Authentication token. Requires scope: `tokens.basic`
     * @param  users  Required parameter: A comma-separated list of user ids, up to 400 per request
     * @param  teamId  Optional parameter: Specify team_id starts with `T` in case of Org Token
     * @param  toOld  Optional parameter: Specify `true` to convert `W` global user IDs to
     *         workspace-specific `U` IDs. Defaults to `false`.
     * @return    Returns the MigrationExchangeSuccessSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<MigrationExchangeSuccessSchema>> migrationExchangeAsync(
            final String token,
            final String users,
            final String teamId,
            final Boolean toOld) {
        try {
            return prepareMigrationExchangeRequest(token, users, teamId, toOld).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for migrationExchange.
     */
    private ApiCall<ApiResponse<MigrationExchangeSuccessSchema>, ApiException> prepareMigrationExchangeRequest(
            final String token,
            final String users,
            final String teamId,
            final Boolean toOld) {
        return new ApiCall.Builder<ApiResponse<MigrationExchangeSuccessSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/migration.exchange")
                        .queryParam(param -> param.key("token")
                                .value(token))
                        .queryParam(param -> param.key("users")
                                .value(users))
                        .queryParam(param -> param.key("team_id")
                                .value(teamId).isRequired(false))
                        .queryParam(param -> param.key("to_old")
                                .value(toOld).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, MigrationExchangeSuccessSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response when there are no mappings to provide",
                                (reason, context) -> new MigrationExchangeErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}