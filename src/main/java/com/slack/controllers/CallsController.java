/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.DefaultErrorTemplateException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.DefaultSuccessTemplate;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class CallsController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public CallsController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Registers a new Call.
     * @param  token  Required parameter: Authentication token. Requires scope: `calls:write`
     * @param  externalUniqueId  Required parameter: An ID supplied by the 3rd-party Call provider.
     *         It must be unique across all Calls from that service.
     * @param  joinUrl  Required parameter: The URL required for a client to join the Call.
     * @param  externalDisplayId  Optional parameter: An optional, human-readable ID supplied by the
     *         3rd-party Call provider. If supplied, this ID will be displayed in the Call object.
     * @param  desktopAppJoinUrl  Optional parameter: When supplied, available Slack clients will
     *         attempt to directly launch the 3rd-party Call with this URL.
     * @param  dateStart  Optional parameter: Call start time in UTC UNIX timestamp format
     * @param  title  Optional parameter: The name of the Call.
     * @param  createdBy  Optional parameter: The valid Slack user ID of the user who created this
     *         Call. When this method is called with a user token, the `created_by` field is
     *         optional and defaults to the authed user of the token. Otherwise, the field is
     *         required.
     * @param  users  Optional parameter: The list of users to register as participants in the Call.
     *         [Read more on how to specify users here](/apis/calls#users).
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> callsAdd(
            final String token,
            final String externalUniqueId,
            final String joinUrl,
            final String externalDisplayId,
            final String desktopAppJoinUrl,
            final Integer dateStart,
            final String title,
            final String createdBy,
            final String users) throws ApiException, IOException {
        return prepareCallsAddRequest(token, externalUniqueId, joinUrl, externalDisplayId,
                desktopAppJoinUrl, dateStart, title, createdBy, users).execute();
    }

    /**
     * Registers a new Call.
     * @param  token  Required parameter: Authentication token. Requires scope: `calls:write`
     * @param  externalUniqueId  Required parameter: An ID supplied by the 3rd-party Call provider.
     *         It must be unique across all Calls from that service.
     * @param  joinUrl  Required parameter: The URL required for a client to join the Call.
     * @param  externalDisplayId  Optional parameter: An optional, human-readable ID supplied by the
     *         3rd-party Call provider. If supplied, this ID will be displayed in the Call object.
     * @param  desktopAppJoinUrl  Optional parameter: When supplied, available Slack clients will
     *         attempt to directly launch the 3rd-party Call with this URL.
     * @param  dateStart  Optional parameter: Call start time in UTC UNIX timestamp format
     * @param  title  Optional parameter: The name of the Call.
     * @param  createdBy  Optional parameter: The valid Slack user ID of the user who created this
     *         Call. When this method is called with a user token, the `created_by` field is
     *         optional and defaults to the authed user of the token. Otherwise, the field is
     *         required.
     * @param  users  Optional parameter: The list of users to register as participants in the Call.
     *         [Read more on how to specify users here](/apis/calls#users).
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> callsAddAsync(
            final String token,
            final String externalUniqueId,
            final String joinUrl,
            final String externalDisplayId,
            final String desktopAppJoinUrl,
            final Integer dateStart,
            final String title,
            final String createdBy,
            final String users) {
        try {
            return prepareCallsAddRequest(token, externalUniqueId, joinUrl, externalDisplayId,
            desktopAppJoinUrl, dateStart, title, createdBy, users).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for callsAdd.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareCallsAddRequest(
            final String token,
            final String externalUniqueId,
            final String joinUrl,
            final String externalDisplayId,
            final String desktopAppJoinUrl,
            final Integer dateStart,
            final String title,
            final String createdBy,
            final String users) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/calls.add")
                        .formParam(param -> param.key("external_unique_id")
                                .value(externalUniqueId))
                        .formParam(param -> param.key("join_url")
                                .value(joinUrl))
                        .formParam(param -> param.key("external_display_id")
                                .value(externalDisplayId).isRequired(false))
                        .formParam(param -> param.key("desktop_app_join_url")
                                .value(desktopAppJoinUrl).isRequired(false))
                        .formParam(param -> param.key("date_start")
                                .value(dateStart).isRequired(false))
                        .formParam(param -> param.key("title")
                                .value(title).isRequired(false))
                        .formParam(param -> param.key("created_by")
                                .value(createdBy).isRequired(false))
                        .formParam(param -> param.key("users")
                                .value(users).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Ends a Call.
     * @param  token  Required parameter: Authentication token. Requires scope: `calls:write`
     * @param  id  Required parameter: `id` returned when registering the call using the
     *         [`calls.add`](/methods/calls.add) method.
     * @param  duration  Optional parameter: Call duration in seconds
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> callsEnd(
            final String token,
            final String id,
            final Integer duration) throws ApiException, IOException {
        return prepareCallsEndRequest(token, id, duration).execute();
    }

    /**
     * Ends a Call.
     * @param  token  Required parameter: Authentication token. Requires scope: `calls:write`
     * @param  id  Required parameter: `id` returned when registering the call using the
     *         [`calls.add`](/methods/calls.add) method.
     * @param  duration  Optional parameter: Call duration in seconds
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> callsEndAsync(
            final String token,
            final String id,
            final Integer duration) {
        try {
            return prepareCallsEndRequest(token, id, duration).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for callsEnd.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareCallsEndRequest(
            final String token,
            final String id,
            final Integer duration) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/calls.end")
                        .formParam(param -> param.key("id")
                                .value(id))
                        .formParam(param -> param.key("duration")
                                .value(duration).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Returns information about a Call.
     * @param  token  Required parameter: Authentication token. Requires scope: `calls:read`
     * @param  id  Required parameter: `id` of the Call returned by the
     *         [`calls.add`](/methods/calls.add) method.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> callsInfo(
            final String token,
            final String id) throws ApiException, IOException {
        return prepareCallsInfoRequest(token, id).execute();
    }

    /**
     * Returns information about a Call.
     * @param  token  Required parameter: Authentication token. Requires scope: `calls:read`
     * @param  id  Required parameter: `id` of the Call returned by the
     *         [`calls.add`](/methods/calls.add) method.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> callsInfoAsync(
            final String token,
            final String id) {
        try {
            return prepareCallsInfoRequest(token, id).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for callsInfo.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareCallsInfoRequest(
            final String token,
            final String id) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/calls.info")
                        .queryParam(param -> param.key("id")
                                .value(id))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Updates information about a Call.
     * @param  token  Required parameter: Authentication token. Requires scope: `calls:write`
     * @param  id  Required parameter: `id` returned by the [`calls.add`](/methods/calls.add)
     *         method.
     * @param  title  Optional parameter: The name of the Call.
     * @param  joinUrl  Optional parameter: The URL required for a client to join the Call.
     * @param  desktopAppJoinUrl  Optional parameter: When supplied, available Slack clients will
     *         attempt to directly launch the 3rd-party Call with this URL.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> callsUpdate(
            final String token,
            final String id,
            final String title,
            final String joinUrl,
            final String desktopAppJoinUrl) throws ApiException, IOException {
        return prepareCallsUpdateRequest(token, id, title, joinUrl, desktopAppJoinUrl).execute();
    }

    /**
     * Updates information about a Call.
     * @param  token  Required parameter: Authentication token. Requires scope: `calls:write`
     * @param  id  Required parameter: `id` returned by the [`calls.add`](/methods/calls.add)
     *         method.
     * @param  title  Optional parameter: The name of the Call.
     * @param  joinUrl  Optional parameter: The URL required for a client to join the Call.
     * @param  desktopAppJoinUrl  Optional parameter: When supplied, available Slack clients will
     *         attempt to directly launch the 3rd-party Call with this URL.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> callsUpdateAsync(
            final String token,
            final String id,
            final String title,
            final String joinUrl,
            final String desktopAppJoinUrl) {
        try {
            return prepareCallsUpdateRequest(token, id, title, joinUrl, desktopAppJoinUrl).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for callsUpdate.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareCallsUpdateRequest(
            final String token,
            final String id,
            final String title,
            final String joinUrl,
            final String desktopAppJoinUrl) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/calls.update")
                        .formParam(param -> param.key("id")
                                .value(id))
                        .formParam(param -> param.key("title")
                                .value(title).isRequired(false))
                        .formParam(param -> param.key("join_url")
                                .value(joinUrl).isRequired(false))
                        .formParam(param -> param.key("desktop_app_join_url")
                                .value(desktopAppJoinUrl).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}