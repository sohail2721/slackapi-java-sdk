/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.FilesCommentsDeleteErrorSchemaException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.FilesCommentsDeleteSchema;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class FilesCommentsController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public FilesCommentsController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Deletes an existing comment on a file.
     * @param  token  Optional parameter: Authentication token. Requires scope: `files:write:user`
     * @param  file  Optional parameter: File to delete a comment from.
     * @param  id  Optional parameter: The comment to delete.
     * @return    Returns the FilesCommentsDeleteSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<FilesCommentsDeleteSchema> filesCommentsDelete(
            final String token,
            final String file,
            final String id) throws ApiException, IOException {
        return prepareFilesCommentsDeleteRequest(token, file, id).execute();
    }

    /**
     * Deletes an existing comment on a file.
     * @param  token  Optional parameter: Authentication token. Requires scope: `files:write:user`
     * @param  file  Optional parameter: File to delete a comment from.
     * @param  id  Optional parameter: The comment to delete.
     * @return    Returns the FilesCommentsDeleteSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<FilesCommentsDeleteSchema>> filesCommentsDeleteAsync(
            final String token,
            final String file,
            final String id) {
        try {
            return prepareFilesCommentsDeleteRequest(token, file, id).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for filesCommentsDelete.
     */
    private ApiCall<ApiResponse<FilesCommentsDeleteSchema>, ApiException> prepareFilesCommentsDeleteRequest(
            final String token,
            final String file,
            final String id) {
        return new ApiCall.Builder<ApiResponse<FilesCommentsDeleteSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/files.comments.delete")
                        .formParam(param -> param.key("file")
                                .value(file).isRequired(false))
                        .formParam(param -> param.key("id")
                                .value(id).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, FilesCommentsDeleteSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Standard failure response when used with an invalid token",
                                (reason, context) -> new FilesCommentsDeleteErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}