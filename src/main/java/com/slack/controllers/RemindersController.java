/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.RemindersAddErrorSchemaException;
import com.slack.exceptions.RemindersCompleteErrorSchemaException;
import com.slack.exceptions.RemindersDeleteErrorSchemaException;
import com.slack.exceptions.RemindersInfoErrorSchemaException;
import com.slack.exceptions.RemindersListErrorSchemaException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.RemindersAddSchema;
import com.slack.models.RemindersCompleteSchema;
import com.slack.models.RemindersDeleteSchema;
import com.slack.models.RemindersInfoSchema;
import com.slack.models.RemindersListSchema;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class RemindersController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public RemindersController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Creates a reminder.
     * @param  token  Required parameter: Authentication token. Requires scope: `reminders:write`
     * @param  text  Required parameter: The content of the reminder
     * @param  time  Required parameter: When this reminder should happen: the Unix timestamp (up to
     *         five years from now), the number of seconds until the reminder (if within 24 hours),
     *         or a natural language description (Ex. "in 15 minutes," or "every Thursday")
     * @param  user  Optional parameter: The user who will receive the reminder. If no user is
     *         specified, the reminder will go to user who created it.
     * @return    Returns the RemindersAddSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<RemindersAddSchema> remindersAdd(
            final String token,
            final String text,
            final String time,
            final String user) throws ApiException, IOException {
        return prepareRemindersAddRequest(token, text, time, user).execute();
    }

    /**
     * Creates a reminder.
     * @param  token  Required parameter: Authentication token. Requires scope: `reminders:write`
     * @param  text  Required parameter: The content of the reminder
     * @param  time  Required parameter: When this reminder should happen: the Unix timestamp (up to
     *         five years from now), the number of seconds until the reminder (if within 24 hours),
     *         or a natural language description (Ex. "in 15 minutes," or "every Thursday")
     * @param  user  Optional parameter: The user who will receive the reminder. If no user is
     *         specified, the reminder will go to user who created it.
     * @return    Returns the RemindersAddSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<RemindersAddSchema>> remindersAddAsync(
            final String token,
            final String text,
            final String time,
            final String user) {
        try {
            return prepareRemindersAddRequest(token, text, time, user).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for remindersAdd.
     */
    private ApiCall<ApiResponse<RemindersAddSchema>, ApiException> prepareRemindersAddRequest(
            final String token,
            final String text,
            final String time,
            final String user) {
        return new ApiCall.Builder<ApiResponse<RemindersAddSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/reminders.add")
                        .formParam(param -> param.key("text")
                                .value(text))
                        .formParam(param -> param.key("time")
                                .value(time))
                        .formParam(param -> param.key("user")
                                .value(user).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, RemindersAddSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new RemindersAddErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Marks a reminder as complete.
     * @param  token  Optional parameter: Authentication token. Requires scope: `reminders:write`
     * @param  reminder  Optional parameter: The ID of the reminder to be marked as complete
     * @return    Returns the RemindersCompleteSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<RemindersCompleteSchema> remindersComplete(
            final String token,
            final String reminder) throws ApiException, IOException {
        return prepareRemindersCompleteRequest(token, reminder).execute();
    }

    /**
     * Marks a reminder as complete.
     * @param  token  Optional parameter: Authentication token. Requires scope: `reminders:write`
     * @param  reminder  Optional parameter: The ID of the reminder to be marked as complete
     * @return    Returns the RemindersCompleteSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<RemindersCompleteSchema>> remindersCompleteAsync(
            final String token,
            final String reminder) {
        try {
            return prepareRemindersCompleteRequest(token, reminder).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for remindersComplete.
     */
    private ApiCall<ApiResponse<RemindersCompleteSchema>, ApiException> prepareRemindersCompleteRequest(
            final String token,
            final String reminder) {
        return new ApiCall.Builder<ApiResponse<RemindersCompleteSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/reminders.complete")
                        .formParam(param -> param.key("reminder")
                                .value(reminder).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, RemindersCompleteSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new RemindersCompleteErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Deletes a reminder.
     * @param  token  Optional parameter: Authentication token. Requires scope: `reminders:write`
     * @param  reminder  Optional parameter: The ID of the reminder
     * @return    Returns the RemindersDeleteSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<RemindersDeleteSchema> remindersDelete(
            final String token,
            final String reminder) throws ApiException, IOException {
        return prepareRemindersDeleteRequest(token, reminder).execute();
    }

    /**
     * Deletes a reminder.
     * @param  token  Optional parameter: Authentication token. Requires scope: `reminders:write`
     * @param  reminder  Optional parameter: The ID of the reminder
     * @return    Returns the RemindersDeleteSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<RemindersDeleteSchema>> remindersDeleteAsync(
            final String token,
            final String reminder) {
        try {
            return prepareRemindersDeleteRequest(token, reminder).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for remindersDelete.
     */
    private ApiCall<ApiResponse<RemindersDeleteSchema>, ApiException> prepareRemindersDeleteRequest(
            final String token,
            final String reminder) {
        return new ApiCall.Builder<ApiResponse<RemindersDeleteSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/reminders.delete")
                        .formParam(param -> param.key("reminder")
                                .value(reminder).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, RemindersDeleteSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new RemindersDeleteErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets information about a reminder.
     * @param  token  Optional parameter: Authentication token. Requires scope: `reminders:read`
     * @param  reminder  Optional parameter: The ID of the reminder
     * @return    Returns the RemindersInfoSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<RemindersInfoSchema> remindersInfo(
            final String token,
            final String reminder) throws ApiException, IOException {
        return prepareRemindersInfoRequest(token, reminder).execute();
    }

    /**
     * Gets information about a reminder.
     * @param  token  Optional parameter: Authentication token. Requires scope: `reminders:read`
     * @param  reminder  Optional parameter: The ID of the reminder
     * @return    Returns the RemindersInfoSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<RemindersInfoSchema>> remindersInfoAsync(
            final String token,
            final String reminder) {
        try {
            return prepareRemindersInfoRequest(token, reminder).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for remindersInfo.
     */
    private ApiCall<ApiResponse<RemindersInfoSchema>, ApiException> prepareRemindersInfoRequest(
            final String token,
            final String reminder) {
        return new ApiCall.Builder<ApiResponse<RemindersInfoSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/reminders.info")
                        .queryParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .queryParam(param -> param.key("reminder")
                                .value(reminder).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, RemindersInfoSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new RemindersInfoErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Lists all reminders created by or for a given user.
     * @param  token  Optional parameter: Authentication token. Requires scope: `reminders:read`
     * @return    Returns the RemindersListSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<RemindersListSchema> remindersList(
            final String token) throws ApiException, IOException {
        return prepareRemindersListRequest(token).execute();
    }

    /**
     * Lists all reminders created by or for a given user.
     * @param  token  Optional parameter: Authentication token. Requires scope: `reminders:read`
     * @return    Returns the RemindersListSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<RemindersListSchema>> remindersListAsync(
            final String token) {
        try {
            return prepareRemindersListRequest(token).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for remindersList.
     */
    private ApiCall<ApiResponse<RemindersListSchema>, ApiException> prepareRemindersListRequest(
            final String token) {
        return new ApiCall.Builder<ApiResponse<RemindersListSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/reminders.list")
                        .queryParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, RemindersListSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new RemindersListErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}