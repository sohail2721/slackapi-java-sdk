/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.DefaultErrorTemplateException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.DefaultSuccessTemplate;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class AdminAppsController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public AdminAppsController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Approve an app for installation on a workspace.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.apps:write`
     * @param  appId  Optional parameter: The id of the app to approve.
     * @param  requestId  Optional parameter: The id of the request to approve.
     * @param  teamId  Optional parameter:
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminAppsApprove(
            final String token,
            final String appId,
            final String requestId,
            final String teamId) throws ApiException, IOException {
        return prepareAdminAppsApproveRequest(token, appId, requestId, teamId).execute();
    }

    /**
     * Approve an app for installation on a workspace.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.apps:write`
     * @param  appId  Optional parameter: The id of the app to approve.
     * @param  requestId  Optional parameter: The id of the request to approve.
     * @param  teamId  Optional parameter:
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminAppsApproveAsync(
            final String token,
            final String appId,
            final String requestId,
            final String teamId) {
        try {
            return prepareAdminAppsApproveRequest(token, appId, requestId, teamId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminAppsApprove.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminAppsApproveRequest(
            final String token,
            final String appId,
            final String requestId,
            final String teamId) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.apps.approve")
                        .formParam(param -> param.key("app_id")
                                .value(appId).isRequired(false))
                        .formParam(param -> param.key("request_id")
                                .value(requestId).isRequired(false))
                        .formParam(param -> param.key("team_id")
                                .value(teamId).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Restrict an app for installation on a workspace.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.apps:write`
     * @param  appId  Optional parameter: The id of the app to restrict.
     * @param  requestId  Optional parameter: The id of the request to restrict.
     * @param  teamId  Optional parameter:
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminAppsRestrict(
            final String token,
            final String appId,
            final String requestId,
            final String teamId) throws ApiException, IOException {
        return prepareAdminAppsRestrictRequest(token, appId, requestId, teamId).execute();
    }

    /**
     * Restrict an app for installation on a workspace.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.apps:write`
     * @param  appId  Optional parameter: The id of the app to restrict.
     * @param  requestId  Optional parameter: The id of the request to restrict.
     * @param  teamId  Optional parameter:
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminAppsRestrictAsync(
            final String token,
            final String appId,
            final String requestId,
            final String teamId) {
        try {
            return prepareAdminAppsRestrictRequest(token, appId, requestId, teamId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminAppsRestrict.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminAppsRestrictRequest(
            final String token,
            final String appId,
            final String requestId,
            final String teamId) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.apps.restrict")
                        .formParam(param -> param.key("app_id")
                                .value(appId).isRequired(false))
                        .formParam(param -> param.key("request_id")
                                .value(requestId).isRequired(false))
                        .formParam(param -> param.key("team_id")
                                .value(teamId).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}