/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.DefaultErrorTemplateException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.DefaultSuccessTemplate;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class AdminTeamsSettingsController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public AdminTeamsSettingsController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Fetch information about settings in a workspace.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:read`
     * @param  teamId  Required parameter:
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminTeamsSettingsInfo(
            final String token,
            final String teamId) throws ApiException, IOException {
        return prepareAdminTeamsSettingsInfoRequest(token, teamId).execute();
    }

    /**
     * Fetch information about settings in a workspace.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:read`
     * @param  teamId  Required parameter:
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminTeamsSettingsInfoAsync(
            final String token,
            final String teamId) {
        try {
            return prepareAdminTeamsSettingsInfoRequest(token, teamId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminTeamsSettingsInfo.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminTeamsSettingsInfoRequest(
            final String token,
            final String teamId) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.teams.settings.info")
                        .queryParam(param -> param.key("team_id")
                                .value(teamId))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Set the default channels of a workspace.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:write`
     * @param  teamId  Required parameter: ID for the workspace to set the default channel for.
     * @param  channelIds  Required parameter: An array of channel IDs.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminTeamsSettingsSetDefaultChannels(
            final String token,
            final String teamId,
            final String channelIds) throws ApiException, IOException {
        return prepareAdminTeamsSettingsSetDefaultChannelsRequest(token, teamId,
                channelIds).execute();
    }

    /**
     * Set the default channels of a workspace.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:write`
     * @param  teamId  Required parameter: ID for the workspace to set the default channel for.
     * @param  channelIds  Required parameter: An array of channel IDs.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminTeamsSettingsSetDefaultChannelsAsync(
            final String token,
            final String teamId,
            final String channelIds) {
        try {
            return prepareAdminTeamsSettingsSetDefaultChannelsRequest(token, teamId,
            channelIds).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminTeamsSettingsSetDefaultChannels.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminTeamsSettingsSetDefaultChannelsRequest(
            final String token,
            final String teamId,
            final String channelIds) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.teams.settings.setDefaultChannels")
                        .formParam(param -> param.key("token")
                                .value(token))
                        .formParam(param -> param.key("team_id")
                                .value(teamId))
                        .formParam(param -> param.key("channel_ids")
                                .value(channelIds))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Set the description of a given workspace.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:write`
     * @param  teamId  Required parameter: ID for the workspace to set the description for.
     * @param  description  Required parameter: The new description for the workspace.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminTeamsSettingsSetDescription(
            final String token,
            final String teamId,
            final String description) throws ApiException, IOException {
        return prepareAdminTeamsSettingsSetDescriptionRequest(token, teamId, description).execute();
    }

    /**
     * Set the description of a given workspace.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:write`
     * @param  teamId  Required parameter: ID for the workspace to set the description for.
     * @param  description  Required parameter: The new description for the workspace.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminTeamsSettingsSetDescriptionAsync(
            final String token,
            final String teamId,
            final String description) {
        try {
            return prepareAdminTeamsSettingsSetDescriptionRequest(token, teamId,
            description).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminTeamsSettingsSetDescription.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminTeamsSettingsSetDescriptionRequest(
            final String token,
            final String teamId,
            final String description) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.teams.settings.setDescription")
                        .formParam(param -> param.key("team_id")
                                .value(teamId))
                        .formParam(param -> param.key("description")
                                .value(description))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * An API method that allows admins to set the discoverability of a given workspace.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:write`
     * @param  teamId  Required parameter: The ID of the workspace to set discoverability on.
     * @param  discoverability  Required parameter: This workspace's discovery setting. It must be
     *         set to one of `open`, `invite_only`, `closed`, or `unlisted`.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminTeamsSettingsSetDiscoverability(
            final String token,
            final String teamId,
            final String discoverability) throws ApiException, IOException {
        return prepareAdminTeamsSettingsSetDiscoverabilityRequest(token, teamId,
                discoverability).execute();
    }

    /**
     * An API method that allows admins to set the discoverability of a given workspace.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:write`
     * @param  teamId  Required parameter: The ID of the workspace to set discoverability on.
     * @param  discoverability  Required parameter: This workspace's discovery setting. It must be
     *         set to one of `open`, `invite_only`, `closed`, or `unlisted`.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminTeamsSettingsSetDiscoverabilityAsync(
            final String token,
            final String teamId,
            final String discoverability) {
        try {
            return prepareAdminTeamsSettingsSetDiscoverabilityRequest(token, teamId,
            discoverability).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminTeamsSettingsSetDiscoverability.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminTeamsSettingsSetDiscoverabilityRequest(
            final String token,
            final String teamId,
            final String discoverability) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.teams.settings.setDiscoverability")
                        .formParam(param -> param.key("team_id")
                                .value(teamId))
                        .formParam(param -> param.key("discoverability")
                                .value(discoverability))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Sets the icon of a workspace.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:write`
     * @param  imageUrl  Required parameter: Image URL for the icon
     * @param  teamId  Required parameter: ID for the workspace to set the icon for.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminTeamsSettingsSetIcon(
            final String token,
            final String imageUrl,
            final String teamId) throws ApiException, IOException {
        return prepareAdminTeamsSettingsSetIconRequest(token, imageUrl, teamId).execute();
    }

    /**
     * Sets the icon of a workspace.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:write`
     * @param  imageUrl  Required parameter: Image URL for the icon
     * @param  teamId  Required parameter: ID for the workspace to set the icon for.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminTeamsSettingsSetIconAsync(
            final String token,
            final String imageUrl,
            final String teamId) {
        try {
            return prepareAdminTeamsSettingsSetIconRequest(token, imageUrl, teamId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminTeamsSettingsSetIcon.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminTeamsSettingsSetIconRequest(
            final String token,
            final String imageUrl,
            final String teamId) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.teams.settings.setIcon")
                        .formParam(param -> param.key("token")
                                .value(token))
                        .formParam(param -> param.key("image_url")
                                .value(imageUrl))
                        .formParam(param -> param.key("team_id")
                                .value(teamId))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Set the name of a given workspace.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:write`
     * @param  teamId  Required parameter: ID for the workspace to set the name for.
     * @param  name  Required parameter: The new name of the workspace.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminTeamsSettingsSetName(
            final String token,
            final String teamId,
            final String name) throws ApiException, IOException {
        return prepareAdminTeamsSettingsSetNameRequest(token, teamId, name).execute();
    }

    /**
     * Set the name of a given workspace.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:write`
     * @param  teamId  Required parameter: ID for the workspace to set the name for.
     * @param  name  Required parameter: The new name of the workspace.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminTeamsSettingsSetNameAsync(
            final String token,
            final String teamId,
            final String name) {
        try {
            return prepareAdminTeamsSettingsSetNameRequest(token, teamId, name).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminTeamsSettingsSetName.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminTeamsSettingsSetNameRequest(
            final String token,
            final String teamId,
            final String name) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.teams.settings.setName")
                        .formParam(param -> param.key("team_id")
                                .value(teamId))
                        .formParam(param -> param.key("name")
                                .value(name))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}