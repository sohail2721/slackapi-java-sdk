/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.DefaultErrorTemplateException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.DefaultSuccessTemplate;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class AdminUsersSessionController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public AdminUsersSessionController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Invalidate a single session for a user by session_id.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.users:write`
     * @param  teamId  Required parameter: ID of the team that the session belongs to
     * @param  sessionId  Required parameter:
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminUsersSessionInvalidate(
            final String token,
            final String teamId,
            final int sessionId) throws ApiException, IOException {
        return prepareAdminUsersSessionInvalidateRequest(token, teamId, sessionId).execute();
    }

    /**
     * Invalidate a single session for a user by session_id.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.users:write`
     * @param  teamId  Required parameter: ID of the team that the session belongs to
     * @param  sessionId  Required parameter:
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminUsersSessionInvalidateAsync(
            final String token,
            final String teamId,
            final int sessionId) {
        try {
            return prepareAdminUsersSessionInvalidateRequest(token, teamId, sessionId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminUsersSessionInvalidate.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminUsersSessionInvalidateRequest(
            final String token,
            final String teamId,
            final int sessionId) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.users.session.invalidate")
                        .formParam(param -> param.key("team_id")
                                .value(teamId))
                        .formParam(param -> param.key("session_id")
                                .value(sessionId).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Wipes all valid sessions on all devices for a given user.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.users:write`
     * @param  userId  Required parameter: The ID of the user to wipe sessions for
     * @param  mobileOnly  Optional parameter: Only expire mobile sessions (default: false)
     * @param  webOnly  Optional parameter: Only expire web sessions (default: false)
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminUsersSessionReset(
            final String token,
            final String userId,
            final Boolean mobileOnly,
            final Boolean webOnly) throws ApiException, IOException {
        return prepareAdminUsersSessionResetRequest(token, userId, mobileOnly, webOnly).execute();
    }

    /**
     * Wipes all valid sessions on all devices for a given user.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.users:write`
     * @param  userId  Required parameter: The ID of the user to wipe sessions for
     * @param  mobileOnly  Optional parameter: Only expire mobile sessions (default: false)
     * @param  webOnly  Optional parameter: Only expire web sessions (default: false)
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminUsersSessionResetAsync(
            final String token,
            final String userId,
            final Boolean mobileOnly,
            final Boolean webOnly) {
        try {
            return prepareAdminUsersSessionResetRequest(token, userId, mobileOnly, webOnly).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminUsersSessionReset.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminUsersSessionResetRequest(
            final String token,
            final String userId,
            final Boolean mobileOnly,
            final Boolean webOnly) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.users.session.reset")
                        .formParam(param -> param.key("user_id")
                                .value(userId))
                        .formParam(param -> param.key("mobile_only")
                                .value(mobileOnly).isRequired(false))
                        .formParam(param -> param.key("web_only")
                                .value(webOnly).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}