/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.FilesDeleteErrorSchemaException;
import com.slack.exceptions.FilesInfoErrorSchemaException;
import com.slack.exceptions.FilesListErrorSchemaException;
import com.slack.exceptions.FilesRevokePublicUrlErrorSchemaException;
import com.slack.exceptions.FilesSharedPublicUrlErrorSchemaException;
import com.slack.exceptions.FilesUploadErrorSchemaException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.FilesDeleteSchema;
import com.slack.models.FilesInfoSchema;
import com.slack.models.FilesListSchema;
import com.slack.models.FilesRevokePublicUrlSchema;
import com.slack.models.FilesSharedPublicUrlSchema;
import com.slack.models.FilesUploadSchema;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class FilesController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public FilesController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Deletes a file.
     * @param  token  Optional parameter: Authentication token. Requires scope: `files:write:user`
     * @param  file  Optional parameter: ID of file to delete.
     * @return    Returns the FilesDeleteSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<FilesDeleteSchema> filesDelete(
            final String token,
            final String file) throws ApiException, IOException {
        return prepareFilesDeleteRequest(token, file).execute();
    }

    /**
     * Deletes a file.
     * @param  token  Optional parameter: Authentication token. Requires scope: `files:write:user`
     * @param  file  Optional parameter: ID of file to delete.
     * @return    Returns the FilesDeleteSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<FilesDeleteSchema>> filesDeleteAsync(
            final String token,
            final String file) {
        try {
            return prepareFilesDeleteRequest(token, file).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for filesDelete.
     */
    private ApiCall<ApiResponse<FilesDeleteSchema>, ApiException> prepareFilesDeleteRequest(
            final String token,
            final String file) {
        return new ApiCall.Builder<ApiResponse<FilesDeleteSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/files.delete")
                        .formParam(param -> param.key("file")
                                .value(file).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, FilesDeleteSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new FilesDeleteErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets information about a file.
     * @param  token  Optional parameter: Authentication token. Requires scope: `files:read`
     * @param  file  Optional parameter: Specify a file by providing its ID.
     * @param  count  Optional parameter:
     * @param  page  Optional parameter:
     * @param  limit  Optional parameter: The maximum number of items to return. Fewer than the
     *         requested number of items may be returned, even if the end of the list hasn't been
     *         reached.
     * @param  cursor  Optional parameter: Parameter for pagination. File comments are paginated for
     *         a single file. Set `cursor` equal to the `next_cursor` attribute returned by the
     *         previous request's `response_metadata`. This parameter is optional, but pagination is
     *         mandatory: the default value simply fetches the first "page" of the collection of
     *         comments. See [pagination](/docs/pagination) for more details.
     * @return    Returns the FilesInfoSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<FilesInfoSchema> filesInfo(
            final String token,
            final String file,
            final String count,
            final String page,
            final Integer limit,
            final String cursor) throws ApiException, IOException {
        return prepareFilesInfoRequest(token, file, count, page, limit, cursor).execute();
    }

    /**
     * Gets information about a file.
     * @param  token  Optional parameter: Authentication token. Requires scope: `files:read`
     * @param  file  Optional parameter: Specify a file by providing its ID.
     * @param  count  Optional parameter:
     * @param  page  Optional parameter:
     * @param  limit  Optional parameter: The maximum number of items to return. Fewer than the
     *         requested number of items may be returned, even if the end of the list hasn't been
     *         reached.
     * @param  cursor  Optional parameter: Parameter for pagination. File comments are paginated for
     *         a single file. Set `cursor` equal to the `next_cursor` attribute returned by the
     *         previous request's `response_metadata`. This parameter is optional, but pagination is
     *         mandatory: the default value simply fetches the first "page" of the collection of
     *         comments. See [pagination](/docs/pagination) for more details.
     * @return    Returns the FilesInfoSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<FilesInfoSchema>> filesInfoAsync(
            final String token,
            final String file,
            final String count,
            final String page,
            final Integer limit,
            final String cursor) {
        try {
            return prepareFilesInfoRequest(token, file, count, page, limit, cursor).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for filesInfo.
     */
    private ApiCall<ApiResponse<FilesInfoSchema>, ApiException> prepareFilesInfoRequest(
            final String token,
            final String file,
            final String count,
            final String page,
            final Integer limit,
            final String cursor) {
        return new ApiCall.Builder<ApiResponse<FilesInfoSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/files.info")
                        .queryParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .queryParam(param -> param.key("file")
                                .value(file).isRequired(false))
                        .queryParam(param -> param.key("count")
                                .value(count).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .queryParam(param -> param.key("limit")
                                .value(limit).isRequired(false))
                        .queryParam(param -> param.key("cursor")
                                .value(cursor).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, FilesInfoSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new FilesInfoErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * List for a team, in a channel, or from a user with applied filters.
     * @param  token  Optional parameter: Authentication token. Requires scope: `files:read`
     * @param  user  Optional parameter: Filter files created by a single user.
     * @param  channel  Optional parameter: Filter files appearing in a specific channel, indicated
     *         by its ID.
     * @param  tsFrom  Optional parameter: Filter files created after this timestamp (inclusive).
     * @param  tsTo  Optional parameter: Filter files created before this timestamp (inclusive).
     * @param  types  Optional parameter: Filter files by type ([see below](#file_types)). You can
     *         pass multiple values in the types argument, like `types=spaces,snippets`.The default
     *         value is `all`, which does not filter the list.
     * @param  count  Optional parameter:
     * @param  page  Optional parameter:
     * @param  showFilesHiddenByLimit  Optional parameter: Show truncated file info for files hidden
     *         due to being too old, and the team who owns the file being over the file limit.
     * @return    Returns the FilesListSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<FilesListSchema> filesList(
            final String token,
            final String user,
            final String channel,
            final Double tsFrom,
            final Double tsTo,
            final String types,
            final String count,
            final String page,
            final Boolean showFilesHiddenByLimit) throws ApiException, IOException {
        return prepareFilesListRequest(token, user, channel, tsFrom, tsTo, types, count, page,
                showFilesHiddenByLimit).execute();
    }

    /**
     * List for a team, in a channel, or from a user with applied filters.
     * @param  token  Optional parameter: Authentication token. Requires scope: `files:read`
     * @param  user  Optional parameter: Filter files created by a single user.
     * @param  channel  Optional parameter: Filter files appearing in a specific channel, indicated
     *         by its ID.
     * @param  tsFrom  Optional parameter: Filter files created after this timestamp (inclusive).
     * @param  tsTo  Optional parameter: Filter files created before this timestamp (inclusive).
     * @param  types  Optional parameter: Filter files by type ([see below](#file_types)). You can
     *         pass multiple values in the types argument, like `types=spaces,snippets`.The default
     *         value is `all`, which does not filter the list.
     * @param  count  Optional parameter:
     * @param  page  Optional parameter:
     * @param  showFilesHiddenByLimit  Optional parameter: Show truncated file info for files hidden
     *         due to being too old, and the team who owns the file being over the file limit.
     * @return    Returns the FilesListSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<FilesListSchema>> filesListAsync(
            final String token,
            final String user,
            final String channel,
            final Double tsFrom,
            final Double tsTo,
            final String types,
            final String count,
            final String page,
            final Boolean showFilesHiddenByLimit) {
        try {
            return prepareFilesListRequest(token, user, channel, tsFrom, tsTo, types, count, page,
            showFilesHiddenByLimit).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for filesList.
     */
    private ApiCall<ApiResponse<FilesListSchema>, ApiException> prepareFilesListRequest(
            final String token,
            final String user,
            final String channel,
            final Double tsFrom,
            final Double tsTo,
            final String types,
            final String count,
            final String page,
            final Boolean showFilesHiddenByLimit) {
        return new ApiCall.Builder<ApiResponse<FilesListSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/files.list")
                        .queryParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .queryParam(param -> param.key("user")
                                .value(user).isRequired(false))
                        .queryParam(param -> param.key("channel")
                                .value(channel).isRequired(false))
                        .queryParam(param -> param.key("ts_from")
                                .value(tsFrom).isRequired(false))
                        .queryParam(param -> param.key("ts_to")
                                .value(tsTo).isRequired(false))
                        .queryParam(param -> param.key("types")
                                .value(types).isRequired(false))
                        .queryParam(param -> param.key("count")
                                .value(count).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .queryParam(param -> param.key("show_files_hidden_by_limit")
                                .value(showFilesHiddenByLimit).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, FilesListSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new FilesListErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Revokes public/external sharing access for a file.
     * @param  token  Optional parameter: Authentication token. Requires scope: `files:write:user`
     * @param  file  Optional parameter: File to revoke
     * @return    Returns the FilesRevokePublicUrlSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<FilesRevokePublicUrlSchema> filesRevokePublicUrl(
            final String token,
            final String file) throws ApiException, IOException {
        return prepareFilesRevokePublicUrlRequest(token, file).execute();
    }

    /**
     * Revokes public/external sharing access for a file.
     * @param  token  Optional parameter: Authentication token. Requires scope: `files:write:user`
     * @param  file  Optional parameter: File to revoke
     * @return    Returns the FilesRevokePublicUrlSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<FilesRevokePublicUrlSchema>> filesRevokePublicUrlAsync(
            final String token,
            final String file) {
        try {
            return prepareFilesRevokePublicUrlRequest(token, file).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for filesRevokePublicUrl.
     */
    private ApiCall<ApiResponse<FilesRevokePublicUrlSchema>, ApiException> prepareFilesRevokePublicUrlRequest(
            final String token,
            final String file) {
        return new ApiCall.Builder<ApiResponse<FilesRevokePublicUrlSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/files.revokePublicURL")
                        .formParam(param -> param.key("file")
                                .value(file).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, FilesRevokePublicUrlSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new FilesRevokePublicUrlErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Enables a file for public/external sharing.
     * @param  token  Optional parameter: Authentication token. Requires scope: `files:write:user`
     * @param  file  Optional parameter: File to share
     * @return    Returns the FilesSharedPublicUrlSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<FilesSharedPublicUrlSchema> filesSharedPublicUrl(
            final String token,
            final String file) throws ApiException, IOException {
        return prepareFilesSharedPublicUrlRequest(token, file).execute();
    }

    /**
     * Enables a file for public/external sharing.
     * @param  token  Optional parameter: Authentication token. Requires scope: `files:write:user`
     * @param  file  Optional parameter: File to share
     * @return    Returns the FilesSharedPublicUrlSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<FilesSharedPublicUrlSchema>> filesSharedPublicUrlAsync(
            final String token,
            final String file) {
        try {
            return prepareFilesSharedPublicUrlRequest(token, file).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for filesSharedPublicUrl.
     */
    private ApiCall<ApiResponse<FilesSharedPublicUrlSchema>, ApiException> prepareFilesSharedPublicUrlRequest(
            final String token,
            final String file) {
        return new ApiCall.Builder<ApiResponse<FilesSharedPublicUrlSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/files.sharedPublicURL")
                        .formParam(param -> param.key("file")
                                .value(file).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, FilesSharedPublicUrlSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new FilesSharedPublicUrlErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Uploads or creates a file.
     * @param  token  Optional parameter: Authentication token. Requires scope: `files:write:user`
     * @param  file  Optional parameter: File contents via `multipart/form-data`. If omitting this
     *         parameter, you must submit `content`.
     * @param  content  Optional parameter: File contents via a POST variable. If omitting this
     *         parameter, you must provide a `file`.
     * @param  filetype  Optional parameter: A [file type](/types/file#file_types) identifier.
     * @param  filename  Optional parameter: Filename of file.
     * @param  title  Optional parameter: Title of file.
     * @param  initialComment  Optional parameter: The message text introducing the file in
     *         specified `channels`.
     * @param  channels  Optional parameter: Comma-separated list of channel names or IDs where the
     *         file will be shared.
     * @param  threadTs  Optional parameter: Provide another message's `ts` value to upload this
     *         file as a reply. Never use a reply's `ts` value; use its parent instead.
     * @return    Returns the FilesUploadSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<FilesUploadSchema> filesUpload(
            final String token,
            final String file,
            final String content,
            final String filetype,
            final String filename,
            final String title,
            final String initialComment,
            final String channels,
            final Double threadTs) throws ApiException, IOException {
        return prepareFilesUploadRequest(token, file, content, filetype, filename, title,
                initialComment, channels, threadTs).execute();
    }

    /**
     * Uploads or creates a file.
     * @param  token  Optional parameter: Authentication token. Requires scope: `files:write:user`
     * @param  file  Optional parameter: File contents via `multipart/form-data`. If omitting this
     *         parameter, you must submit `content`.
     * @param  content  Optional parameter: File contents via a POST variable. If omitting this
     *         parameter, you must provide a `file`.
     * @param  filetype  Optional parameter: A [file type](/types/file#file_types) identifier.
     * @param  filename  Optional parameter: Filename of file.
     * @param  title  Optional parameter: Title of file.
     * @param  initialComment  Optional parameter: The message text introducing the file in
     *         specified `channels`.
     * @param  channels  Optional parameter: Comma-separated list of channel names or IDs where the
     *         file will be shared.
     * @param  threadTs  Optional parameter: Provide another message's `ts` value to upload this
     *         file as a reply. Never use a reply's `ts` value; use its parent instead.
     * @return    Returns the FilesUploadSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<FilesUploadSchema>> filesUploadAsync(
            final String token,
            final String file,
            final String content,
            final String filetype,
            final String filename,
            final String title,
            final String initialComment,
            final String channels,
            final Double threadTs) {
        try {
            return prepareFilesUploadRequest(token, file, content, filetype, filename, title,
            initialComment, channels, threadTs).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for filesUpload.
     */
    private ApiCall<ApiResponse<FilesUploadSchema>, ApiException> prepareFilesUploadRequest(
            final String token,
            final String file,
            final String content,
            final String filetype,
            final String filename,
            final String title,
            final String initialComment,
            final String channels,
            final Double threadTs) {
        return new ApiCall.Builder<ApiResponse<FilesUploadSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/files.upload")
                        .formParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .formParam(param -> param.key("file")
                                .value(file).isRequired(false))
                        .formParam(param -> param.key("content")
                                .value(content).isRequired(false))
                        .formParam(param -> param.key("filetype")
                                .value(filetype).isRequired(false))
                        .formParam(param -> param.key("filename")
                                .value(filename).isRequired(false))
                        .formParam(param -> param.key("title")
                                .value(title).isRequired(false))
                        .formParam(param -> param.key("initial_comment")
                                .value(initialComment).isRequired(false))
                        .formParam(param -> param.key("channels")
                                .value(channels).isRequired(false))
                        .formParam(param -> param.key("thread_ts")
                                .value(threadTs).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, FilesUploadSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new FilesUploadErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}