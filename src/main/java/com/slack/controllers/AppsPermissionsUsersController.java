/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.DefaultErrorTemplateException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.DefaultSuccessTemplate;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class AppsPermissionsUsersController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public AppsPermissionsUsersController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Returns list of user grants and corresponding scopes this app has on a team.
     * @param  token  Required parameter: Authentication token. Requires scope: `none`
     * @param  cursor  Optional parameter: Paginate through collections of data by setting the
     *         `cursor` parameter to a `next_cursor` attribute returned by a previous request's
     *         `response_metadata`. Default value fetches the first "page" of the collection. See
     *         [pagination](/docs/pagination) for more detail.
     * @param  limit  Optional parameter: The maximum number of items to return.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> appsPermissionsUsersList(
            final String token,
            final String cursor,
            final Integer limit) throws ApiException, IOException {
        return prepareAppsPermissionsUsersListRequest(token, cursor, limit).execute();
    }

    /**
     * Returns list of user grants and corresponding scopes this app has on a team.
     * @param  token  Required parameter: Authentication token. Requires scope: `none`
     * @param  cursor  Optional parameter: Paginate through collections of data by setting the
     *         `cursor` parameter to a `next_cursor` attribute returned by a previous request's
     *         `response_metadata`. Default value fetches the first "page" of the collection. See
     *         [pagination](/docs/pagination) for more detail.
     * @param  limit  Optional parameter: The maximum number of items to return.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> appsPermissionsUsersListAsync(
            final String token,
            final String cursor,
            final Integer limit) {
        try {
            return prepareAppsPermissionsUsersListRequest(token, cursor, limit).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for appsPermissionsUsersList.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAppsPermissionsUsersListRequest(
            final String token,
            final String cursor,
            final Integer limit) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/apps.permissions.users.list")
                        .queryParam(param -> param.key("token")
                                .value(token))
                        .queryParam(param -> param.key("cursor")
                                .value(cursor).isRequired(false))
                        .queryParam(param -> param.key("limit")
                                .value(limit).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Enables an app to trigger a permissions modal to grant an app access to a user access scope.
     * @param  token  Required parameter: Authentication token. Requires scope: `none`
     * @param  scopes  Required parameter: A comma separated list of user scopes to request for
     * @param  triggerId  Required parameter: Token used to trigger the request
     * @param  user  Required parameter: The user this scope is being requested for
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> appsPermissionsUsersRequest(
            final String token,
            final String scopes,
            final String triggerId,
            final String user) throws ApiException, IOException {
        return prepareAppsPermissionsUsersRequestRequest(token, scopes, triggerId, user).execute();
    }

    /**
     * Enables an app to trigger a permissions modal to grant an app access to a user access scope.
     * @param  token  Required parameter: Authentication token. Requires scope: `none`
     * @param  scopes  Required parameter: A comma separated list of user scopes to request for
     * @param  triggerId  Required parameter: Token used to trigger the request
     * @param  user  Required parameter: The user this scope is being requested for
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> appsPermissionsUsersRequestAsync(
            final String token,
            final String scopes,
            final String triggerId,
            final String user) {
        try {
            return prepareAppsPermissionsUsersRequestRequest(token, scopes, triggerId, user).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for appsPermissionsUsersRequest.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAppsPermissionsUsersRequestRequest(
            final String token,
            final String scopes,
            final String triggerId,
            final String user) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/apps.permissions.users.request")
                        .queryParam(param -> param.key("token")
                                .value(token))
                        .queryParam(param -> param.key("scopes")
                                .value(scopes))
                        .queryParam(param -> param.key("trigger_id")
                                .value(triggerId))
                        .queryParam(param -> param.key("user")
                                .value(user))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Standard failure response when trigger_id is invalid",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}