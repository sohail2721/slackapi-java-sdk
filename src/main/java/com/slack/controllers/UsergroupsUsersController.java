/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.UsergroupsUsersListErrorSchemaException;
import com.slack.exceptions.UsergroupsUsersUpdateErrorSchemaException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.UsergroupsUsersListSchema;
import com.slack.models.UsergroupsUsersUpdateSchema;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class UsergroupsUsersController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public UsergroupsUsersController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * List all users in a User Group.
     * @param  token  Required parameter: Authentication token. Requires scope: `usergroups:read`
     * @param  usergroup  Required parameter: The encoded ID of the User Group to update.
     * @param  includeDisabled  Optional parameter: Allow results that involve disabled User Groups.
     * @return    Returns the UsergroupsUsersListSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<UsergroupsUsersListSchema> usergroupsUsersList(
            final String token,
            final String usergroup,
            final Boolean includeDisabled) throws ApiException, IOException {
        return prepareUsergroupsUsersListRequest(token, usergroup, includeDisabled).execute();
    }

    /**
     * List all users in a User Group.
     * @param  token  Required parameter: Authentication token. Requires scope: `usergroups:read`
     * @param  usergroup  Required parameter: The encoded ID of the User Group to update.
     * @param  includeDisabled  Optional parameter: Allow results that involve disabled User Groups.
     * @return    Returns the UsergroupsUsersListSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<UsergroupsUsersListSchema>> usergroupsUsersListAsync(
            final String token,
            final String usergroup,
            final Boolean includeDisabled) {
        try {
            return prepareUsergroupsUsersListRequest(token, usergroup, includeDisabled).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for usergroupsUsersList.
     */
    private ApiCall<ApiResponse<UsergroupsUsersListSchema>, ApiException> prepareUsergroupsUsersListRequest(
            final String token,
            final String usergroup,
            final Boolean includeDisabled) {
        return new ApiCall.Builder<ApiResponse<UsergroupsUsersListSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/usergroups.users.list")
                        .queryParam(param -> param.key("token")
                                .value(token))
                        .queryParam(param -> param.key("usergroup")
                                .value(usergroup))
                        .queryParam(param -> param.key("include_disabled")
                                .value(includeDisabled).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, UsergroupsUsersListSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Standard failure response when used with an invalid token",
                                (reason, context) -> new UsergroupsUsersListErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Update the list of users for a User Group.
     * @param  token  Required parameter: Authentication token. Requires scope: `usergroups:write`
     * @param  usergroup  Required parameter: The encoded ID of the User Group to update.
     * @param  users  Required parameter: A comma separated string of encoded user IDs that
     *         represent the entire list of users for the User Group.
     * @param  includeCount  Optional parameter: Include the number of users in the User Group.
     * @return    Returns the UsergroupsUsersUpdateSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<UsergroupsUsersUpdateSchema> usergroupsUsersUpdate(
            final String token,
            final String usergroup,
            final String users,
            final Boolean includeCount) throws ApiException, IOException {
        return prepareUsergroupsUsersUpdateRequest(token, usergroup, users, includeCount).execute();
    }

    /**
     * Update the list of users for a User Group.
     * @param  token  Required parameter: Authentication token. Requires scope: `usergroups:write`
     * @param  usergroup  Required parameter: The encoded ID of the User Group to update.
     * @param  users  Required parameter: A comma separated string of encoded user IDs that
     *         represent the entire list of users for the User Group.
     * @param  includeCount  Optional parameter: Include the number of users in the User Group.
     * @return    Returns the UsergroupsUsersUpdateSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<UsergroupsUsersUpdateSchema>> usergroupsUsersUpdateAsync(
            final String token,
            final String usergroup,
            final String users,
            final Boolean includeCount) {
        try {
            return prepareUsergroupsUsersUpdateRequest(token, usergroup, users,
            includeCount).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for usergroupsUsersUpdate.
     */
    private ApiCall<ApiResponse<UsergroupsUsersUpdateSchema>, ApiException> prepareUsergroupsUsersUpdateRequest(
            final String token,
            final String usergroup,
            final String users,
            final Boolean includeCount) {
        return new ApiCall.Builder<ApiResponse<UsergroupsUsersUpdateSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/usergroups.users.update")
                        .formParam(param -> param.key("usergroup")
                                .value(usergroup))
                        .formParam(param -> param.key("users")
                                .value(users))
                        .formParam(param -> param.key("include_count")
                                .value(includeCount).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, UsergroupsUsersUpdateSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new UsergroupsUsersUpdateErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}