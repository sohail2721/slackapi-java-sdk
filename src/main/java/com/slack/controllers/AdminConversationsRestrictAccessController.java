/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.DefaultErrorTemplateException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.DefaultSuccessTemplate;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class AdminConversationsRestrictAccessController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public AdminConversationsRestrictAccessController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Add an allowlist of IDP groups for accessing a channel.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:write`
     * @param  groupId  Required parameter: The [IDP
     *         Group](https://slack.com/help/articles/115001435788-Connect-identity-provider-groups-to-your-Enterprise-Grid-org)
     *         ID to be an allowlist for the private channel.
     * @param  channelId  Required parameter: The channel to link this group to.
     * @param  teamId  Optional parameter: The workspace where the channel exists. This argument is
     *         required for channels only tied to one workspace, and optional for channels that are
     *         shared across an organization.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminConversationsRestrictAccessAddGroup(
            final String token,
            final String groupId,
            final String channelId,
            final String teamId) throws ApiException, IOException {
        return prepareAdminConversationsRestrictAccessAddGroupRequest(token, groupId, channelId,
                teamId).execute();
    }

    /**
     * Add an allowlist of IDP groups for accessing a channel.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:write`
     * @param  groupId  Required parameter: The [IDP
     *         Group](https://slack.com/help/articles/115001435788-Connect-identity-provider-groups-to-your-Enterprise-Grid-org)
     *         ID to be an allowlist for the private channel.
     * @param  channelId  Required parameter: The channel to link this group to.
     * @param  teamId  Optional parameter: The workspace where the channel exists. This argument is
     *         required for channels only tied to one workspace, and optional for channels that are
     *         shared across an organization.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminConversationsRestrictAccessAddGroupAsync(
            final String token,
            final String groupId,
            final String channelId,
            final String teamId) {
        try {
            return prepareAdminConversationsRestrictAccessAddGroupRequest(token, groupId, channelId,
            teamId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminConversationsRestrictAccessAddGroup.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminConversationsRestrictAccessAddGroupRequest(
            final String token,
            final String groupId,
            final String channelId,
            final String teamId) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.conversations.restrictAccess.addGroup")
                        .formParam(param -> param.key("token")
                                .value(token))
                        .formParam(param -> param.key("group_id")
                                .value(groupId))
                        .formParam(param -> param.key("channel_id")
                                .value(channelId))
                        .formParam(param -> param.key("team_id")
                                .value(teamId).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * List all IDP Groups linked to a channel.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:read`
     * @param  channelId  Required parameter:
     * @param  teamId  Optional parameter: The workspace where the channel exists. This argument is
     *         required for channels only tied to one workspace, and optional for channels that are
     *         shared across an organization.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminConversationsRestrictAccessListGroups(
            final String token,
            final String channelId,
            final String teamId) throws ApiException, IOException {
        return prepareAdminConversationsRestrictAccessListGroupsRequest(token, channelId,
                teamId).execute();
    }

    /**
     * List all IDP Groups linked to a channel.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:read`
     * @param  channelId  Required parameter:
     * @param  teamId  Optional parameter: The workspace where the channel exists. This argument is
     *         required for channels only tied to one workspace, and optional for channels that are
     *         shared across an organization.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminConversationsRestrictAccessListGroupsAsync(
            final String token,
            final String channelId,
            final String teamId) {
        try {
            return prepareAdminConversationsRestrictAccessListGroupsRequest(token, channelId,
            teamId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminConversationsRestrictAccessListGroups.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminConversationsRestrictAccessListGroupsRequest(
            final String token,
            final String channelId,
            final String teamId) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.conversations.restrictAccess.listGroups")
                        .queryParam(param -> param.key("token")
                                .value(token))
                        .queryParam(param -> param.key("channel_id")
                                .value(channelId))
                        .queryParam(param -> param.key("team_id")
                                .value(teamId).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Remove a linked IDP group linked from a private channel.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:write`
     * @param  teamId  Required parameter: The workspace where the channel exists. This argument is
     *         required for channels only tied to one workspace, and optional for channels that are
     *         shared across an organization.
     * @param  groupId  Required parameter: The [IDP
     *         Group](https://slack.com/help/articles/115001435788-Connect-identity-provider-groups-to-your-Enterprise-Grid-org)
     *         ID to remove from the private channel.
     * @param  channelId  Required parameter: The channel to remove the linked group from.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminConversationsRestrictAccessRemoveGroup(
            final String token,
            final String teamId,
            final String groupId,
            final String channelId) throws ApiException, IOException {
        return prepareAdminConversationsRestrictAccessRemoveGroupRequest(token, teamId, groupId,
                channelId).execute();
    }

    /**
     * Remove a linked IDP group linked from a private channel.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:write`
     * @param  teamId  Required parameter: The workspace where the channel exists. This argument is
     *         required for channels only tied to one workspace, and optional for channels that are
     *         shared across an organization.
     * @param  groupId  Required parameter: The [IDP
     *         Group](https://slack.com/help/articles/115001435788-Connect-identity-provider-groups-to-your-Enterprise-Grid-org)
     *         ID to remove from the private channel.
     * @param  channelId  Required parameter: The channel to remove the linked group from.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminConversationsRestrictAccessRemoveGroupAsync(
            final String token,
            final String teamId,
            final String groupId,
            final String channelId) {
        try {
            return prepareAdminConversationsRestrictAccessRemoveGroupRequest(token, teamId, groupId,
            channelId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminConversationsRestrictAccessRemoveGroup.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminConversationsRestrictAccessRemoveGroupRequest(
            final String token,
            final String teamId,
            final String groupId,
            final String channelId) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.conversations.restrictAccess.removeGroup")
                        .formParam(param -> param.key("token")
                                .value(token))
                        .formParam(param -> param.key("team_id")
                                .value(teamId))
                        .formParam(param -> param.key("group_id")
                                .value(groupId))
                        .formParam(param -> param.key("channel_id")
                                .value(channelId))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}