/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.DefaultErrorTemplateException;
import com.slack.exceptions.TeamAccessLogsErrorSchemaException;
import com.slack.exceptions.TeamInfoErrorSchemaException;
import com.slack.exceptions.TeamIntegrationLogsErrorSchemaException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.DefaultSuccessTemplate;
import com.slack.models.TeamAccessLogsSchema;
import com.slack.models.TeamInfoSchema;
import com.slack.models.TeamIntegrationLogsSchema;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class TeamController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public TeamController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Gets the access logs for the current team.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin`
     * @param  before  Optional parameter: End of time range of logs to include in results
     *         (inclusive).
     * @param  count  Optional parameter:
     * @param  page  Optional parameter:
     * @return    Returns the TeamAccessLogsSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<TeamAccessLogsSchema> teamAccessLogs(
            final String token,
            final String before,
            final String count,
            final String page) throws ApiException, IOException {
        return prepareTeamAccessLogsRequest(token, before, count, page).execute();
    }

    /**
     * Gets the access logs for the current team.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin`
     * @param  before  Optional parameter: End of time range of logs to include in results
     *         (inclusive).
     * @param  count  Optional parameter:
     * @param  page  Optional parameter:
     * @return    Returns the TeamAccessLogsSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<TeamAccessLogsSchema>> teamAccessLogsAsync(
            final String token,
            final String before,
            final String count,
            final String page) {
        try {
            return prepareTeamAccessLogsRequest(token, before, count, page).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for teamAccessLogs.
     */
    private ApiCall<ApiResponse<TeamAccessLogsSchema>, ApiException> prepareTeamAccessLogsRequest(
            final String token,
            final String before,
            final String count,
            final String page) {
        return new ApiCall.Builder<ApiResponse<TeamAccessLogsSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/team.accessLogs")
                        .queryParam(param -> param.key("token")
                                .value(token))
                        .queryParam(param -> param.key("before")
                                .value(before).isRequired(false))
                        .queryParam(param -> param.key("count")
                                .value(count).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, TeamAccessLogsSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("A workspace must be on a paid plan to use this method, otherwise the `paid_only` error is thrown:",
                                (reason, context) -> new TeamAccessLogsErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets billable users information for the current team.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin`
     * @param  user  Optional parameter: A user to retrieve the billable information for. Defaults
     *         to all users.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> teamBillableInfo(
            final String token,
            final String user) throws ApiException, IOException {
        return prepareTeamBillableInfoRequest(token, user).execute();
    }

    /**
     * Gets billable users information for the current team.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin`
     * @param  user  Optional parameter: A user to retrieve the billable information for. Defaults
     *         to all users.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> teamBillableInfoAsync(
            final String token,
            final String user) {
        try {
            return prepareTeamBillableInfoRequest(token, user).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for teamBillableInfo.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareTeamBillableInfoRequest(
            final String token,
            final String user) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/team.billableInfo")
                        .queryParam(param -> param.key("token")
                                .value(token))
                        .queryParam(param -> param.key("user")
                                .value(user).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets information about the current team.
     * @param  token  Required parameter: Authentication token. Requires scope: `team:read`
     * @param  team  Optional parameter: Team to get info on, if omitted, will return information
     *         about the current team. Will only return team that the authenticated token is allowed
     *         to see through external shared channels
     * @return    Returns the TeamInfoSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<TeamInfoSchema> teamInfo(
            final String token,
            final String team) throws ApiException, IOException {
        return prepareTeamInfoRequest(token, team).execute();
    }

    /**
     * Gets information about the current team.
     * @param  token  Required parameter: Authentication token. Requires scope: `team:read`
     * @param  team  Optional parameter: Team to get info on, if omitted, will return information
     *         about the current team. Will only return team that the authenticated token is allowed
     *         to see through external shared channels
     * @return    Returns the TeamInfoSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<TeamInfoSchema>> teamInfoAsync(
            final String token,
            final String team) {
        try {
            return prepareTeamInfoRequest(token, team).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for teamInfo.
     */
    private ApiCall<ApiResponse<TeamInfoSchema>, ApiException> prepareTeamInfoRequest(
            final String token,
            final String team) {
        return new ApiCall.Builder<ApiResponse<TeamInfoSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/team.info")
                        .queryParam(param -> param.key("token")
                                .value(token))
                        .queryParam(param -> param.key("team")
                                .value(team).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, TeamInfoSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new TeamInfoErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets the integration logs for the current team.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin`
     * @param  appId  Optional parameter: Filter logs to this Slack app. Defaults to all logs.
     * @param  changeType  Optional parameter: Filter logs with this change type. Defaults to all
     *         logs.
     * @param  count  Optional parameter:
     * @param  page  Optional parameter:
     * @param  serviceId  Optional parameter: Filter logs to this service. Defaults to all logs.
     * @param  user  Optional parameter: Filter logs generated by this user’s actions. Defaults to
     *         all logs.
     * @return    Returns the TeamIntegrationLogsSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<TeamIntegrationLogsSchema> teamIntegrationLogs(
            final String token,
            final String appId,
            final String changeType,
            final String count,
            final String page,
            final String serviceId,
            final String user) throws ApiException, IOException {
        return prepareTeamIntegrationLogsRequest(token, appId, changeType, count, page, serviceId,
                user).execute();
    }

    /**
     * Gets the integration logs for the current team.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin`
     * @param  appId  Optional parameter: Filter logs to this Slack app. Defaults to all logs.
     * @param  changeType  Optional parameter: Filter logs with this change type. Defaults to all
     *         logs.
     * @param  count  Optional parameter:
     * @param  page  Optional parameter:
     * @param  serviceId  Optional parameter: Filter logs to this service. Defaults to all logs.
     * @param  user  Optional parameter: Filter logs generated by this user’s actions. Defaults to
     *         all logs.
     * @return    Returns the TeamIntegrationLogsSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<TeamIntegrationLogsSchema>> teamIntegrationLogsAsync(
            final String token,
            final String appId,
            final String changeType,
            final String count,
            final String page,
            final String serviceId,
            final String user) {
        try {
            return prepareTeamIntegrationLogsRequest(token, appId, changeType, count, page, serviceId,
            user).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for teamIntegrationLogs.
     */
    private ApiCall<ApiResponse<TeamIntegrationLogsSchema>, ApiException> prepareTeamIntegrationLogsRequest(
            final String token,
            final String appId,
            final String changeType,
            final String count,
            final String page,
            final String serviceId,
            final String user) {
        return new ApiCall.Builder<ApiResponse<TeamIntegrationLogsSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/team.integrationLogs")
                        .queryParam(param -> param.key("token")
                                .value(token))
                        .queryParam(param -> param.key("app_id")
                                .value(appId).isRequired(false))
                        .queryParam(param -> param.key("change_type")
                                .value(changeType).isRequired(false))
                        .queryParam(param -> param.key("count")
                                .value(count).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .queryParam(param -> param.key("service_id")
                                .value(serviceId).isRequired(false))
                        .queryParam(param -> param.key("user")
                                .value(user).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, TeamIntegrationLogsSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new TeamIntegrationLogsErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}