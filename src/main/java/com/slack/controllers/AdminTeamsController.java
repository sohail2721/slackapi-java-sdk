/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.DefaultErrorTemplateException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.DefaultSuccessTemplate;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class AdminTeamsController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public AdminTeamsController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Create an Enterprise team.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:write`
     * @param  teamDomain  Required parameter: Team domain (for example, slacksoftballteam).
     * @param  teamName  Required parameter: Team name (for example, Slack Softball Team).
     * @param  teamDescription  Optional parameter: Description for the team.
     * @param  teamDiscoverability  Optional parameter: Who can join the team. A team's
     *         discoverability can be `open`, `closed`, `invite_only`, or `unlisted`.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminTeamsCreate(
            final String token,
            final String teamDomain,
            final String teamName,
            final String teamDescription,
            final String teamDiscoverability) throws ApiException, IOException {
        return prepareAdminTeamsCreateRequest(token, teamDomain, teamName, teamDescription,
                teamDiscoverability).execute();
    }

    /**
     * Create an Enterprise team.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:write`
     * @param  teamDomain  Required parameter: Team domain (for example, slacksoftballteam).
     * @param  teamName  Required parameter: Team name (for example, Slack Softball Team).
     * @param  teamDescription  Optional parameter: Description for the team.
     * @param  teamDiscoverability  Optional parameter: Who can join the team. A team's
     *         discoverability can be `open`, `closed`, `invite_only`, or `unlisted`.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminTeamsCreateAsync(
            final String token,
            final String teamDomain,
            final String teamName,
            final String teamDescription,
            final String teamDiscoverability) {
        try {
            return prepareAdminTeamsCreateRequest(token, teamDomain, teamName, teamDescription,
            teamDiscoverability).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminTeamsCreate.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminTeamsCreateRequest(
            final String token,
            final String teamDomain,
            final String teamName,
            final String teamDescription,
            final String teamDiscoverability) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.teams.create")
                        .formParam(param -> param.key("team_domain")
                                .value(teamDomain))
                        .formParam(param -> param.key("team_name")
                                .value(teamName))
                        .formParam(param -> param.key("team_description")
                                .value(teamDescription).isRequired(false))
                        .formParam(param -> param.key("team_discoverability")
                                .value(teamDiscoverability).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * List all teams on an Enterprise organization.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:read`
     * @param  limit  Optional parameter: The maximum number of items to return. Must be between 1 -
     *         100 both inclusive.
     * @param  cursor  Optional parameter: Set `cursor` to `next_cursor` returned by the previous
     *         call to list items in the next page.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminTeamsList(
            final String token,
            final Integer limit,
            final String cursor) throws ApiException, IOException {
        return prepareAdminTeamsListRequest(token, limit, cursor).execute();
    }

    /**
     * List all teams on an Enterprise organization.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:read`
     * @param  limit  Optional parameter: The maximum number of items to return. Must be between 1 -
     *         100 both inclusive.
     * @param  cursor  Optional parameter: Set `cursor` to `next_cursor` returned by the previous
     *         call to list items in the next page.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminTeamsListAsync(
            final String token,
            final Integer limit,
            final String cursor) {
        try {
            return prepareAdminTeamsListRequest(token, limit, cursor).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminTeamsList.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminTeamsListRequest(
            final String token,
            final Integer limit,
            final String cursor) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.teams.list")
                        .queryParam(param -> param.key("limit")
                                .value(limit).isRequired(false))
                        .queryParam(param -> param.key("cursor")
                                .value(cursor).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}