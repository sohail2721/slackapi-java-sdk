/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.DefaultErrorTemplateException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.DefaultSuccessTemplate;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class AdminUsergroupsController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public AdminUsergroupsController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Add one or more default channels to an IDP group.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.usergroups:write`
     * @param  usergroupId  Required parameter: ID of the IDP group to add default channels for.
     * @param  channelIds  Required parameter: Comma separated string of channel IDs.
     * @param  teamId  Optional parameter: The workspace to add default channels in.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminUsergroupsAddChannels(
            final String token,
            final String usergroupId,
            final String channelIds,
            final String teamId) throws ApiException, IOException {
        return prepareAdminUsergroupsAddChannelsRequest(token, usergroupId, channelIds,
                teamId).execute();
    }

    /**
     * Add one or more default channels to an IDP group.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.usergroups:write`
     * @param  usergroupId  Required parameter: ID of the IDP group to add default channels for.
     * @param  channelIds  Required parameter: Comma separated string of channel IDs.
     * @param  teamId  Optional parameter: The workspace to add default channels in.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminUsergroupsAddChannelsAsync(
            final String token,
            final String usergroupId,
            final String channelIds,
            final String teamId) {
        try {
            return prepareAdminUsergroupsAddChannelsRequest(token, usergroupId, channelIds,
            teamId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminUsergroupsAddChannels.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminUsergroupsAddChannelsRequest(
            final String token,
            final String usergroupId,
            final String channelIds,
            final String teamId) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.usergroups.addChannels")
                        .formParam(param -> param.key("usergroup_id")
                                .value(usergroupId))
                        .formParam(param -> param.key("channel_ids")
                                .value(channelIds))
                        .formParam(param -> param.key("team_id")
                                .value(teamId).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response if the token provided is not associated with an Org Admin or Owner",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Associate one or more default workspaces with an organization-wide IDP group.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:write`
     * @param  usergroupId  Required parameter: An encoded usergroup (IDP Group) ID.
     * @param  teamIds  Required parameter: A comma separated list of encoded team (workspace) IDs.
     *         Each workspace *MUST* belong to the organization associated with the token.
     * @param  autoProvision  Optional parameter: When `true`, this method automatically creates new
     *         workspace accounts for the IDP group members.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminUsergroupsAddTeams(
            final String token,
            final String usergroupId,
            final String teamIds,
            final Boolean autoProvision) throws ApiException, IOException {
        return prepareAdminUsergroupsAddTeamsRequest(token, usergroupId, teamIds,
                autoProvision).execute();
    }

    /**
     * Associate one or more default workspaces with an organization-wide IDP group.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:write`
     * @param  usergroupId  Required parameter: An encoded usergroup (IDP Group) ID.
     * @param  teamIds  Required parameter: A comma separated list of encoded team (workspace) IDs.
     *         Each workspace *MUST* belong to the organization associated with the token.
     * @param  autoProvision  Optional parameter: When `true`, this method automatically creates new
     *         workspace accounts for the IDP group members.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminUsergroupsAddTeamsAsync(
            final String token,
            final String usergroupId,
            final String teamIds,
            final Boolean autoProvision) {
        try {
            return prepareAdminUsergroupsAddTeamsRequest(token, usergroupId, teamIds,
            autoProvision).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminUsergroupsAddTeams.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminUsergroupsAddTeamsRequest(
            final String token,
            final String usergroupId,
            final String teamIds,
            final Boolean autoProvision) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.usergroups.addTeams")
                        .formParam(param -> param.key("usergroup_id")
                                .value(usergroupId))
                        .formParam(param -> param.key("team_ids")
                                .value(teamIds))
                        .formParam(param -> param.key("auto_provision")
                                .value(autoProvision).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * List the channels linked to an org-level IDP group (user group).
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.usergroups:read`
     * @param  usergroupId  Required parameter: ID of the IDP group to list default channels for.
     * @param  teamId  Optional parameter: ID of the the workspace.
     * @param  includeNumMembers  Optional parameter: Flag to include or exclude the count of
     *         members per channel.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminUsergroupsListChannels(
            final String token,
            final String usergroupId,
            final String teamId,
            final Boolean includeNumMembers) throws ApiException, IOException {
        return prepareAdminUsergroupsListChannelsRequest(token, usergroupId, teamId,
                includeNumMembers).execute();
    }

    /**
     * List the channels linked to an org-level IDP group (user group).
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.usergroups:read`
     * @param  usergroupId  Required parameter: ID of the IDP group to list default channels for.
     * @param  teamId  Optional parameter: ID of the the workspace.
     * @param  includeNumMembers  Optional parameter: Flag to include or exclude the count of
     *         members per channel.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminUsergroupsListChannelsAsync(
            final String token,
            final String usergroupId,
            final String teamId,
            final Boolean includeNumMembers) {
        try {
            return prepareAdminUsergroupsListChannelsRequest(token, usergroupId, teamId,
            includeNumMembers).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminUsergroupsListChannels.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminUsergroupsListChannelsRequest(
            final String token,
            final String usergroupId,
            final String teamId,
            final Boolean includeNumMembers) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.usergroups.listChannels")
                        .queryParam(param -> param.key("usergroup_id")
                                .value(usergroupId))
                        .queryParam(param -> param.key("team_id")
                                .value(teamId).isRequired(false))
                        .queryParam(param -> param.key("include_num_members")
                                .value(includeNumMembers).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response if the token provided is not associated with an Org Admin or Owner",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Remove one or more default channels from an org-level IDP group (user group).
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.usergroups:write`
     * @param  usergroupId  Required parameter: ID of the IDP Group
     * @param  channelIds  Required parameter: Comma-separated string of channel IDs
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminUsergroupsRemoveChannels(
            final String token,
            final String usergroupId,
            final String channelIds) throws ApiException, IOException {
        return prepareAdminUsergroupsRemoveChannelsRequest(token, usergroupId,
                channelIds).execute();
    }

    /**
     * Remove one or more default channels from an org-level IDP group (user group).
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.usergroups:write`
     * @param  usergroupId  Required parameter: ID of the IDP Group
     * @param  channelIds  Required parameter: Comma-separated string of channel IDs
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminUsergroupsRemoveChannelsAsync(
            final String token,
            final String usergroupId,
            final String channelIds) {
        try {
            return prepareAdminUsergroupsRemoveChannelsRequest(token, usergroupId,
            channelIds).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminUsergroupsRemoveChannels.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminUsergroupsRemoveChannelsRequest(
            final String token,
            final String usergroupId,
            final String channelIds) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.usergroups.removeChannels")
                        .formParam(param -> param.key("usergroup_id")
                                .value(usergroupId))
                        .formParam(param -> param.key("channel_ids")
                                .value(channelIds))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response if the token provided is not associated with an Org Admin or Owner",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}