/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.DefaultErrorTemplateException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.DefaultSuccessTemplate;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class AdminEmojiController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public AdminEmojiController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Add an emoji.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:write`
     * @param  name  Required parameter: The name of the emoji to be removed. Colons (`:myemoji:`)
     *         around the value are not required, although they may be included.
     * @param  url  Required parameter: The URL of a file to use as an image for the emoji. Square
     *         images under 128KB and with transparent backgrounds work best.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminEmojiAdd(
            final String token,
            final String name,
            final String url) throws ApiException, IOException {
        return prepareAdminEmojiAddRequest(token, name, url).execute();
    }

    /**
     * Add an emoji.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:write`
     * @param  name  Required parameter: The name of the emoji to be removed. Colons (`:myemoji:`)
     *         around the value are not required, although they may be included.
     * @param  url  Required parameter: The URL of a file to use as an image for the emoji. Square
     *         images under 128KB and with transparent backgrounds work best.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminEmojiAddAsync(
            final String token,
            final String name,
            final String url) {
        try {
            return prepareAdminEmojiAddRequest(token, name, url).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminEmojiAdd.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminEmojiAddRequest(
            final String token,
            final String name,
            final String url) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.emoji.add")
                        .formParam(param -> param.key("token")
                                .value(token))
                        .formParam(param -> param.key("name")
                                .value(name))
                        .formParam(param -> param.key("url")
                                .value(url))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Add an emoji alias.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:write`
     * @param  name  Required parameter: The name of the emoji to be aliased. Colons (`:myemoji:`)
     *         around the value are not required, although they may be included.
     * @param  aliasFor  Required parameter: The alias of the emoji.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminEmojiAddAlias(
            final String token,
            final String name,
            final String aliasFor) throws ApiException, IOException {
        return prepareAdminEmojiAddAliasRequest(token, name, aliasFor).execute();
    }

    /**
     * Add an emoji alias.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:write`
     * @param  name  Required parameter: The name of the emoji to be aliased. Colons (`:myemoji:`)
     *         around the value are not required, although they may be included.
     * @param  aliasFor  Required parameter: The alias of the emoji.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminEmojiAddAliasAsync(
            final String token,
            final String name,
            final String aliasFor) {
        try {
            return prepareAdminEmojiAddAliasRequest(token, name, aliasFor).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminEmojiAddAlias.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminEmojiAddAliasRequest(
            final String token,
            final String name,
            final String aliasFor) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.emoji.addAlias")
                        .formParam(param -> param.key("token")
                                .value(token))
                        .formParam(param -> param.key("name")
                                .value(name))
                        .formParam(param -> param.key("alias_for")
                                .value(aliasFor))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * List emoji for an Enterprise Grid organization.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:read`
     * @param  cursor  Optional parameter: Set `cursor` to `next_cursor` returned by the previous
     *         call to list items in the next page
     * @param  limit  Optional parameter: The maximum number of items to return. Must be between 1 -
     *         1000 both inclusive.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminEmojiList(
            final String token,
            final String cursor,
            final Integer limit) throws ApiException, IOException {
        return prepareAdminEmojiListRequest(token, cursor, limit).execute();
    }

    /**
     * List emoji for an Enterprise Grid organization.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:read`
     * @param  cursor  Optional parameter: Set `cursor` to `next_cursor` returned by the previous
     *         call to list items in the next page
     * @param  limit  Optional parameter: The maximum number of items to return. Must be between 1 -
     *         1000 both inclusive.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminEmojiListAsync(
            final String token,
            final String cursor,
            final Integer limit) {
        try {
            return prepareAdminEmojiListRequest(token, cursor, limit).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminEmojiList.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminEmojiListRequest(
            final String token,
            final String cursor,
            final Integer limit) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.emoji.list")
                        .queryParam(param -> param.key("token")
                                .value(token))
                        .queryParam(param -> param.key("cursor")
                                .value(cursor).isRequired(false))
                        .queryParam(param -> param.key("limit")
                                .value(limit).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Remove an emoji across an Enterprise Grid organization.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:write`
     * @param  name  Required parameter: The name of the emoji to be removed. Colons (`:myemoji:`)
     *         around the value are not required, although they may be included.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminEmojiRemove(
            final String token,
            final String name) throws ApiException, IOException {
        return prepareAdminEmojiRemoveRequest(token, name).execute();
    }

    /**
     * Remove an emoji across an Enterprise Grid organization.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:write`
     * @param  name  Required parameter: The name of the emoji to be removed. Colons (`:myemoji:`)
     *         around the value are not required, although they may be included.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminEmojiRemoveAsync(
            final String token,
            final String name) {
        try {
            return prepareAdminEmojiRemoveRequest(token, name).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminEmojiRemove.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminEmojiRemoveRequest(
            final String token,
            final String name) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.emoji.remove")
                        .formParam(param -> param.key("token")
                                .value(token))
                        .formParam(param -> param.key("name")
                                .value(name))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Rename an emoji.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:write`
     * @param  name  Required parameter: The name of the emoji to be renamed. Colons (`:myemoji:`)
     *         around the value are not required, although they may be included.
     * @param  newName  Required parameter: The new name of the emoji.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminEmojiRename(
            final String token,
            final String name,
            final String newName) throws ApiException, IOException {
        return prepareAdminEmojiRenameRequest(token, name, newName).execute();
    }

    /**
     * Rename an emoji.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.teams:write`
     * @param  name  Required parameter: The name of the emoji to be renamed. Colons (`:myemoji:`)
     *         around the value are not required, although they may be included.
     * @param  newName  Required parameter: The new name of the emoji.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminEmojiRenameAsync(
            final String token,
            final String name,
            final String newName) {
        try {
            return prepareAdminEmojiRenameRequest(token, name, newName).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminEmojiRename.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminEmojiRenameRequest(
            final String token,
            final String name,
            final String newName) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.emoji.rename")
                        .formParam(param -> param.key("token")
                                .value(token))
                        .formParam(param -> param.key("name")
                                .value(name))
                        .formParam(param -> param.key("new_name")
                                .value(newName))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}