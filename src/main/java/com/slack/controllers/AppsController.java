/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.AppsUninstallErrorSchemaException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.AppsUninstallSchema;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class AppsController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public AppsController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Uninstalls your app from a workspace.
     * @param  token  Optional parameter: Authentication token. Requires scope: `none`
     * @param  clientId  Optional parameter: Issued when you created your application.
     * @param  clientSecret  Optional parameter: Issued when you created your application.
     * @return    Returns the AppsUninstallSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<AppsUninstallSchema> appsUninstall(
            final String token,
            final String clientId,
            final String clientSecret) throws ApiException, IOException {
        return prepareAppsUninstallRequest(token, clientId, clientSecret).execute();
    }

    /**
     * Uninstalls your app from a workspace.
     * @param  token  Optional parameter: Authentication token. Requires scope: `none`
     * @param  clientId  Optional parameter: Issued when you created your application.
     * @param  clientSecret  Optional parameter: Issued when you created your application.
     * @return    Returns the AppsUninstallSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<AppsUninstallSchema>> appsUninstallAsync(
            final String token,
            final String clientId,
            final String clientSecret) {
        try {
            return prepareAppsUninstallRequest(token, clientId, clientSecret).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for appsUninstall.
     */
    private ApiCall<ApiResponse<AppsUninstallSchema>, ApiException> prepareAppsUninstallRequest(
            final String token,
            final String clientId,
            final String clientSecret) {
        return new ApiCall.Builder<ApiResponse<AppsUninstallSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/apps.uninstall")
                        .queryParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .queryParam(param -> param.key("client_id")
                                .value(clientId).isRequired(false))
                        .queryParam(param -> param.key("client_secret")
                                .value(clientSecret).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, AppsUninstallSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new AppsUninstallErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}