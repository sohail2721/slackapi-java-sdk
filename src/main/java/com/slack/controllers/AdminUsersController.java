/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.DefaultErrorTemplateException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.DefaultSuccessTemplate;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class AdminUsersController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public AdminUsersController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Add an Enterprise user to a workspace.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.users:write`
     * @param  teamId  Required parameter: The ID (`T1234`) of the workspace.
     * @param  userId  Required parameter: The ID of the user to add to the workspace.
     * @param  isRestricted  Optional parameter: True if user should be added to the workspace as a
     *         guest.
     * @param  isUltraRestricted  Optional parameter: True if user should be added to the workspace
     *         as a single-channel guest.
     * @param  channelIds  Optional parameter: Comma separated values of channel IDs to add user in
     *         the new workspace.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminUsersAssign(
            final String token,
            final String teamId,
            final String userId,
            final Boolean isRestricted,
            final Boolean isUltraRestricted,
            final String channelIds) throws ApiException, IOException {
        return prepareAdminUsersAssignRequest(token, teamId, userId, isRestricted,
                isUltraRestricted, channelIds).execute();
    }

    /**
     * Add an Enterprise user to a workspace.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.users:write`
     * @param  teamId  Required parameter: The ID (`T1234`) of the workspace.
     * @param  userId  Required parameter: The ID of the user to add to the workspace.
     * @param  isRestricted  Optional parameter: True if user should be added to the workspace as a
     *         guest.
     * @param  isUltraRestricted  Optional parameter: True if user should be added to the workspace
     *         as a single-channel guest.
     * @param  channelIds  Optional parameter: Comma separated values of channel IDs to add user in
     *         the new workspace.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminUsersAssignAsync(
            final String token,
            final String teamId,
            final String userId,
            final Boolean isRestricted,
            final Boolean isUltraRestricted,
            final String channelIds) {
        try {
            return prepareAdminUsersAssignRequest(token, teamId, userId, isRestricted, isUltraRestricted,
            channelIds).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminUsersAssign.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminUsersAssignRequest(
            final String token,
            final String teamId,
            final String userId,
            final Boolean isRestricted,
            final Boolean isUltraRestricted,
            final String channelIds) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.users.assign")
                        .formParam(param -> param.key("team_id")
                                .value(teamId))
                        .formParam(param -> param.key("user_id")
                                .value(userId))
                        .formParam(param -> param.key("is_restricted")
                                .value(isRestricted).isRequired(false))
                        .formParam(param -> param.key("is_ultra_restricted")
                                .value(isUltraRestricted).isRequired(false))
                        .formParam(param -> param.key("channel_ids")
                                .value(channelIds).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Invite a user to a workspace.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.users:write`
     * @param  teamId  Required parameter: The ID (`T1234`) of the workspace.
     * @param  email  Required parameter: The email address of the person to invite.
     * @param  channelIds  Required parameter: A comma-separated list of `channel_id`s for this user
     *         to join. At least one channel is required.
     * @param  customMessage  Optional parameter: An optional message to send to the user in the
     *         invite email.
     * @param  realName  Optional parameter: Full name of the user.
     * @param  resend  Optional parameter: Allow this invite to be resent in the future if a user
     *         has not signed up yet. (default: false)
     * @param  isRestricted  Optional parameter: Is this user a multi-channel guest user? (default:
     *         false)
     * @param  isUltraRestricted  Optional parameter: Is this user a single channel guest user?
     *         (default: false)
     * @param  guestExpirationTs  Optional parameter: Timestamp when guest account should be
     *         disabled. Only include this timestamp if you are inviting a guest user and you want
     *         their account to expire on a certain date.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminUsersInvite(
            final String token,
            final String teamId,
            final String email,
            final String channelIds,
            final String customMessage,
            final String realName,
            final Boolean resend,
            final Boolean isRestricted,
            final Boolean isUltraRestricted,
            final String guestExpirationTs) throws ApiException, IOException {
        return prepareAdminUsersInviteRequest(token, teamId, email, channelIds, customMessage,
                realName, resend, isRestricted, isUltraRestricted, guestExpirationTs).execute();
    }

    /**
     * Invite a user to a workspace.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.users:write`
     * @param  teamId  Required parameter: The ID (`T1234`) of the workspace.
     * @param  email  Required parameter: The email address of the person to invite.
     * @param  channelIds  Required parameter: A comma-separated list of `channel_id`s for this user
     *         to join. At least one channel is required.
     * @param  customMessage  Optional parameter: An optional message to send to the user in the
     *         invite email.
     * @param  realName  Optional parameter: Full name of the user.
     * @param  resend  Optional parameter: Allow this invite to be resent in the future if a user
     *         has not signed up yet. (default: false)
     * @param  isRestricted  Optional parameter: Is this user a multi-channel guest user? (default:
     *         false)
     * @param  isUltraRestricted  Optional parameter: Is this user a single channel guest user?
     *         (default: false)
     * @param  guestExpirationTs  Optional parameter: Timestamp when guest account should be
     *         disabled. Only include this timestamp if you are inviting a guest user and you want
     *         their account to expire on a certain date.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminUsersInviteAsync(
            final String token,
            final String teamId,
            final String email,
            final String channelIds,
            final String customMessage,
            final String realName,
            final Boolean resend,
            final Boolean isRestricted,
            final Boolean isUltraRestricted,
            final String guestExpirationTs) {
        try {
            return prepareAdminUsersInviteRequest(token, teamId, email, channelIds, customMessage, realName,
            resend, isRestricted, isUltraRestricted, guestExpirationTs).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminUsersInvite.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminUsersInviteRequest(
            final String token,
            final String teamId,
            final String email,
            final String channelIds,
            final String customMessage,
            final String realName,
            final Boolean resend,
            final Boolean isRestricted,
            final Boolean isUltraRestricted,
            final String guestExpirationTs) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.users.invite")
                        .formParam(param -> param.key("team_id")
                                .value(teamId))
                        .formParam(param -> param.key("email")
                                .value(email))
                        .formParam(param -> param.key("channel_ids")
                                .value(channelIds))
                        .formParam(param -> param.key("custom_message")
                                .value(customMessage).isRequired(false))
                        .formParam(param -> param.key("real_name")
                                .value(realName).isRequired(false))
                        .formParam(param -> param.key("resend")
                                .value(resend).isRequired(false))
                        .formParam(param -> param.key("is_restricted")
                                .value(isRestricted).isRequired(false))
                        .formParam(param -> param.key("is_ultra_restricted")
                                .value(isUltraRestricted).isRequired(false))
                        .formParam(param -> param.key("guest_expiration_ts")
                                .value(guestExpirationTs).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * List users on a workspace.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.users:read`
     * @param  teamId  Required parameter: The ID (`T1234`) of the workspace.
     * @param  cursor  Optional parameter: Set `cursor` to `next_cursor` returned by the previous
     *         call to list items in the next page.
     * @param  limit  Optional parameter: Limit for how many users to be retrieved per page
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminUsersList(
            final String token,
            final String teamId,
            final String cursor,
            final Integer limit) throws ApiException, IOException {
        return prepareAdminUsersListRequest(token, teamId, cursor, limit).execute();
    }

    /**
     * List users on a workspace.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.users:read`
     * @param  teamId  Required parameter: The ID (`T1234`) of the workspace.
     * @param  cursor  Optional parameter: Set `cursor` to `next_cursor` returned by the previous
     *         call to list items in the next page.
     * @param  limit  Optional parameter: Limit for how many users to be retrieved per page
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminUsersListAsync(
            final String token,
            final String teamId,
            final String cursor,
            final Integer limit) {
        try {
            return prepareAdminUsersListRequest(token, teamId, cursor, limit).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminUsersList.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminUsersListRequest(
            final String token,
            final String teamId,
            final String cursor,
            final Integer limit) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.users.list")
                        .queryParam(param -> param.key("team_id")
                                .value(teamId))
                        .queryParam(param -> param.key("cursor")
                                .value(cursor).isRequired(false))
                        .queryParam(param -> param.key("limit")
                                .value(limit).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Remove a user from a workspace.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.users:write`
     * @param  teamId  Required parameter: The ID (`T1234`) of the workspace.
     * @param  userId  Required parameter: The ID of the user to remove.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminUsersRemove(
            final String token,
            final String teamId,
            final String userId) throws ApiException, IOException {
        return prepareAdminUsersRemoveRequest(token, teamId, userId).execute();
    }

    /**
     * Remove a user from a workspace.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.users:write`
     * @param  teamId  Required parameter: The ID (`T1234`) of the workspace.
     * @param  userId  Required parameter: The ID of the user to remove.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminUsersRemoveAsync(
            final String token,
            final String teamId,
            final String userId) {
        try {
            return prepareAdminUsersRemoveRequest(token, teamId, userId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminUsersRemove.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminUsersRemoveRequest(
            final String token,
            final String teamId,
            final String userId) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.users.remove")
                        .formParam(param -> param.key("team_id")
                                .value(teamId))
                        .formParam(param -> param.key("user_id")
                                .value(userId))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Set an existing guest, regular user, or owner to be an admin user.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.users:write`
     * @param  teamId  Required parameter: The ID (`T1234`) of the workspace.
     * @param  userId  Required parameter: The ID of the user to designate as an admin.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminUsersSetAdmin(
            final String token,
            final String teamId,
            final String userId) throws ApiException, IOException {
        return prepareAdminUsersSetAdminRequest(token, teamId, userId).execute();
    }

    /**
     * Set an existing guest, regular user, or owner to be an admin user.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.users:write`
     * @param  teamId  Required parameter: The ID (`T1234`) of the workspace.
     * @param  userId  Required parameter: The ID of the user to designate as an admin.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminUsersSetAdminAsync(
            final String token,
            final String teamId,
            final String userId) {
        try {
            return prepareAdminUsersSetAdminRequest(token, teamId, userId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminUsersSetAdmin.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminUsersSetAdminRequest(
            final String token,
            final String teamId,
            final String userId) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.users.setAdmin")
                        .formParam(param -> param.key("team_id")
                                .value(teamId))
                        .formParam(param -> param.key("user_id")
                                .value(userId))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Set an expiration for a guest user.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.users:write`
     * @param  teamId  Required parameter: The ID (`T1234`) of the workspace.
     * @param  userId  Required parameter: The ID of the user to set an expiration for.
     * @param  expirationTs  Required parameter: Timestamp when guest account should be disabled.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminUsersSetExpiration(
            final String token,
            final String teamId,
            final String userId,
            final int expirationTs) throws ApiException, IOException {
        return prepareAdminUsersSetExpirationRequest(token, teamId, userId, expirationTs).execute();
    }

    /**
     * Set an expiration for a guest user.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.users:write`
     * @param  teamId  Required parameter: The ID (`T1234`) of the workspace.
     * @param  userId  Required parameter: The ID of the user to set an expiration for.
     * @param  expirationTs  Required parameter: Timestamp when guest account should be disabled.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminUsersSetExpirationAsync(
            final String token,
            final String teamId,
            final String userId,
            final int expirationTs) {
        try {
            return prepareAdminUsersSetExpirationRequest(token, teamId, userId,
            expirationTs).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminUsersSetExpiration.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminUsersSetExpirationRequest(
            final String token,
            final String teamId,
            final String userId,
            final int expirationTs) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.users.setExpiration")
                        .formParam(param -> param.key("team_id")
                                .value(teamId))
                        .formParam(param -> param.key("user_id")
                                .value(userId))
                        .formParam(param -> param.key("expiration_ts")
                                .value(expirationTs).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Set an existing guest, regular user, or admin user to be a workspace owner.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.users:write`
     * @param  teamId  Required parameter: The ID (`T1234`) of the workspace.
     * @param  userId  Required parameter: Id of the user to promote to owner.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminUsersSetOwner(
            final String token,
            final String teamId,
            final String userId) throws ApiException, IOException {
        return prepareAdminUsersSetOwnerRequest(token, teamId, userId).execute();
    }

    /**
     * Set an existing guest, regular user, or admin user to be a workspace owner.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.users:write`
     * @param  teamId  Required parameter: The ID (`T1234`) of the workspace.
     * @param  userId  Required parameter: Id of the user to promote to owner.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminUsersSetOwnerAsync(
            final String token,
            final String teamId,
            final String userId) {
        try {
            return prepareAdminUsersSetOwnerRequest(token, teamId, userId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminUsersSetOwner.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminUsersSetOwnerRequest(
            final String token,
            final String teamId,
            final String userId) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.users.setOwner")
                        .formParam(param -> param.key("team_id")
                                .value(teamId))
                        .formParam(param -> param.key("user_id")
                                .value(userId))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Set an existing guest user, admin user, or owner to be a regular user.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.users:write`
     * @param  teamId  Required parameter: The ID (`T1234`) of the workspace.
     * @param  userId  Required parameter: The ID of the user to designate as a regular user.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminUsersSetRegular(
            final String token,
            final String teamId,
            final String userId) throws ApiException, IOException {
        return prepareAdminUsersSetRegularRequest(token, teamId, userId).execute();
    }

    /**
     * Set an existing guest user, admin user, or owner to be a regular user.
     * @param  token  Required parameter: Authentication token. Requires scope: `admin.users:write`
     * @param  teamId  Required parameter: The ID (`T1234`) of the workspace.
     * @param  userId  Required parameter: The ID of the user to designate as a regular user.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminUsersSetRegularAsync(
            final String token,
            final String teamId,
            final String userId) {
        try {
            return prepareAdminUsersSetRegularRequest(token, teamId, userId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminUsersSetRegular.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminUsersSetRegularRequest(
            final String token,
            final String teamId,
            final String userId) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.users.setRegular")
                        .formParam(param -> param.key("team_id")
                                .value(teamId))
                        .formParam(param -> param.key("user_id")
                                .value(userId))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}