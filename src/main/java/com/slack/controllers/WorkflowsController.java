/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.DefaultErrorTemplateException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.DefaultSuccessTemplate;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class WorkflowsController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public WorkflowsController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Indicate that an app's step in a workflow completed execution.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `workflow.steps:execute`
     * @param  workflowStepExecuteId  Required parameter: Context identifier that maps to the
     *         correct workflow step execution.
     * @param  outputs  Optional parameter: Key-value object of outputs from your step. Keys of this
     *         object reflect the configured `key` properties of your
     *         [`outputs`](/reference/workflows/workflow_step#output) array from your
     *         `workflow_step` object.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> workflowsStepCompleted(
            final String token,
            final String workflowStepExecuteId,
            final String outputs) throws ApiException, IOException {
        return prepareWorkflowsStepCompletedRequest(token, workflowStepExecuteId,
                outputs).execute();
    }

    /**
     * Indicate that an app's step in a workflow completed execution.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `workflow.steps:execute`
     * @param  workflowStepExecuteId  Required parameter: Context identifier that maps to the
     *         correct workflow step execution.
     * @param  outputs  Optional parameter: Key-value object of outputs from your step. Keys of this
     *         object reflect the configured `key` properties of your
     *         [`outputs`](/reference/workflows/workflow_step#output) array from your
     *         `workflow_step` object.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> workflowsStepCompletedAsync(
            final String token,
            final String workflowStepExecuteId,
            final String outputs) {
        try {
            return prepareWorkflowsStepCompletedRequest(token, workflowStepExecuteId,
            outputs).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for workflowsStepCompleted.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareWorkflowsStepCompletedRequest(
            final String token,
            final String workflowStepExecuteId,
            final String outputs) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/workflows.stepCompleted")
                        .queryParam(param -> param.key("workflow_step_execute_id")
                                .value(workflowStepExecuteId))
                        .queryParam(param -> param.key("outputs")
                                .value(outputs).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Indicate that an app's step in a workflow failed to execute.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `workflow.steps:execute`
     * @param  workflowStepExecuteId  Required parameter: Context identifier that maps to the
     *         correct workflow step execution.
     * @param  error  Required parameter: A JSON-based object with a `message` property that should
     *         contain a human readable error message.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> workflowsStepFailed(
            final String token,
            final String workflowStepExecuteId,
            final String error) throws ApiException, IOException {
        return prepareWorkflowsStepFailedRequest(token, workflowStepExecuteId, error).execute();
    }

    /**
     * Indicate that an app's step in a workflow failed to execute.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `workflow.steps:execute`
     * @param  workflowStepExecuteId  Required parameter: Context identifier that maps to the
     *         correct workflow step execution.
     * @param  error  Required parameter: A JSON-based object with a `message` property that should
     *         contain a human readable error message.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> workflowsStepFailedAsync(
            final String token,
            final String workflowStepExecuteId,
            final String error) {
        try {
            return prepareWorkflowsStepFailedRequest(token, workflowStepExecuteId, error).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for workflowsStepFailed.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareWorkflowsStepFailedRequest(
            final String token,
            final String workflowStepExecuteId,
            final String error) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/workflows.stepFailed")
                        .queryParam(param -> param.key("workflow_step_execute_id")
                                .value(workflowStepExecuteId))
                        .queryParam(param -> param.key("error")
                                .value(error))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Update the configuration for a workflow extension step.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `workflow.steps:execute`
     * @param  workflowStepEditId  Required parameter: A context identifier provided with
     *         `view_submission` payloads used to call back to `workflows.updateStep`.
     * @param  inputs  Optional parameter: A JSON key-value map of inputs required from a user
     *         during configuration. This is the data your app expects to receive when the workflow
     *         step starts. **Please note**: the embedded variable format is set and replaced by the
     *         workflow system. You cannot create custom variables that will be replaced at runtime.
     *         [Read more about variables in workflow steps here](/workflows/steps#variables).
     * @param  outputs  Optional parameter: An JSON array of output objects used during step
     *         execution. This is the data your app agrees to provide when your workflow step was
     *         executed.
     * @param  stepName  Optional parameter: An optional field that can be used to override the step
     *         name that is shown in the Workflow Builder.
     * @param  stepImageUrl  Optional parameter: An optional field that can be used to override app
     *         image that is shown in the Workflow Builder.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> workflowsUpdateStep(
            final String token,
            final String workflowStepEditId,
            final String inputs,
            final String outputs,
            final String stepName,
            final String stepImageUrl) throws ApiException, IOException {
        return prepareWorkflowsUpdateStepRequest(token, workflowStepEditId, inputs, outputs,
                stepName, stepImageUrl).execute();
    }

    /**
     * Update the configuration for a workflow extension step.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `workflow.steps:execute`
     * @param  workflowStepEditId  Required parameter: A context identifier provided with
     *         `view_submission` payloads used to call back to `workflows.updateStep`.
     * @param  inputs  Optional parameter: A JSON key-value map of inputs required from a user
     *         during configuration. This is the data your app expects to receive when the workflow
     *         step starts. **Please note**: the embedded variable format is set and replaced by the
     *         workflow system. You cannot create custom variables that will be replaced at runtime.
     *         [Read more about variables in workflow steps here](/workflows/steps#variables).
     * @param  outputs  Optional parameter: An JSON array of output objects used during step
     *         execution. This is the data your app agrees to provide when your workflow step was
     *         executed.
     * @param  stepName  Optional parameter: An optional field that can be used to override the step
     *         name that is shown in the Workflow Builder.
     * @param  stepImageUrl  Optional parameter: An optional field that can be used to override app
     *         image that is shown in the Workflow Builder.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> workflowsUpdateStepAsync(
            final String token,
            final String workflowStepEditId,
            final String inputs,
            final String outputs,
            final String stepName,
            final String stepImageUrl) {
        try {
            return prepareWorkflowsUpdateStepRequest(token, workflowStepEditId, inputs, outputs, stepName,
            stepImageUrl).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for workflowsUpdateStep.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareWorkflowsUpdateStepRequest(
            final String token,
            final String workflowStepEditId,
            final String inputs,
            final String outputs,
            final String stepName,
            final String stepImageUrl) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/workflows.updateStep")
                        .queryParam(param -> param.key("workflow_step_edit_id")
                                .value(workflowStepEditId))
                        .queryParam(param -> param.key("inputs")
                                .value(inputs).isRequired(false))
                        .queryParam(param -> param.key("outputs")
                                .value(outputs).isRequired(false))
                        .queryParam(param -> param.key("step_name")
                                .value(stepName).isRequired(false))
                        .queryParam(param -> param.key("step_image_url")
                                .value(stepImageUrl).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}