/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.PinsAddErrorSchemaException;
import com.slack.exceptions.PinsListErrorSchemaException;
import com.slack.exceptions.PinsRemoveErrorSchemaException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.DynamicResponse;
import com.slack.models.PinsAddSchema;
import com.slack.models.PinsRemoveSchema;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class PinsController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public PinsController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Pins an item to a channel.
     * @param  token  Required parameter: Authentication token. Requires scope: `pins:write`
     * @param  channel  Required parameter: Channel to pin the item in.
     * @param  timestamp  Optional parameter: Timestamp of the message to pin.
     * @return    Returns the PinsAddSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<PinsAddSchema> pinsAdd(
            final String token,
            final String channel,
            final String timestamp) throws ApiException, IOException {
        return preparePinsAddRequest(token, channel, timestamp).execute();
    }

    /**
     * Pins an item to a channel.
     * @param  token  Required parameter: Authentication token. Requires scope: `pins:write`
     * @param  channel  Required parameter: Channel to pin the item in.
     * @param  timestamp  Optional parameter: Timestamp of the message to pin.
     * @return    Returns the PinsAddSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<PinsAddSchema>> pinsAddAsync(
            final String token,
            final String channel,
            final String timestamp) {
        try {
            return preparePinsAddRequest(token, channel, timestamp).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for pinsAdd.
     */
    private ApiCall<ApiResponse<PinsAddSchema>, ApiException> preparePinsAddRequest(
            final String token,
            final String channel,
            final String timestamp) {
        return new ApiCall.Builder<ApiResponse<PinsAddSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/pins.add")
                        .formParam(param -> param.key("channel")
                                .value(channel))
                        .formParam(param -> param.key("timestamp")
                                .value(timestamp).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, PinsAddSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new PinsAddErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Lists items pinned to a channel.
     * @param  token  Required parameter: Authentication token. Requires scope: `pins:read`
     * @param  channel  Required parameter: Channel to get pinned items for.
     * @return    Returns the DynamicResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DynamicResponse> pinsList(
            final String token,
            final String channel) throws ApiException, IOException {
        return preparePinsListRequest(token, channel).execute();
    }

    /**
     * Lists items pinned to a channel.
     * @param  token  Required parameter: Authentication token. Requires scope: `pins:read`
     * @param  channel  Required parameter: Channel to get pinned items for.
     * @return    Returns the DynamicResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DynamicResponse>> pinsListAsync(
            final String token,
            final String channel) {
        try {
            return preparePinsListRequest(token, channel).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for pinsList.
     */
    private ApiCall<ApiResponse<DynamicResponse>, ApiException> preparePinsListRequest(
            final String token,
            final String channel) {
        return new ApiCall.Builder<ApiResponse<DynamicResponse>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/pins.list")
                        .queryParam(param -> param.key("token")
                                .value(token))
                        .queryParam(param -> param.key("channel")
                                .value(channel))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.DYNAMIC_API_RESPONSE)
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new PinsListErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Un-pins an item from a channel.
     * @param  token  Required parameter: Authentication token. Requires scope: `pins:write`
     * @param  channel  Required parameter: Channel where the item is pinned to.
     * @param  timestamp  Optional parameter: Timestamp of the message to un-pin.
     * @return    Returns the PinsRemoveSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<PinsRemoveSchema> pinsRemove(
            final String token,
            final String channel,
            final String timestamp) throws ApiException, IOException {
        return preparePinsRemoveRequest(token, channel, timestamp).execute();
    }

    /**
     * Un-pins an item from a channel.
     * @param  token  Required parameter: Authentication token. Requires scope: `pins:write`
     * @param  channel  Required parameter: Channel where the item is pinned to.
     * @param  timestamp  Optional parameter: Timestamp of the message to un-pin.
     * @return    Returns the PinsRemoveSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<PinsRemoveSchema>> pinsRemoveAsync(
            final String token,
            final String channel,
            final String timestamp) {
        try {
            return preparePinsRemoveRequest(token, channel, timestamp).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for pinsRemove.
     */
    private ApiCall<ApiResponse<PinsRemoveSchema>, ApiException> preparePinsRemoveRequest(
            final String token,
            final String channel,
            final String timestamp) {
        return new ApiCall.Builder<ApiResponse<PinsRemoveSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/pins.remove")
                        .formParam(param -> param.key("channel")
                                .value(channel))
                        .formParam(param -> param.key("timestamp")
                                .value(timestamp).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, PinsRemoveSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new PinsRemoveErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}