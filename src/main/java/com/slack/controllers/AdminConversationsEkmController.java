/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.DefaultErrorTemplateException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.DefaultSuccessTemplate;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class AdminConversationsEkmController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public AdminConversationsEkmController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * List all disconnected channels—i.e., channels that were once connected to other workspaces
     * and then disconnected—and the corresponding original channel IDs for key revocation with EKM.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:read`
     * @param  channelIds  Optional parameter: A comma-separated list of channels to filter to.
     * @param  teamIds  Optional parameter: A comma-separated list of the workspaces to which the
     *         channels you would like returned belong.
     * @param  limit  Optional parameter: The maximum number of items to return. Must be between 1 -
     *         1000 both inclusive.
     * @param  cursor  Optional parameter: Set `cursor` to `next_cursor` returned by the previous
     *         call to list items in the next page.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> adminConversationsEkmListOriginalConnectedChannelInfo(
            final String token,
            final String channelIds,
            final String teamIds,
            final Integer limit,
            final String cursor) throws ApiException, IOException {
        return prepareAdminConversationsEkmListOriginalConnectedChannelInfoRequest(token,
                channelIds, teamIds, limit, cursor).execute();
    }

    /**
     * List all disconnected channels—i.e., channels that were once connected to other workspaces
     * and then disconnected—and the corresponding original channel IDs for key revocation with EKM.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `admin.conversations:read`
     * @param  channelIds  Optional parameter: A comma-separated list of channels to filter to.
     * @param  teamIds  Optional parameter: A comma-separated list of the workspaces to which the
     *         channels you would like returned belong.
     * @param  limit  Optional parameter: The maximum number of items to return. Must be between 1 -
     *         1000 both inclusive.
     * @param  cursor  Optional parameter: Set `cursor` to `next_cursor` returned by the previous
     *         call to list items in the next page.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> adminConversationsEkmListOriginalConnectedChannelInfoAsync(
            final String token,
            final String channelIds,
            final String teamIds,
            final Integer limit,
            final String cursor) {
        try {
            return prepareAdminConversationsEkmListOriginalConnectedChannelInfoRequest(token, channelIds,
            teamIds, limit, cursor).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for adminConversationsEkmListOriginalConnectedChannelInfo.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareAdminConversationsEkmListOriginalConnectedChannelInfoRequest(
            final String token,
            final String channelIds,
            final String teamIds,
            final Integer limit,
            final String cursor) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/admin.conversations.ekm.listOriginalConnectedChannelInfo")
                        .queryParam(param -> param.key("token")
                                .value(token))
                        .queryParam(param -> param.key("channel_ids")
                                .value(channelIds).isRequired(false))
                        .queryParam(param -> param.key("team_ids")
                                .value(teamIds).isRequired(false))
                        .queryParam(param -> param.key("limit")
                                .value(limit).isRequired(false))
                        .queryParam(param -> param.key("cursor")
                                .value(cursor).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}