/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.StarsAddErrorSchemaException;
import com.slack.exceptions.StarsListErrorSchemaException;
import com.slack.exceptions.StarsRemoveErrorSchemaException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.StarsAddSchema;
import com.slack.models.StarsListSchema;
import com.slack.models.StarsRemoveSchema;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class StarsController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public StarsController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Adds a star to an item.
     * @param  token  Required parameter: Authentication token. Requires scope: `stars:write`
     * @param  channel  Optional parameter: Channel to add star to, or channel where the message to
     *         add star to was posted (used with `timestamp`).
     * @param  file  Optional parameter: File to add star to.
     * @param  fileComment  Optional parameter: File comment to add star to.
     * @param  timestamp  Optional parameter: Timestamp of the message to add star to.
     * @return    Returns the StarsAddSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<StarsAddSchema> starsAdd(
            final String token,
            final String channel,
            final String file,
            final String fileComment,
            final String timestamp) throws ApiException, IOException {
        return prepareStarsAddRequest(token, channel, file, fileComment, timestamp).execute();
    }

    /**
     * Adds a star to an item.
     * @param  token  Required parameter: Authentication token. Requires scope: `stars:write`
     * @param  channel  Optional parameter: Channel to add star to, or channel where the message to
     *         add star to was posted (used with `timestamp`).
     * @param  file  Optional parameter: File to add star to.
     * @param  fileComment  Optional parameter: File comment to add star to.
     * @param  timestamp  Optional parameter: Timestamp of the message to add star to.
     * @return    Returns the StarsAddSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<StarsAddSchema>> starsAddAsync(
            final String token,
            final String channel,
            final String file,
            final String fileComment,
            final String timestamp) {
        try {
            return prepareStarsAddRequest(token, channel, file, fileComment, timestamp).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for starsAdd.
     */
    private ApiCall<ApiResponse<StarsAddSchema>, ApiException> prepareStarsAddRequest(
            final String token,
            final String channel,
            final String file,
            final String fileComment,
            final String timestamp) {
        return new ApiCall.Builder<ApiResponse<StarsAddSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/stars.add")
                        .formParam(param -> param.key("channel")
                                .value(channel).isRequired(false))
                        .formParam(param -> param.key("file")
                                .value(file).isRequired(false))
                        .formParam(param -> param.key("file_comment")
                                .value(fileComment).isRequired(false))
                        .formParam(param -> param.key("timestamp")
                                .value(timestamp).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, StarsAddSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new StarsAddErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Lists stars for a user.
     * @param  token  Optional parameter: Authentication token. Requires scope: `stars:read`
     * @param  count  Optional parameter:
     * @param  page  Optional parameter:
     * @param  cursor  Optional parameter: Parameter for pagination. Set `cursor` equal to the
     *         `next_cursor` attribute returned by the previous request's `response_metadata`. This
     *         parameter is optional, but pagination is mandatory: the default value simply fetches
     *         the first "page" of the collection. See [pagination](/docs/pagination) for more
     *         details.
     * @param  limit  Optional parameter: The maximum number of items to return. Fewer than the
     *         requested number of items may be returned, even if the end of the list hasn't been
     *         reached.
     * @return    Returns the StarsListSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<StarsListSchema> starsList(
            final String token,
            final String count,
            final String page,
            final String cursor,
            final Integer limit) throws ApiException, IOException {
        return prepareStarsListRequest(token, count, page, cursor, limit).execute();
    }

    /**
     * Lists stars for a user.
     * @param  token  Optional parameter: Authentication token. Requires scope: `stars:read`
     * @param  count  Optional parameter:
     * @param  page  Optional parameter:
     * @param  cursor  Optional parameter: Parameter for pagination. Set `cursor` equal to the
     *         `next_cursor` attribute returned by the previous request's `response_metadata`. This
     *         parameter is optional, but pagination is mandatory: the default value simply fetches
     *         the first "page" of the collection. See [pagination](/docs/pagination) for more
     *         details.
     * @param  limit  Optional parameter: The maximum number of items to return. Fewer than the
     *         requested number of items may be returned, even if the end of the list hasn't been
     *         reached.
     * @return    Returns the StarsListSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<StarsListSchema>> starsListAsync(
            final String token,
            final String count,
            final String page,
            final String cursor,
            final Integer limit) {
        try {
            return prepareStarsListRequest(token, count, page, cursor, limit).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for starsList.
     */
    private ApiCall<ApiResponse<StarsListSchema>, ApiException> prepareStarsListRequest(
            final String token,
            final String count,
            final String page,
            final String cursor,
            final Integer limit) {
        return new ApiCall.Builder<ApiResponse<StarsListSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/stars.list")
                        .queryParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .queryParam(param -> param.key("count")
                                .value(count).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .queryParam(param -> param.key("cursor")
                                .value(cursor).isRequired(false))
                        .queryParam(param -> param.key("limit")
                                .value(limit).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, StarsListSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new StarsListErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Removes a star from an item.
     * @param  token  Required parameter: Authentication token. Requires scope: `stars:write`
     * @param  channel  Optional parameter: Channel to remove star from, or channel where the
     *         message to remove star from was posted (used with `timestamp`).
     * @param  file  Optional parameter: File to remove star from.
     * @param  fileComment  Optional parameter: File comment to remove star from.
     * @param  timestamp  Optional parameter: Timestamp of the message to remove star from.
     * @return    Returns the StarsRemoveSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<StarsRemoveSchema> starsRemove(
            final String token,
            final String channel,
            final String file,
            final String fileComment,
            final String timestamp) throws ApiException, IOException {
        return prepareStarsRemoveRequest(token, channel, file, fileComment, timestamp).execute();
    }

    /**
     * Removes a star from an item.
     * @param  token  Required parameter: Authentication token. Requires scope: `stars:write`
     * @param  channel  Optional parameter: Channel to remove star from, or channel where the
     *         message to remove star from was posted (used with `timestamp`).
     * @param  file  Optional parameter: File to remove star from.
     * @param  fileComment  Optional parameter: File comment to remove star from.
     * @param  timestamp  Optional parameter: Timestamp of the message to remove star from.
     * @return    Returns the StarsRemoveSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<StarsRemoveSchema>> starsRemoveAsync(
            final String token,
            final String channel,
            final String file,
            final String fileComment,
            final String timestamp) {
        try {
            return prepareStarsRemoveRequest(token, channel, file, fileComment, timestamp).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for starsRemove.
     */
    private ApiCall<ApiResponse<StarsRemoveSchema>, ApiException> prepareStarsRemoveRequest(
            final String token,
            final String channel,
            final String file,
            final String fileComment,
            final String timestamp) {
        return new ApiCall.Builder<ApiResponse<StarsRemoveSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/stars.remove")
                        .formParam(param -> param.key("channel")
                                .value(channel).isRequired(false))
                        .formParam(param -> param.key("file")
                                .value(file).isRequired(false))
                        .formParam(param -> param.key("file_comment")
                                .value(fileComment).isRequired(false))
                        .formParam(param -> param.key("timestamp")
                                .value(timestamp).isRequired(false))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, StarsRemoveSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new StarsRemoveErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}