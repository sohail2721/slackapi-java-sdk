/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.DefaultErrorTemplateException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.DefaultSuccessTemplate;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class FilesRemoteController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public FilesRemoteController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Adds a file from a remote service.
     * @param  token  Optional parameter: Authentication token. Requires scope: `remote_files:write`
     * @param  externalId  Optional parameter: Creator defined GUID for the file.
     * @param  title  Optional parameter: Title of the file being shared.
     * @param  filetype  Optional parameter: type of file
     * @param  externalUrl  Optional parameter: URL of the remote file.
     * @param  previewImage  Optional parameter: Preview of the document via `multipart/form-data`.
     * @param  indexableFileContents  Optional parameter: A text file (txt, pdf, doc, etc.)
     *         containing textual search terms that are used to improve discovery of the remote
     *         file.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> filesRemoteAdd(
            final String token,
            final String externalId,
            final String title,
            final String filetype,
            final String externalUrl,
            final String previewImage,
            final String indexableFileContents) throws ApiException, IOException {
        return prepareFilesRemoteAddRequest(token, externalId, title, filetype, externalUrl,
                previewImage, indexableFileContents).execute();
    }

    /**
     * Adds a file from a remote service.
     * @param  token  Optional parameter: Authentication token. Requires scope: `remote_files:write`
     * @param  externalId  Optional parameter: Creator defined GUID for the file.
     * @param  title  Optional parameter: Title of the file being shared.
     * @param  filetype  Optional parameter: type of file
     * @param  externalUrl  Optional parameter: URL of the remote file.
     * @param  previewImage  Optional parameter: Preview of the document via `multipart/form-data`.
     * @param  indexableFileContents  Optional parameter: A text file (txt, pdf, doc, etc.)
     *         containing textual search terms that are used to improve discovery of the remote
     *         file.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> filesRemoteAddAsync(
            final String token,
            final String externalId,
            final String title,
            final String filetype,
            final String externalUrl,
            final String previewImage,
            final String indexableFileContents) {
        try {
            return prepareFilesRemoteAddRequest(token, externalId, title, filetype, externalUrl,
            previewImage, indexableFileContents).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for filesRemoteAdd.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareFilesRemoteAddRequest(
            final String token,
            final String externalId,
            final String title,
            final String filetype,
            final String externalUrl,
            final String previewImage,
            final String indexableFileContents) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/files.remote.add")
                        .formParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .formParam(param -> param.key("external_id")
                                .value(externalId).isRequired(false))
                        .formParam(param -> param.key("title")
                                .value(title).isRequired(false))
                        .formParam(param -> param.key("filetype")
                                .value(filetype).isRequired(false))
                        .formParam(param -> param.key("external_url")
                                .value(externalUrl).isRequired(false))
                        .formParam(param -> param.key("preview_image")
                                .value(previewImage).isRequired(false))
                        .formParam(param -> param.key("indexable_file_contents")
                                .value(indexableFileContents).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Retrieve information about a remote file added to Slack.
     * @param  token  Optional parameter: Authentication token. Requires scope: `remote_files:read`
     * @param  file  Optional parameter: Specify a file by providing its ID.
     * @param  externalId  Optional parameter: Creator defined GUID for the file.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> filesRemoteInfo(
            final String token,
            final String file,
            final String externalId) throws ApiException, IOException {
        return prepareFilesRemoteInfoRequest(token, file, externalId).execute();
    }

    /**
     * Retrieve information about a remote file added to Slack.
     * @param  token  Optional parameter: Authentication token. Requires scope: `remote_files:read`
     * @param  file  Optional parameter: Specify a file by providing its ID.
     * @param  externalId  Optional parameter: Creator defined GUID for the file.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> filesRemoteInfoAsync(
            final String token,
            final String file,
            final String externalId) {
        try {
            return prepareFilesRemoteInfoRequest(token, file, externalId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for filesRemoteInfo.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareFilesRemoteInfoRequest(
            final String token,
            final String file,
            final String externalId) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/files.remote.info")
                        .queryParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .queryParam(param -> param.key("file")
                                .value(file).isRequired(false))
                        .queryParam(param -> param.key("external_id")
                                .value(externalId).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Retrieve information about a remote file added to Slack.
     * @param  token  Optional parameter: Authentication token. Requires scope: `remote_files:read`
     * @param  channel  Optional parameter: Filter files appearing in a specific channel, indicated
     *         by its ID.
     * @param  tsFrom  Optional parameter: Filter files created after this timestamp (inclusive).
     * @param  tsTo  Optional parameter: Filter files created before this timestamp (inclusive).
     * @param  limit  Optional parameter: The maximum number of items to return.
     * @param  cursor  Optional parameter: Paginate through collections of data by setting the
     *         `cursor` parameter to a `next_cursor` attribute returned by a previous request's
     *         `response_metadata`. Default value fetches the first "page" of the collection. See
     *         [pagination](/docs/pagination) for more detail.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> filesRemoteList(
            final String token,
            final String channel,
            final Double tsFrom,
            final Double tsTo,
            final Integer limit,
            final String cursor) throws ApiException, IOException {
        return prepareFilesRemoteListRequest(token, channel, tsFrom, tsTo, limit, cursor).execute();
    }

    /**
     * Retrieve information about a remote file added to Slack.
     * @param  token  Optional parameter: Authentication token. Requires scope: `remote_files:read`
     * @param  channel  Optional parameter: Filter files appearing in a specific channel, indicated
     *         by its ID.
     * @param  tsFrom  Optional parameter: Filter files created after this timestamp (inclusive).
     * @param  tsTo  Optional parameter: Filter files created before this timestamp (inclusive).
     * @param  limit  Optional parameter: The maximum number of items to return.
     * @param  cursor  Optional parameter: Paginate through collections of data by setting the
     *         `cursor` parameter to a `next_cursor` attribute returned by a previous request's
     *         `response_metadata`. Default value fetches the first "page" of the collection. See
     *         [pagination](/docs/pagination) for more detail.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> filesRemoteListAsync(
            final String token,
            final String channel,
            final Double tsFrom,
            final Double tsTo,
            final Integer limit,
            final String cursor) {
        try {
            return prepareFilesRemoteListRequest(token, channel, tsFrom, tsTo, limit,
            cursor).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for filesRemoteList.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareFilesRemoteListRequest(
            final String token,
            final String channel,
            final Double tsFrom,
            final Double tsTo,
            final Integer limit,
            final String cursor) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/files.remote.list")
                        .queryParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .queryParam(param -> param.key("channel")
                                .value(channel).isRequired(false))
                        .queryParam(param -> param.key("ts_from")
                                .value(tsFrom).isRequired(false))
                        .queryParam(param -> param.key("ts_to")
                                .value(tsTo).isRequired(false))
                        .queryParam(param -> param.key("limit")
                                .value(limit).isRequired(false))
                        .queryParam(param -> param.key("cursor")
                                .value(cursor).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Remove a remote file.
     * @param  token  Optional parameter: Authentication token. Requires scope: `remote_files:write`
     * @param  file  Optional parameter: Specify a file by providing its ID.
     * @param  externalId  Optional parameter: Creator defined GUID for the file.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> filesRemoteRemove(
            final String token,
            final String file,
            final String externalId) throws ApiException, IOException {
        return prepareFilesRemoteRemoveRequest(token, file, externalId).execute();
    }

    /**
     * Remove a remote file.
     * @param  token  Optional parameter: Authentication token. Requires scope: `remote_files:write`
     * @param  file  Optional parameter: Specify a file by providing its ID.
     * @param  externalId  Optional parameter: Creator defined GUID for the file.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> filesRemoteRemoveAsync(
            final String token,
            final String file,
            final String externalId) {
        try {
            return prepareFilesRemoteRemoveRequest(token, file, externalId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for filesRemoteRemove.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareFilesRemoteRemoveRequest(
            final String token,
            final String file,
            final String externalId) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/files.remote.remove")
                        .formParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .formParam(param -> param.key("file")
                                .value(file).isRequired(false))
                        .formParam(param -> param.key("external_id")
                                .value(externalId).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Share a remote file into a channel.
     * @param  token  Optional parameter: Authentication token. Requires scope: `remote_files:share`
     * @param  file  Optional parameter: Specify a file registered with Slack by providing its ID.
     *         Either this field or `external_id` or both are required.
     * @param  externalId  Optional parameter: The globally unique identifier (GUID) for the file,
     *         as set by the app registering the file with Slack. Either this field or `file` or
     *         both are required.
     * @param  channels  Optional parameter: Comma-separated list of channel IDs where the file will
     *         be shared.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> filesRemoteShare(
            final String token,
            final String file,
            final String externalId,
            final String channels) throws ApiException, IOException {
        return prepareFilesRemoteShareRequest(token, file, externalId, channels).execute();
    }

    /**
     * Share a remote file into a channel.
     * @param  token  Optional parameter: Authentication token. Requires scope: `remote_files:share`
     * @param  file  Optional parameter: Specify a file registered with Slack by providing its ID.
     *         Either this field or `external_id` or both are required.
     * @param  externalId  Optional parameter: The globally unique identifier (GUID) for the file,
     *         as set by the app registering the file with Slack. Either this field or `file` or
     *         both are required.
     * @param  channels  Optional parameter: Comma-separated list of channel IDs where the file will
     *         be shared.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> filesRemoteShareAsync(
            final String token,
            final String file,
            final String externalId,
            final String channels) {
        try {
            return prepareFilesRemoteShareRequest(token, file, externalId, channels).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for filesRemoteShare.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareFilesRemoteShareRequest(
            final String token,
            final String file,
            final String externalId,
            final String channels) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/files.remote.share")
                        .queryParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .queryParam(param -> param.key("file")
                                .value(file).isRequired(false))
                        .queryParam(param -> param.key("external_id")
                                .value(externalId).isRequired(false))
                        .queryParam(param -> param.key("channels")
                                .value(channels).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Updates an existing remote file.
     * @param  token  Optional parameter: Authentication token. Requires scope: `remote_files:write`
     * @param  file  Optional parameter: Specify a file by providing its ID.
     * @param  externalId  Optional parameter: Creator defined GUID for the file.
     * @param  title  Optional parameter: Title of the file being shared.
     * @param  filetype  Optional parameter: type of file
     * @param  externalUrl  Optional parameter: URL of the remote file.
     * @param  previewImage  Optional parameter: Preview of the document via `multipart/form-data`.
     * @param  indexableFileContents  Optional parameter: File containing contents that can be used
     *         to improve searchability for the remote file.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> filesRemoteUpdate(
            final String token,
            final String file,
            final String externalId,
            final String title,
            final String filetype,
            final String externalUrl,
            final String previewImage,
            final String indexableFileContents) throws ApiException, IOException {
        return prepareFilesRemoteUpdateRequest(token, file, externalId, title, filetype,
                externalUrl, previewImage, indexableFileContents).execute();
    }

    /**
     * Updates an existing remote file.
     * @param  token  Optional parameter: Authentication token. Requires scope: `remote_files:write`
     * @param  file  Optional parameter: Specify a file by providing its ID.
     * @param  externalId  Optional parameter: Creator defined GUID for the file.
     * @param  title  Optional parameter: Title of the file being shared.
     * @param  filetype  Optional parameter: type of file
     * @param  externalUrl  Optional parameter: URL of the remote file.
     * @param  previewImage  Optional parameter: Preview of the document via `multipart/form-data`.
     * @param  indexableFileContents  Optional parameter: File containing contents that can be used
     *         to improve searchability for the remote file.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> filesRemoteUpdateAsync(
            final String token,
            final String file,
            final String externalId,
            final String title,
            final String filetype,
            final String externalUrl,
            final String previewImage,
            final String indexableFileContents) {
        try {
            return prepareFilesRemoteUpdateRequest(token, file, externalId, title, filetype, externalUrl,
            previewImage, indexableFileContents).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for filesRemoteUpdate.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareFilesRemoteUpdateRequest(
            final String token,
            final String file,
            final String externalId,
            final String title,
            final String filetype,
            final String externalUrl,
            final String previewImage,
            final String indexableFileContents) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/files.remote.update")
                        .formParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .formParam(param -> param.key("file")
                                .value(file).isRequired(false))
                        .formParam(param -> param.key("external_id")
                                .value(externalId).isRequired(false))
                        .formParam(param -> param.key("title")
                                .value(title).isRequired(false))
                        .formParam(param -> param.key("filetype")
                                .value(filetype).isRequired(false))
                        .formParam(param -> param.key("external_url")
                                .value(externalUrl).isRequired(false))
                        .formParam(param -> param.key("preview_image")
                                .value(previewImage).isRequired(false))
                        .formParam(param -> param.key("indexable_file_contents")
                                .value(indexableFileContents).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}