/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.UsersConversationsErrorSchemaException;
import com.slack.exceptions.UsersCountsErrorSchemaException;
import com.slack.exceptions.UsersDeletePhotoErrorSchemaException;
import com.slack.exceptions.UsersIdentityErrorSchemaException;
import com.slack.exceptions.UsersInfoErrorSchemaException;
import com.slack.exceptions.UsersListErrorSchemaException;
import com.slack.exceptions.UsersLookupByEmailErrorSchemaException;
import com.slack.exceptions.UsersSetActiveErrorSchemaException;
import com.slack.exceptions.UsersSetPhotoErrorSchemaException;
import com.slack.exceptions.UsersSetPresenceErrorSchemaException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.ApiMethodUsersGetPresence;
import com.slack.models.DynamicResponse;
import com.slack.models.UsersConversationsSuccessSchema;
import com.slack.models.UsersDeletePhotoSchema;
import com.slack.models.UsersInfoSuccessSchema;
import com.slack.models.UsersListSchema;
import com.slack.models.UsersLookupByEmailSuccessSchema;
import com.slack.models.UsersSetActiveSchema;
import com.slack.models.UsersSetPhotoSchema;
import com.slack.models.UsersSetPresenceSchema;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class UsersController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public UsersController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * List conversations the calling user may access.
     * @param  token  Optional parameter: Authentication token. Requires scope: `conversations:read`
     * @param  user  Optional parameter: Browse conversations by a specific user ID's membership.
     *         Non-public channels are restricted to those where the calling user shares membership.
     * @param  types  Optional parameter: Mix and match channel types by providing a comma-separated
     *         list of any combination of `public_channel`, `private_channel`, `mpim`, `im`
     * @param  excludeArchived  Optional parameter: Set to `true` to exclude archived channels from
     *         the list
     * @param  limit  Optional parameter: The maximum number of items to return. Fewer than the
     *         requested number of items may be returned, even if the end of the list hasn't been
     *         reached. Must be an integer no larger than 1000.
     * @param  cursor  Optional parameter: Paginate through collections of data by setting the
     *         `cursor` parameter to a `next_cursor` attribute returned by a previous request's
     *         `response_metadata`. Default value fetches the first "page" of the collection. See
     *         [pagination](/docs/pagination) for more detail.
     * @return    Returns the UsersConversationsSuccessSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<UsersConversationsSuccessSchema> usersConversations(
            final String token,
            final String user,
            final String types,
            final Boolean excludeArchived,
            final Integer limit,
            final String cursor) throws ApiException, IOException {
        return prepareUsersConversationsRequest(token, user, types, excludeArchived, limit,
                cursor).execute();
    }

    /**
     * List conversations the calling user may access.
     * @param  token  Optional parameter: Authentication token. Requires scope: `conversations:read`
     * @param  user  Optional parameter: Browse conversations by a specific user ID's membership.
     *         Non-public channels are restricted to those where the calling user shares membership.
     * @param  types  Optional parameter: Mix and match channel types by providing a comma-separated
     *         list of any combination of `public_channel`, `private_channel`, `mpim`, `im`
     * @param  excludeArchived  Optional parameter: Set to `true` to exclude archived channels from
     *         the list
     * @param  limit  Optional parameter: The maximum number of items to return. Fewer than the
     *         requested number of items may be returned, even if the end of the list hasn't been
     *         reached. Must be an integer no larger than 1000.
     * @param  cursor  Optional parameter: Paginate through collections of data by setting the
     *         `cursor` parameter to a `next_cursor` attribute returned by a previous request's
     *         `response_metadata`. Default value fetches the first "page" of the collection. See
     *         [pagination](/docs/pagination) for more detail.
     * @return    Returns the UsersConversationsSuccessSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<UsersConversationsSuccessSchema>> usersConversationsAsync(
            final String token,
            final String user,
            final String types,
            final Boolean excludeArchived,
            final Integer limit,
            final String cursor) {
        try {
            return prepareUsersConversationsRequest(token, user, types, excludeArchived, limit,
            cursor).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for usersConversations.
     */
    private ApiCall<ApiResponse<UsersConversationsSuccessSchema>, ApiException> prepareUsersConversationsRequest(
            final String token,
            final String user,
            final String types,
            final Boolean excludeArchived,
            final Integer limit,
            final String cursor) {
        return new ApiCall.Builder<ApiResponse<UsersConversationsSuccessSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/users.conversations")
                        .queryParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .queryParam(param -> param.key("user")
                                .value(user).isRequired(false))
                        .queryParam(param -> param.key("types")
                                .value(types).isRequired(false))
                        .queryParam(param -> param.key("exclude_archived")
                                .value(excludeArchived).isRequired(false))
                        .queryParam(param -> param.key("limit")
                                .value(limit).isRequired(false))
                        .queryParam(param -> param.key("cursor")
                                .value(cursor).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, UsersConversationsSuccessSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new UsersConversationsErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Delete the user profile photo.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `users.profile:write`
     * @return    Returns the UsersDeletePhotoSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<UsersDeletePhotoSchema> usersDeletePhoto(
            final String token) throws ApiException, IOException {
        return prepareUsersDeletePhotoRequest(token).execute();
    }

    /**
     * Delete the user profile photo.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `users.profile:write`
     * @return    Returns the UsersDeletePhotoSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<UsersDeletePhotoSchema>> usersDeletePhotoAsync(
            final String token) {
        try {
            return prepareUsersDeletePhotoRequest(token).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for usersDeletePhoto.
     */
    private ApiCall<ApiResponse<UsersDeletePhotoSchema>, ApiException> prepareUsersDeletePhotoRequest(
            final String token) {
        return new ApiCall.Builder<ApiResponse<UsersDeletePhotoSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/users.deletePhoto")
                        .formParam(param -> param.key("token")
                                .value(token))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, UsersDeletePhotoSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new UsersDeletePhotoErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets user presence information.
     * @param  token  Required parameter: Authentication token. Requires scope: `users:read`
     * @param  user  Optional parameter: User to get presence info on. Defaults to the authed user.
     * @return    Returns the ApiMethodUsersGetPresence wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<ApiMethodUsersGetPresence> usersGetPresence(
            final String token,
            final String user) throws ApiException, IOException {
        return prepareUsersGetPresenceRequest(token, user).execute();
    }

    /**
     * Gets user presence information.
     * @param  token  Required parameter: Authentication token. Requires scope: `users:read`
     * @param  user  Optional parameter: User to get presence info on. Defaults to the authed user.
     * @return    Returns the ApiMethodUsersGetPresence wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<ApiMethodUsersGetPresence>> usersGetPresenceAsync(
            final String token,
            final String user) {
        try {
            return prepareUsersGetPresenceRequest(token, user).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for usersGetPresence.
     */
    private ApiCall<ApiResponse<ApiMethodUsersGetPresence>, ApiException> prepareUsersGetPresenceRequest(
            final String token,
            final String user) {
        return new ApiCall.Builder<ApiResponse<ApiMethodUsersGetPresence>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/users.getPresence")
                        .queryParam(param -> param.key("token")
                                .value(token))
                        .queryParam(param -> param.key("user")
                                .value(user).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, ApiMethodUsersGetPresence.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new UsersCountsErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Get a user's identity.
     * @param  token  Optional parameter: Authentication token. Requires scope: `identity.basic`
     * @return    Returns the DynamicResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DynamicResponse> usersIdentity(
            final String token) throws ApiException, IOException {
        return prepareUsersIdentityRequest(token).execute();
    }

    /**
     * Get a user's identity.
     * @param  token  Optional parameter: Authentication token. Requires scope: `identity.basic`
     * @return    Returns the DynamicResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DynamicResponse>> usersIdentityAsync(
            final String token) {
        try {
            return prepareUsersIdentityRequest(token).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for usersIdentity.
     */
    private ApiCall<ApiResponse<DynamicResponse>, ApiException> prepareUsersIdentityRequest(
            final String token) {
        return new ApiCall.Builder<ApiResponse<DynamicResponse>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/users.identity")
                        .queryParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.DYNAMIC_API_RESPONSE)
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new UsersIdentityErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets information about a user.
     * @param  token  Required parameter: Authentication token. Requires scope: `users:read`
     * @param  includeLocale  Optional parameter: Set this to `true` to receive the locale for this
     *         user. Defaults to `false`
     * @param  user  Optional parameter: User to get info on
     * @return    Returns the UsersInfoSuccessSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<UsersInfoSuccessSchema> usersInfo(
            final String token,
            final Boolean includeLocale,
            final String user) throws ApiException, IOException {
        return prepareUsersInfoRequest(token, includeLocale, user).execute();
    }

    /**
     * Gets information about a user.
     * @param  token  Required parameter: Authentication token. Requires scope: `users:read`
     * @param  includeLocale  Optional parameter: Set this to `true` to receive the locale for this
     *         user. Defaults to `false`
     * @param  user  Optional parameter: User to get info on
     * @return    Returns the UsersInfoSuccessSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<UsersInfoSuccessSchema>> usersInfoAsync(
            final String token,
            final Boolean includeLocale,
            final String user) {
        try {
            return prepareUsersInfoRequest(token, includeLocale, user).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for usersInfo.
     */
    private ApiCall<ApiResponse<UsersInfoSuccessSchema>, ApiException> prepareUsersInfoRequest(
            final String token,
            final Boolean includeLocale,
            final String user) {
        return new ApiCall.Builder<ApiResponse<UsersInfoSuccessSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/users.info")
                        .queryParam(param -> param.key("token")
                                .value(token))
                        .queryParam(param -> param.key("include_locale")
                                .value(includeLocale).isRequired(false))
                        .queryParam(param -> param.key("user")
                                .value(user).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, UsersInfoSuccessSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new UsersInfoErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Lists all users in a Slack team.
     * @param  token  Optional parameter: Authentication token. Requires scope: `users:read`
     * @param  limit  Optional parameter: The maximum number of items to return. Fewer than the
     *         requested number of items may be returned, even if the end of the users list hasn't
     *         been reached. Providing no `limit` value will result in Slack attempting to deliver
     *         you the entire result set. If the collection is too large you may experience
     *         `limit_required` or HTTP 500 errors.
     * @param  cursor  Optional parameter: Paginate through collections of data by setting the
     *         `cursor` parameter to a `next_cursor` attribute returned by a previous request's
     *         `response_metadata`. Default value fetches the first "page" of the collection. See
     *         [pagination](/docs/pagination) for more detail.
     * @param  includeLocale  Optional parameter: Set this to `true` to receive the locale for
     *         users. Defaults to `false`
     * @return    Returns the UsersListSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<UsersListSchema> usersList(
            final String token,
            final Integer limit,
            final String cursor,
            final Boolean includeLocale) throws ApiException, IOException {
        return prepareUsersListRequest(token, limit, cursor, includeLocale).execute();
    }

    /**
     * Lists all users in a Slack team.
     * @param  token  Optional parameter: Authentication token. Requires scope: `users:read`
     * @param  limit  Optional parameter: The maximum number of items to return. Fewer than the
     *         requested number of items may be returned, even if the end of the users list hasn't
     *         been reached. Providing no `limit` value will result in Slack attempting to deliver
     *         you the entire result set. If the collection is too large you may experience
     *         `limit_required` or HTTP 500 errors.
     * @param  cursor  Optional parameter: Paginate through collections of data by setting the
     *         `cursor` parameter to a `next_cursor` attribute returned by a previous request's
     *         `response_metadata`. Default value fetches the first "page" of the collection. See
     *         [pagination](/docs/pagination) for more detail.
     * @param  includeLocale  Optional parameter: Set this to `true` to receive the locale for
     *         users. Defaults to `false`
     * @return    Returns the UsersListSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<UsersListSchema>> usersListAsync(
            final String token,
            final Integer limit,
            final String cursor,
            final Boolean includeLocale) {
        try {
            return prepareUsersListRequest(token, limit, cursor, includeLocale).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for usersList.
     */
    private ApiCall<ApiResponse<UsersListSchema>, ApiException> prepareUsersListRequest(
            final String token,
            final Integer limit,
            final String cursor,
            final Boolean includeLocale) {
        return new ApiCall.Builder<ApiResponse<UsersListSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/users.list")
                        .queryParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .queryParam(param -> param.key("limit")
                                .value(limit).isRequired(false))
                        .queryParam(param -> param.key("cursor")
                                .value(cursor).isRequired(false))
                        .queryParam(param -> param.key("include_locale")
                                .value(includeLocale).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, UsersListSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new UsersListErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Find a user with an email address.
     * @param  token  Required parameter: Authentication token. Requires scope: `users:read.email`
     * @param  email  Required parameter: An email address belonging to a user in the workspace
     * @return    Returns the UsersLookupByEmailSuccessSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<UsersLookupByEmailSuccessSchema> usersLookupByEmail(
            final String token,
            final String email) throws ApiException, IOException {
        return prepareUsersLookupByEmailRequest(token, email).execute();
    }

    /**
     * Find a user with an email address.
     * @param  token  Required parameter: Authentication token. Requires scope: `users:read.email`
     * @param  email  Required parameter: An email address belonging to a user in the workspace
     * @return    Returns the UsersLookupByEmailSuccessSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<UsersLookupByEmailSuccessSchema>> usersLookupByEmailAsync(
            final String token,
            final String email) {
        try {
            return prepareUsersLookupByEmailRequest(token, email).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for usersLookupByEmail.
     */
    private ApiCall<ApiResponse<UsersLookupByEmailSuccessSchema>, ApiException> prepareUsersLookupByEmailRequest(
            final String token,
            final String email) {
        return new ApiCall.Builder<ApiResponse<UsersLookupByEmailSuccessSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/users.lookupByEmail")
                        .queryParam(param -> param.key("token")
                                .value(token))
                        .queryParam(param -> param.key("email")
                                .value(email))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, UsersLookupByEmailSuccessSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new UsersLookupByEmailErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Marked a user as active. Deprecated and non-functional.
     * @param  token  Required parameter: Authentication token. Requires scope: `users:write`
     * @return    Returns the UsersSetActiveSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<UsersSetActiveSchema> usersSetActive(
            final String token) throws ApiException, IOException {
        return prepareUsersSetActiveRequest(token).execute();
    }

    /**
     * Marked a user as active. Deprecated and non-functional.
     * @param  token  Required parameter: Authentication token. Requires scope: `users:write`
     * @return    Returns the UsersSetActiveSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<UsersSetActiveSchema>> usersSetActiveAsync(
            final String token) {
        try {
            return prepareUsersSetActiveRequest(token).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for usersSetActive.
     */
    private ApiCall<ApiResponse<UsersSetActiveSchema>, ApiException> prepareUsersSetActiveRequest(
            final String token) {
        return new ApiCall.Builder<ApiResponse<UsersSetActiveSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/users.setActive")
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, UsersSetActiveSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new UsersSetActiveErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Set the user profile photo.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `users.profile:write`
     * @param  cropW  Optional parameter: Width/height of crop box (always square)
     * @param  cropX  Optional parameter: X coordinate of top-left corner of crop box
     * @param  cropY  Optional parameter: Y coordinate of top-left corner of crop box
     * @param  image  Optional parameter: File contents via `multipart/form-data`.
     * @return    Returns the UsersSetPhotoSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<UsersSetPhotoSchema> usersSetPhoto(
            final String token,
            final String cropW,
            final String cropX,
            final String cropY,
            final String image) throws ApiException, IOException {
        return prepareUsersSetPhotoRequest(token, cropW, cropX, cropY, image).execute();
    }

    /**
     * Set the user profile photo.
     * @param  token  Required parameter: Authentication token. Requires scope:
     *         `users.profile:write`
     * @param  cropW  Optional parameter: Width/height of crop box (always square)
     * @param  cropX  Optional parameter: X coordinate of top-left corner of crop box
     * @param  cropY  Optional parameter: Y coordinate of top-left corner of crop box
     * @param  image  Optional parameter: File contents via `multipart/form-data`.
     * @return    Returns the UsersSetPhotoSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<UsersSetPhotoSchema>> usersSetPhotoAsync(
            final String token,
            final String cropW,
            final String cropX,
            final String cropY,
            final String image) {
        try {
            return prepareUsersSetPhotoRequest(token, cropW, cropX, cropY, image).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for usersSetPhoto.
     */
    private ApiCall<ApiResponse<UsersSetPhotoSchema>, ApiException> prepareUsersSetPhotoRequest(
            final String token,
            final String cropW,
            final String cropX,
            final String cropY,
            final String image) {
        return new ApiCall.Builder<ApiResponse<UsersSetPhotoSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/users.setPhoto")
                        .formParam(param -> param.key("token")
                                .value(token))
                        .formParam(param -> param.key("crop_w")
                                .value(cropW).isRequired(false))
                        .formParam(param -> param.key("crop_x")
                                .value(cropX).isRequired(false))
                        .formParam(param -> param.key("crop_y")
                                .value(cropY).isRequired(false))
                        .formParam(param -> param.key("image")
                                .value(image).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, UsersSetPhotoSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new UsersSetPhotoErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Manually sets user presence.
     * @param  token  Required parameter: Authentication token. Requires scope: `users:write`
     * @param  presence  Required parameter: Either `auto` or `away`
     * @return    Returns the UsersSetPresenceSchema wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<UsersSetPresenceSchema> usersSetPresence(
            final String token,
            final String presence) throws ApiException, IOException {
        return prepareUsersSetPresenceRequest(token, presence).execute();
    }

    /**
     * Manually sets user presence.
     * @param  token  Required parameter: Authentication token. Requires scope: `users:write`
     * @param  presence  Required parameter: Either `auto` or `away`
     * @return    Returns the UsersSetPresenceSchema wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<UsersSetPresenceSchema>> usersSetPresenceAsync(
            final String token,
            final String presence) {
        try {
            return prepareUsersSetPresenceRequest(token, presence).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for usersSetPresence.
     */
    private ApiCall<ApiResponse<UsersSetPresenceSchema>, ApiException> prepareUsersSetPresenceRequest(
            final String token,
            final String presence) {
        return new ApiCall.Builder<ApiResponse<UsersSetPresenceSchema>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/users.setPresence")
                        .formParam(param -> param.key("presence")
                                .value(presence))
                        .headerParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, UsersSetPresenceSchema.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new UsersSetPresenceErrorSchemaException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}