/*
 * SlackWebApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.slack.controllers;

import com.slack.ApiHelper;
import com.slack.Server;
import com.slack.exceptions.ApiException;
import com.slack.exceptions.DefaultErrorTemplateException;
import com.slack.http.request.HttpMethod;
import com.slack.http.response.ApiResponse;
import com.slack.models.DefaultSuccessTemplate;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class OauthController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public OauthController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Exchanges a temporary OAuth verifier code for an access token.
     * @param  clientId  Optional parameter: Issued when you created your application.
     * @param  clientSecret  Optional parameter: Issued when you created your application.
     * @param  code  Optional parameter: The `code` param returned via the OAuth callback.
     * @param  redirectUri  Optional parameter: This must match the originally submitted URI (if one
     *         was sent).
     * @param  singleChannel  Optional parameter: Request the user to add your app only to a single
     *         channel. Only valid with a [legacy workspace
     *         app](https://api.slack.com/legacy-workspace-apps).
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> oauthAccess(
            final String clientId,
            final String clientSecret,
            final String code,
            final String redirectUri,
            final Boolean singleChannel) throws ApiException, IOException {
        return prepareOauthAccessRequest(clientId, clientSecret, code, redirectUri,
                singleChannel).execute();
    }

    /**
     * Exchanges a temporary OAuth verifier code for an access token.
     * @param  clientId  Optional parameter: Issued when you created your application.
     * @param  clientSecret  Optional parameter: Issued when you created your application.
     * @param  code  Optional parameter: The `code` param returned via the OAuth callback.
     * @param  redirectUri  Optional parameter: This must match the originally submitted URI (if one
     *         was sent).
     * @param  singleChannel  Optional parameter: Request the user to add your app only to a single
     *         channel. Only valid with a [legacy workspace
     *         app](https://api.slack.com/legacy-workspace-apps).
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> oauthAccessAsync(
            final String clientId,
            final String clientSecret,
            final String code,
            final String redirectUri,
            final Boolean singleChannel) {
        try {
            return prepareOauthAccessRequest(clientId, clientSecret, code, redirectUri,
            singleChannel).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for oauthAccess.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareOauthAccessRequest(
            final String clientId,
            final String clientSecret,
            final String code,
            final String redirectUri,
            final Boolean singleChannel) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/oauth.access")
                        .queryParam(param -> param.key("client_id")
                                .value(clientId).isRequired(false))
                        .queryParam(param -> param.key("client_secret")
                                .value(clientSecret).isRequired(false))
                        .queryParam(param -> param.key("code")
                                .value(code).isRequired(false))
                        .queryParam(param -> param.key("redirect_uri")
                                .value(redirectUri).isRequired(false))
                        .queryParam(param -> param.key("single_channel")
                                .value(singleChannel).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Exchanges a temporary OAuth verifier code for a workspace token.
     * @param  clientId  Optional parameter: Issued when you created your application.
     * @param  clientSecret  Optional parameter: Issued when you created your application.
     * @param  code  Optional parameter: The `code` param returned via the OAuth callback.
     * @param  redirectUri  Optional parameter: This must match the originally submitted URI (if one
     *         was sent).
     * @param  singleChannel  Optional parameter: Request the user to add your app only to a single
     *         channel.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DefaultSuccessTemplate> oauthToken(
            final String clientId,
            final String clientSecret,
            final String code,
            final String redirectUri,
            final Boolean singleChannel) throws ApiException, IOException {
        return prepareOauthTokenRequest(clientId, clientSecret, code, redirectUri,
                singleChannel).execute();
    }

    /**
     * Exchanges a temporary OAuth verifier code for a workspace token.
     * @param  clientId  Optional parameter: Issued when you created your application.
     * @param  clientSecret  Optional parameter: Issued when you created your application.
     * @param  code  Optional parameter: The `code` param returned via the OAuth callback.
     * @param  redirectUri  Optional parameter: This must match the originally submitted URI (if one
     *         was sent).
     * @param  singleChannel  Optional parameter: Request the user to add your app only to a single
     *         channel.
     * @return    Returns the DefaultSuccessTemplate wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DefaultSuccessTemplate>> oauthTokenAsync(
            final String clientId,
            final String clientSecret,
            final String code,
            final String redirectUri,
            final Boolean singleChannel) {
        try {
            return prepareOauthTokenRequest(clientId, clientSecret, code, redirectUri,
            singleChannel).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for oauthToken.
     */
    private ApiCall<ApiResponse<DefaultSuccessTemplate>, ApiException> prepareOauthTokenRequest(
            final String clientId,
            final String clientSecret,
            final String code,
            final String redirectUri,
            final Boolean singleChannel) {
        return new ApiCall.Builder<ApiResponse<DefaultSuccessTemplate>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/oauth.token")
                        .queryParam(param -> param.key("client_id")
                                .value(clientId).isRequired(false))
                        .queryParam(param -> param.key("client_secret")
                                .value(clientSecret).isRequired(false))
                        .queryParam(param -> param.key("code")
                                .value(code).isRequired(false))
                        .queryParam(param -> param.key("redirect_uri")
                                .value(redirectUri).isRequired(false))
                        .queryParam(param -> param.key("single_channel")
                                .value(singleChannel).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("slackAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DefaultSuccessTemplate.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Typical error response",
                                (reason, context) -> new DefaultErrorTemplateException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}